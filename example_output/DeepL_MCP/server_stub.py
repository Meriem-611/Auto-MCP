# This file is auto-generated by mcp_generator.py.
# MCP server stub generated from OpenAPI spec.
import os
import json
import re
from typing import Optional, Union, Any
from dotenv import load_dotenv
load_dotenv()
from fastmcp import FastMCP
import requests
import sys
# MCP servers must only output JSON-RPC to stdout, so use stderr for logging
print("[MCP SERVER] FastMCP server running.", file=sys.stderr)
if __name__ == "__main__":
    mcp = FastMCP("my-server")

    @mcp.tool(name='listGlossaryLanguages', description="List Language Pairs Supported by Glossaries: Retrieve the list of language pairs supported by the glossary feature.")
    def listGlossaryLanguages():
        '''Retrieve the list of language pairs supported by the glossary feature.'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v2/glossary-language-pairs"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        try:
            resp = requests.get(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='listMultilingualGlossaries', description="List all Glossaries: List: List all glossaries and their meta-information, but not the glossary entries. | Get by ID: Retrieve meta information for a single glossary, omitting the glossary entries.")
    def listMultilingualGlossaries(glossary_id: Optional[str] = None):
        '''List: List all glossaries and their meta-information, but not the glossary entries. | Get by ID: Retrieve meta information for a single glossary, omitting the glossary entries.'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        # Merged operation: use detail path if ID provided, collection path otherwise
        if glossary_id is not None:
            api_url = f"{base_url}/v3/glossaries/{glossary_id}"
        else:
            api_url = f"{base_url}/v3/glossaries"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        try:
            resp = requests.get(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='createMultilingualGlossary', description="Create a Glossary\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (required)\n    Name to be associated with the glossary.\n  - dictionaries (array of object) (required)\n    Dictionaries to populate the glossary with.\n\nExample request body:\n```json\n{\n  \"name\": \"My Glossary\",\n  \"dictionaries\": [\n    {\n      \"source_lang\": \"en\",\n      \"target_lang\": \"de\",\n      \"entries\": \"Hello\\tGuten Tag\",\n      \"entries_format\": \"tsv\"\n    },\n    {\n      \"source_lang\": \"de\",\n      \"target_lang\": \"en\",\n      \"entries\": \"Guten Tag\\tHello\",\n      \"entries_format\": \"tsv\"\n    }\n  ]\n}\n```")
    def createMultilingualGlossary(body: Optional[dict] = None):
        '''Auto-generated MCP handler for POST /v3/glossaries'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v3/glossaries"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='getMultilingualGlossaryEntries', description="Retrieve Glossary Entries: List the entries of a single glossary in tsv format.")
    def getMultilingualGlossaryEntries(glossary_id: str, source_lang: Optional[str] = None, target_lang: Optional[str] = None):
        '''List the entries of a single glossary in tsv format.'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v3/glossaries/{glossary_id}/entries"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if glossary_id is None:
            raise ValueError('Missing required path parameter: glossary_id')
        if source_lang is not None:
            params['source_lang'] = source_lang
        if target_lang is not None:
            params['target_lang'] = target_lang
        try:
            resp = requests.get(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='listGlossaries', description="List all Glossaries: List: List all glossaries and their meta-information, but not the glossary entries. | Get by ID: Retrieve meta information for a single glossary, omitting the glossary entries.")
    def listGlossaries(glossary_id: Optional[str] = None):
        '''List: List all glossaries and their meta-information, but not the glossary entries. | Get by ID: Retrieve meta information for a single glossary, omitting the glossary entries.'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        # Merged operation: use detail path if ID provided, collection path otherwise
        if glossary_id is not None:
            api_url = f"{base_url}/v2/glossaries/{glossary_id}"
        else:
            api_url = f"{base_url}/v2/glossaries"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        try:
            resp = requests.get(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='createGlossary', description="Create a Glossary\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (required)\n    Name to be associated with the glossary.\n  - source_lang (string) (required)\n    The language in which the source texts in the glossary are specified.\n  - target_lang (string) (required)\n    The language in which the target texts in the glossary are specified.\n  - entries (string) (required)\n    The entries of the glossary. The entries have to be specified in the format provided by the `entries\n  - entries_format (string) (required)\n    The format in which the glossary entries are provided. Formats currently available:\n- `tsv` (default\n\nExample request body:\n```json\n{\n  \"name\": \"My Glossary\",\n  \"source_lang\": \"en\",\n  \"target_lang\": \"de\",\n  \"entries\": \"Hello\\tGuten Tag\",\n  \"entries_format\": \"tsv\"\n}\n```")
    def createGlossary(body: Optional[dict] = None):
        '''Auto-generated MCP handler for POST /v2/glossaries'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v2/glossaries"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='getGlossaryEntries', description="Retrieve Glossary Entries: List the entries of a single glossary in the format specified by the `Accept` header.")
    def getGlossaryEntries(glossary_id: str, Accept: Optional[str] = None):
        '''List the entries of a single glossary in the format specified by the `Accept` header.'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v2/glossaries/{glossary_id}/entries"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if glossary_id is None:
            raise ValueError('Missing required path parameter: glossary_id')
        header_value = Accept if Accept is not None else os.getenv('ACCEPT')
        if header_value is not None:
            headers['Accept'] = header_value
        try:
            resp = requests.get(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='getUsage', description="Check Usage and Limits: Retrieve usage information within the current billing period together with the corresponding account limits. Usage is returned for:\n- translated characters\n- translated documents\n- translated documents, team totals (for team accounts only)\n\nCharacter usage includes both text and document translations, and is measured by the source text length in Unicode code points,\nso for example \"A\", \"\u0394\", \"\u3042\", and \"\u6df1\" are each counted as a single character.\n\nDocument usage only includes document translations, and is measured in individual documents.\n\nDepending on the user account type, some usage types will be omitted.\nCharacter usage is only included for developer accounts.\nDocument usage is only included for non-developer accounts, and team-combined document usage is only included for non-developer team accounts.")
    def getUsage():
        '''Retrieve usage information within the current billing period together with the corresponding account limits. Usage is returned for: - translated characters - translated documents - translated documents, team totals (for team accounts only)  Character usage includes both text and document translations, and is measured by the source text length in Unicode code points, so for example "A", "Δ", "あ", and "深" are each counted as a single character.  Document usage only includes document translations, and is measured in individual documents.  Depending on the user account type, some usage types will be omitted. Character usage is only included for developer accounts. Document usage is only included for non-developer accounts, and team-combined document usage is only included for non-developer team accounts.'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v2/usage"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        try:
            resp = requests.get(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='getLanguages', description="Retrieve Supported Languages: Retrieve the list of languages that are currently supported for translation, either as source or target language, respectively.\n\nAs of May 2025, Hebrew (HE), Thai (TH), and Vietnamese (VI) are supported as a source and target language for text translation, \nbut it is not yet supported for document translation. Therefore, they have not yet been added to the `/languages` endpoint. We \nwill add Hebrew, Thai, and Vietnamese to the `/languages` endpoint after document translation support is added.")
    def getLanguages(type: Optional[str] = None):
        '''Retrieve the list of languages that are currently supported for translation, either as source or target language, respectively.  As of May 2025, Hebrew (HE), Thai (TH), and Vietnamese (VI) are supported as a source and target language for text translation,  but it is not yet supported for document translation. Therefore, they have not yet been added to the `/languages` endpoint. We  will add Hebrew, Thai, and Vietnamese to the `/languages` endpoint after document translation support is added.'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v2/languages"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if type is not None:
            params['type'] = type
        try:
            resp = requests.get(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='adminGetDeveloperKeys', description="Get all developer keys as an admin")
    def adminGetDeveloperKeys():
        '''Auto-generated MCP handler for GET /v2/admin/developer-keys'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v2/admin/developer-keys"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        try:
            resp = requests.get(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='adminCreateDeveloperKey', description="Create a developer key as an admin\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - label (string) (optional)\n    API key label. The default value is `DeepL API Key`.")
    def adminCreateDeveloperKey(body: Optional[dict] = None):
        '''Auto-generated MCP handler for POST /v2/admin/developer-keys'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v2/admin/developer-keys"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='adminGetAnalytics', description="Get usage statistics as an admin: Retrieve usage statistics for the organization within a specified date range.\nOptionally group the results by API key or by API key and day.")
    def adminGetAnalytics(start_date: Optional[str] = None, end_date: Optional[str] = None, group_by: Optional[str] = None):
        '''Retrieve usage statistics for the organization within a specified date range. Optionally group the results by API key or by API key and day.'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v2/admin/analytics"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if start_date is not None:
            params['start_date'] = start_date
        if end_date is not None:
            params['end_date'] = end_date
        if group_by is not None:
            params['group_by'] = group_by
        try:
            resp = requests.get(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='translateText', description="Request Translation: The translate function.\n\n\nThe total request body size must not exceed 128 KiB (128 \u00b7 1024 bytes). Please split up your text into multiple\ncalls if it exceeds this limit.\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - text (array of string) (required)\n    Text to be translated. Only UTF-8-encoded plain text is supported. The parameter may be specified\nmu\n  - source_lang (string) (optional)\n    Language of the text to be translated. Options currently available:\n * `AR` - Arabic\n * `BG` - Bulga\n  - target_lang (string) (required)\n    The language into which the text should be translated. Options currently available:\n * `AR` - Arabic\n  - context (string) (optional)\n    The `context` parameter makes it possible to include additional context that can influence a transla\n  - show_billed_characters (boolean) (optional)\n    When true, the response will include the billed_characters parameter, giving the \nnumber of characte\n  - split_sentences (string) (optional)\n    Sets whether the translation engine should first split the input into sentences. For text translatio\n  - preserve_formatting (boolean) (optional)\n    Sets whether the translation engine should respect the original formatting, even if it would usually\n  - formality (string) (optional)\n    Sets whether the translated text should lean towards formal or informal language.\nThis feature curre\n  - model_type (string) (optional)\n    Specifies which DeepL model should be used for translation.\n\nPossible values are:\n* `latency_optimiz\n  - glossary_id (any) (optional)\n  - tag_handling (string) (optional)\n    Sets which kind of tags should be handled. Options currently available:\n * `xml`: Enable XML tag han\n  - outline_detection (boolean) (optional)\n    The automatic detection of the XML structure won't yield best results in all XML files. You can disa\n  - non_splitting_tags (any) (optional)\n    List of XML tags which never split sentences.\n\n\nFor some XML files, finding tags with textual conten\n  - splitting_tags (any) (optional)\n    List of XML tags which always cause splits.\n\n\nSee the example in the `outline_detection` parameter's\n  - ignore_tags (any) (optional)\n    List of XML tags that indicate text not to be translated.\n\n\nUse this parameter to ensure that elemen")
    def translateText(body: Optional[dict] = None):
        '''The translate function.   The total request body size must not exceed 128 KiB (128 · 1024 bytes). Please split up your text into multiple calls if it exceeds this limit.'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v2/translate"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='translateDocument', description="Upload and Translate a Document: This call uploads a document and queues it for translation.\nThe call returns once the upload is complete, returning a document ID and key which can be used to\n[query the translation status](https://www.deepl.com/docs-api/documents/get-document-status)\nand to [download the translated document](https://www.deepl.com/docs-api/documents/download-document) once translation is complete.\n\n\n\nBecause the request includes a file upload, it must be an HTTP POST request with content type `multipart/form-data`.\n\n\nPlease be aware that the uploaded document is automatically removed from the server once the translated document has been downloaded.\nYou have to upload the document again in order to restart the translation.\n\n\nThe maximum upload limit for documents is [available here](https://support.deepl.com/hc/articles/360020582359-Document-formats)\nand may vary based on API plan and document type.\n\n\nYou may specify the glossary to use for the document translation using the `glossary_id` parameter.\n**Important:** This requires the `source_lang` parameter to be set and the language pair of the glossary has to match the language pair of the request.\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - source_lang (string) (optional)\n    Language of the text to be translated. Options currently available:\n * `AR` - Arabic\n * `BG` - Bulga\n  - target_lang (string) (required)\n    The language into which the text should be translated. Options currently available:\n * `AR` - Arabic\n  - file (string) (required)\n    The document file to be translated. The file name should be included in this part's content disposit\n  - filename (string) (optional)\n    The name of the uploaded file. Can be used as an alternative to including the file name in the file \n  - output_format (string) (optional)\n    File extension of desired format of translated file, for example: `docx`. If unspecified, by default\n  - formality (string) (optional)\n    Sets whether the translated text should lean towards formal or informal language.\nThis feature curre\n  - glossary_id (string) (optional)\n    A unique ID assigned to a glossary.\n\nExample request body:\n```json\n{\n  \"target_lang\": \"DE\",\n  \"file\": \"@document.docx\"\n}\n```")
    def translateDocument(body: Optional[dict] = None):
        '''This call uploads a document and queues it for translation. The call returns once the upload is complete, returning a document ID and key which can be used to [query the translation status](https://www.deepl.com/docs-api/documents/get-document-status) and to [download the translated document](https://www.deepl.com/docs-api/documents/download-document) once translation is complete.    Because the request includes a file upload, it must be an HTTP POST request with content type `multipart/form-data`.   Please be aware that the uploaded document is automatically removed from the server once the translated document has been downloaded. You have to upload the document again in order to restart the translation.   The maximum upload limit for documents is [available here](https://support.deepl.com/hc/articles/360020582359-Document-formats) and may vary based on API plan and document type.   You may specify the glossary to use for the document translation using the `glossary_id` parameter. **Important:** This requires the `source_lang` parameter to be set and the language pair of the glossary has to match the language pair of the request.'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v2/document"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'multipart/form-data'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='getDocumentStatus', description="Check Document Status: Retrieve the current status of a document translation process.\nIf the translation is still in progress, the estimated time remaining is also included in the response.\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - document_key (string) (required)\n    The document encryption key that was sent to the client when the document was uploaded to the API.")
    def getDocumentStatus(document_id: str, body: Optional[dict] = None):
        '''Retrieve the current status of a document translation process. If the translation is still in progress, the estimated time remaining is also included in the response.'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v2/document/{document_id}"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if document_id is None:
            raise ValueError('Missing required path parameter: document_id')
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/x-www-form-urlencoded'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='downloadDocument', description="Download Translated Document: Once the status of the document translation process is `done`, the result can be downloaded.\n\n\nFor privacy reasons the translated document is automatically removed from the server once it was downloaded and cannot be downloaded again.\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - document_key (string) (required)\n    The document encryption key that was sent to the client when the document was uploaded to the API.")
    def downloadDocument(document_id: str, body: Optional[dict] = None):
        '''Once the status of the document translation process is `done`, the result can be downloaded.   For privacy reasons the translated document is automatically removed from the server once it was downloaded and cannot be downloaded again.'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v2/document/{document_id}/result"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if document_id is None:
            raise ValueError('Missing required path parameter: document_id')
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/x-www-form-urlencoded'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='patchMultilingualGlossary', description="Edit glossary details: Edit glossary details, such as name or a dictionary for a source and target language.\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (optional)\n    A unique ID assigned to a glossary.\n  - dictionaries (array of object) (optional)\n    Dictionaries to edit the glossary with. Currently only supports 0 or 1 dictionaries in the array.\n\nExample request body:\n```json\n{\n  \"name\": \"My Glossary\",\n  \"dictionaries\": [\n    {\n      \"source_lang\": \"en\",\n      \"target_lang\": \"de\",\n      \"entries\": \"Hello\\tGuten Tag\",\n      \"entries_format\": \"tsv\"\n    }\n  ]\n}\n```")
    def patchMultilingualGlossary(glossary_id: str, body: Optional[dict] = None):
        '''Edit glossary details, such as name or a dictionary for a source and target language.'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v3/glossaries/{glossary_id}"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if glossary_id is None:
            raise ValueError('Missing required path parameter: glossary_id')
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.patch(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='replaceDictionary', description="Replaces or creates a dictionary in the glossary with the specified entries.\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - source_lang (string) (optional)\n    The language in which the source texts in the glossary are specified.\n  - target_lang (string) (optional)\n    The language in which the target texts in the glossary are specified.\n  - entries (string) (optional)\n    The entries of the glossary. The entries have to be specified in the format provided by the `entries\n  - entries_format (string) (optional)\n    The format in which the glossary entries are provided. Formats currently available:\n- `tsv` (default")
    def replaceDictionary(glossary_id: str, body: Optional[dict] = None):
        '''Auto-generated MCP handler for PUT /v3/glossaries/{glossary_id}/dictionaries'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v3/glossaries/{glossary_id}/dictionaries"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if glossary_id is None:
            raise ValueError('Missing required path parameter: glossary_id')
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.put(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='rephraseText', description="Request text improvement\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - text (array of string) (required)\n    Text to be improved. Only UTF-8-encoded plain text is supported. Improvements are returned in the sa\n  - target_lang (string) (optional)\n    The language for the text improvement. Options currently available:\n * `de` - German\n * `en` - Engli\n  - writing_style (string) (optional)\n    Specify a style to rephrase your text in a way that fits your audience and goals.\nThe `prefer_` pref\n  - tone (string) (optional)\n    Specify the desired tone for your text.\nThe `prefer_` prefix allows falling back to the default tone")
    def rephraseText(body: Optional[dict] = None):
        '''Auto-generated MCP handler for POST /v2/write/rephrase'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v2/write/rephrase"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='adminDeactivateDeveloperKey', description="Deactivate a developer key as an admin\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - key_id (string) (required)\n    API key ID. Consists of two valid GUIDs separated by a colon.")
    def adminDeactivateDeveloperKey(body: Optional[dict] = None):
        '''Auto-generated MCP handler for PUT /v2/admin/developer-keys/deactivate'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v2/admin/developer-keys/deactivate"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.put(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='adminRenameDeveloperKey', description="Rename a developer key as an admin\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - key_id (string) (required)\n    API key ID. Consists of two valid GUIDs separated by a colon.\n  - label (string) (required)\n    API key label.")
    def adminRenameDeveloperKey(body: Optional[dict] = None):
        '''Auto-generated MCP handler for PUT /v2/admin/developer-keys/label'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v2/admin/developer-keys/label"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.put(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='adminSetDeveloperKeyUsageLimits', description="Set developer key usage limits as an admin\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - key_id (string) (required)\n    API key ID. Consists of two valid GUIDs separated by a colon.\n  - characters (number) (optional)\n    Restricts the number of total characters (across text translation, document translation, and text im")
    def adminSetDeveloperKeyUsageLimits(body: Optional[dict] = None):
        '''Auto-generated MCP handler for PUT /v2/admin/developer-keys/limits'''
        base_url = os.getenv("BASE_URL", "https://api.deepl.com")
        api_url = f"{base_url}/v2/admin/developer-keys/limits"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                api_key_auth_header = os.getenv('AUTH_HEADER_API_KEY')
                if not api_key_auth_header:
                    raise ValueError('Missing API key in .env: AUTH_HEADER_API_KEY')
                headers['Authorization'] = api_key_auth_header
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.put(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    mcp.run(transport="stdio")
