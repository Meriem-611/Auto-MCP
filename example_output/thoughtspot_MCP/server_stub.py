# This file is auto-generated by mcp_generator.py.
# MCP server stub generated from OpenAPI spec.
import os
import json
import re
from typing import Optional, Union, Any
from dotenv import load_dotenv
load_dotenv()
from fastmcp import FastMCP
import requests
import sys
# MCP servers must only output JSON-RPC to stdout, so use stderr for logging
print("[MCP SERVER] FastMCP server running.", file=sys.stderr)
if __name__ == "__main__":
    mcp = FastMCP("my-server")

    @mcp.tool(name='getSystemConfig', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nRetrieves the current configuration details of the cluster. If the request is successful, the API returns a list configuration settings applied on the cluster.\n\nRequires `ADMINISTRATION`(**Can administer ThoughtSpot**) privilege to view these complete configuration settings of the cluster.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `SYSTEM_INFO_ADMINISTRATION` (**Can view system activities**) privilege is required.\n\nThis API does not require any parameters to be passed in the request.\n\n\n\n\n#### Endpoint URL\n")
    def getSystemConfig():
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Retrieves the current configuration details of the cluster. If the request is successful, the API returns a list configuration settings applied on the cluster.  Requires `ADMINISTRATION`(**Can administer ThoughtSpot**) privilege to view these complete configuration settings of the cluster. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `SYSTEM_INFO_ADMINISTRATION` (**Can view system activities**) privilege is required.  This API does not require any parameters to be passed in the request.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/system/config"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        try:
            resp = requests.get(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='getSystemInformation', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nGets system information such as the release version, locale, time zone, deployment environment, date format, and date time format of the cluster.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privileges.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `SYSTEM_INFO_ADMINISTRATION` (**Can view system activities**) privilege is required.\n\nThis API does not require any parameters to be passed in the request.  \n\n\n\n\n#### Endpoint URL\n")
    def getSystemInformation():
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Gets system information such as the release version, locale, time zone, deployment environment, date format, and date time format of the cluster.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privileges. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `SYSTEM_INFO_ADMINISTRATION` (**Can view system activities**) privilege is required.  This API does not require any parameters to be passed in the request.       #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/system"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        try:
            resp = requests.get(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='getSystemOverrideInfo', description="\n <span class=\"since-beta-tag\">Version: 9.2.0.cl or later</span>\n\nGets a list of configuration overrides applied on the cluster.  \n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `APPLICATION_ADMINISTRATION` (**Can manage application settings**) privilege is required.\n\nThis API does not require any parameters to be passed in the request.  \n\n\n\n\n#### Endpoint URL\n")
    def getSystemOverrideInfo():
        '''  <span class="since-beta-tag">Version: 9.2.0.cl or later</span>  Gets a list of configuration overrides applied on the cluster.    Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `APPLICATION_ADMINISTRATION` (**Can manage application settings**) privilege is required.  This API does not require any parameters to be passed in the request.       #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/system/config-overrides"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        try:
            resp = requests.get(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='createAgentConversation', description="<span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.13.0.cl or later</span>\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_context (any) (required)\n    Context for the conversation.\n  - conversation_settings (any) (required)\n    Conversation settings.")
    def createAgentConversation(body: Optional[dict] = None):
        '''<span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.13.0.cl or later</span>'''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/ai/agent/conversation/create"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='createConversation', description="\n<span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.4.0.cl or later</span>\n\nCreates a Conversation object to start an AI-driven conversation based on a specific data model.\n\nRequires at least view access to the metadata object specified in the request.\n\n#### Usage guidelines\n\nThis API requires the `metadata_identifier` parameter to define the context for the conversation.\n\nYou can also specify the tokens to initiate the conversation as shown in this example:\n\n`\"tokens\": \"[tea],[sales],[type]\"`\n\nIf the API request is successful, ThoughtSpot returns the ID of the conversation.\n\n> ###### Note:\n> * This endpoint is currently in Beta. Breaking changes may be introduced before the endpoint is made Generally Available.\n> * This endpoint requires Spotter - please contact ThoughtSpot support to enable Spotter on your cluster.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_identifier (string) (required)\n    ID of the metadata object, such as a Worksheet or Model, to use as a data source for the conversatio\n  - tokens (string) (optional)\n    Token string to set the context for the conversation. For example,`[sales],[item type],[state]`.")
    def createConversation(body: Optional[dict] = None):
        ''' <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.4.0.cl or later</span>  Creates a Conversation object to start an AI-driven conversation based on a specific data model.  Requires at least view access to the metadata object specified in the request.  #### Usage guidelines  This API requires the `metadata_identifier` parameter to define the context for the conversation.  You can also specify the tokens to initiate the conversation as shown in this example:  `"tokens": "[tea],[sales],[type]"`  If the API request is successful, ThoughtSpot returns the ID of the conversation.  > ###### Note: > * This endpoint is currently in Beta. Breaking changes may be introduced before the endpoint is made Generally Available. > * This endpoint requires Spotter - please contact ThoughtSpot support to enable Spotter on your cluster.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/ai/conversation/create"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='getDataSourceSuggestions', description="\n<span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.13.0.cl or later</span>\n\nProvides relevant data source recommendations for a user-submitted natural language query.\n\nTo use this API, the user must have at least view-level access to the underlying metadata entities referenced in the response.\n\n#### Usage guidelines\n\nThe request must include a `query` string via the request body.\n\nThe returned results include metadata such as:\n- `confidence`: a float indicating the model's confidence in the relevance of each recommendation\n- `details`: includes `data_source_identifier`, `data_source_name`, and `description` of each recommended data source\n- `reasoning`: rationale provided by the LLM to explain why each data source was recommended\n\nIf the API request is successful, ThoughtSpot returns a ranked list of data sources, each annotated with relevant reasoning.\n\n> ###### Note:\n> * This endpoint is currently in Beta. Breaking changes may be introduced before it is made Generally Available.\n> * This endpoint requires Spotter \u2014 please contact ThoughtSpot Support to enable Spotter on your cluster.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - query (string) (required)\n    User query used to suggest data sources.")
    def getDataSourceSuggestions(body: Optional[dict] = None):
        ''' <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.13.0.cl or later</span>  Provides relevant data source recommendations for a user-submitted natural language query.  To use this API, the user must have at least view-level access to the underlying metadata entities referenced in the response.  #### Usage guidelines  The request must include a `query` string via the request body.  The returned results include metadata such as: - `confidence`: a float indicating the model's confidence in the relevance of each recommendation - `details`: includes `data_source_identifier`, `data_source_name`, and `description` of each recommended data source - `reasoning`: rationale provided by the LLM to explain why each data source was recommended  If the API request is successful, ThoughtSpot returns a ranked list of data sources, each annotated with relevant reasoning.  > ###### Note: > * This endpoint is currently in Beta. Breaking changes may be introduced before it is made Generally Available. > * This endpoint requires Spotter — please contact ThoughtSpot Support to enable Spotter on your cluster.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/ai/data-source-suggestions"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='getRelevantQuestions', description="\n<span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.13.0.cl or later</span>\n\nBreaks down a user-submitted query into a series of analytical sub-questions using relevant contextual metadata.\n\nTo use this API, the user must have at least view-level access to the referenced metadata objects.\n\n#### Usage guidelines\n\nTo accurately generate relevant questions, the request must include at least one of the following metadata identifiers within `metadata_context` : `conversation_identifier`, `answer_identifiers`, `liveboard_identifiers`, or `data_source_identifiers`.\n\nYou can further enhance the quality and precision of breakdown by providing additional `ai_context` such as:\n\n- `content`: User provided content like text data, csv data as a string message to provide context & potentially improve the quality of the response.\n- `instructions`: User specific text instructions sent to AI system for processing the query.\n\nAdditional optional parameters include:\n\n- `limit_relevant_questions`: Controls the maximum number of relevant questions returned. Defaults to 5 if not specified.\n- `bypass_cache`: If set to true, forces fresh computation instead of returning cached results.\n\nIf the API request is successful, ThoughtSpot returns a list of relevant analytical queries, each aligned with the user's original question. Each returned question includes the query string, along with the identifier and name of the corresponding data source.\n\n> ###### Note:\n> * This endpoint is currently in Beta. Breaking changes may be introduced before the endpoint is made Generally Available.\n> * This endpoint requires Spotter - please contact ThoughtSpot support to enable Spotter on your cluster.\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_context (any) (required)\n    metadata for the query to enable generation of relevant sub-questions; at least one context identifi\n  - limit_relevant_questions (integer) (optional)\n    Maximum number of relevant questions that is allowed in the response, default = 5.\n  - bypass_cache (boolean) (optional)\n    If true, results are not returned from cache & calculated every time.\n  - query (string) (required)\n    A user query that requires breaking down into smaller, more manageable analytical questions to facil\n  - ai_context (any) (optional)\n    Additional context to guide the response.")
    def getRelevantQuestions(body: Optional[dict] = None):
        ''' <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.13.0.cl or later</span>  Breaks down a user-submitted query into a series of analytical sub-questions using relevant contextual metadata.  To use this API, the user must have at least view-level access to the referenced metadata objects.  #### Usage guidelines  To accurately generate relevant questions, the request must include at least one of the following metadata identifiers within `metadata_context` : `conversation_identifier`, `answer_identifiers`, `liveboard_identifiers`, or `data_source_identifiers`.  You can further enhance the quality and precision of breakdown by providing additional `ai_context` such as:  - `content`: User provided content like text data, csv data as a string message to provide context & potentially improve the quality of the response. - `instructions`: User specific text instructions sent to AI system for processing the query.  Additional optional parameters include:  - `limit_relevant_questions`: Controls the maximum number of relevant questions returned. Defaults to 5 if not specified. - `bypass_cache`: If set to true, forces fresh computation instead of returning cached results.  If the API request is successful, ThoughtSpot returns a list of relevant analytical queries, each aligned with the user's original question. Each returned question includes the query string, along with the identifier and name of the corresponding data source.  > ###### Note: > * This endpoint is currently in Beta. Breaking changes may be introduced before the endpoint is made Generally Available. > * This endpoint requires Spotter - please contact ThoughtSpot support to enable Spotter on your cluster.    #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/ai/relevant-questions/"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='sendAgentMessageStreaming', description="\n<span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.13.0.cl or later</span>\n\nThis API allows users to initiate or continue an agent (Spotter) conversation by submitting one or more natural language messages. \nTo use this API, the user must have access to the relevant conversational session (via conversation_identifier) and submit at least one message.\n\n\n#### Usage guidelines\n\nTo initiate or continue a conversation, the request must include:\n- `conversation_identifier`: a unique session ID for continuity and message tracking\n- `messages`: an array of one or more text messages, each with a value and type\n\nAdditionally, user can specify what tool can be included `conversation_settings` parameter, which supports:\n- `enable_contextual_change_analysis` (default: false)\n- `enable_natural_language_answer_generation` (default: true)\n- `enable_reasoning` (default: false)\n\nIf the request is valid, the API returns a stream of messages in real time, including:\n- `ack`: confirms receipt of the request\n- `text / text-chunk`: content chunks, optionally formatted (e.g., markdown)\n- `answer`: the final structured response with metadata and analytics\n- `error`: if a failure occurs\n- `notification`: notification messages for operation being performed\n\n> ###### Note:\n> * This endpoint is currently in Beta. Breaking changes may be introduced before the endpoint is made Generally Available.\n> * This endpoint requires Spotter - please contact ThoughtSpot support to enable Spotter on your cluster.\n> * The streaming protocol uses Server-Sent Events (SSE)\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - conversation_identifier (string) (required)\n    Unique identifier for the conversation (used to track context)\n  - messages (array of string) (required)\n    messages to be sent to the agent")
    def sendAgentMessageStreaming(body: Optional[dict] = None):
        ''' <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.13.0.cl or later</span>  This API allows users to initiate or continue an agent (Spotter) conversation by submitting one or more natural language messages.  To use this API, the user must have access to the relevant conversational session (via conversation_identifier) and submit at least one message.   #### Usage guidelines  To initiate or continue a conversation, the request must include: - `conversation_identifier`: a unique session ID for continuity and message tracking - `messages`: an array of one or more text messages, each with a value and type  Additionally, user can specify what tool can be included `conversation_settings` parameter, which supports: - `enable_contextual_change_analysis` (default: false) - `enable_natural_language_answer_generation` (default: true) - `enable_reasoning` (default: false)  If the request is valid, the API returns a stream of messages in real time, including: - `ack`: confirms receipt of the request - `text / text-chunk`: content chunks, optionally formatted (e.g., markdown) - `answer`: the final structured response with metadata and analytics - `error`: if a failure occurs - `notification`: notification messages for operation being performed  > ###### Note: > * This endpoint is currently in Beta. Breaking changes may be introduced before the endpoint is made Generally Available. > * This endpoint requires Spotter - please contact ThoughtSpot support to enable Spotter on your cluster. > * The streaming protocol uses Server-Sent Events (SSE)    #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/ai/agent/converse/sse"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='sendMessage', description="\n<span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.4.0.cl or later</span>\n\nAllows sending a follow-up message to an ongoing conversation within the context of the metadata model.\n\nRequires at least view access to the metadata object specified in the request.\n\n#### Usage guidelines\n\nThe API requires you to specify the `conversation_identifier` in the request path, and a `metadata_identifier` and `message` string in the request body.\n\nIf the API request is successful, ThoughtSpot returns the session ID, tokens used in the conversation, and visualization type.\n\n> ###### Note:\n> * This endpoint is currently in Beta. Breaking changes may be introduced before the endpoint is made Generally Available.\n> * This endpoint requires Spotter - please contact ThoughtSpot support to enable Spotter on your cluster.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_identifier (string) (required)\n    ID of the metadata object, such as a Worksheet or Model, to use as a data source for the conversatio\n  - message (string) (required)\n    A message string with the follow-up question to continue the conversation.")
    def sendMessage(conversation_identifier: str, body: Optional[dict] = None):
        ''' <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.4.0.cl or later</span>  Allows sending a follow-up message to an ongoing conversation within the context of the metadata model.  Requires at least view access to the metadata object specified in the request.  #### Usage guidelines  The API requires you to specify the `conversation_identifier` in the request path, and a `metadata_identifier` and `message` string in the request body.  If the API request is successful, ThoughtSpot returns the session ID, tokens used in the conversation, and visualization type.  > ###### Note: > * This endpoint is currently in Beta. Breaking changes may be introduced before the endpoint is made Generally Available. > * This endpoint requires Spotter - please contact ThoughtSpot support to enable Spotter on your cluster.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/ai/conversation/{conversation_identifier}/converse"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if conversation_identifier is None:
            raise ValueError('Missing required path parameter: conversation_identifier')
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='singleAnswer', description="\n<span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.4.0.cl or later</span>\n\nProcesses a natural language query and returns an AI-generated response based on a specified data model.\n\nRequires at least view access to the metadata object specified in the request.\n\n> ###### Note:\n> * This endpoint is currently in Beta. Breaking changes may be introduced before the endpoint is made Generally Available.\n> * This endpoint requires Spotter - please contact ThoughtSpot support to enable Spotter on your cluster.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - query (string) (required)\n    A natural language query string to generate the Answer.\n  - metadata_identifier (string) (required)\n    ID of the metadata object, such as a Worksheet or Model, to use as a data source for the query.")
    def singleAnswer(body: Optional[dict] = None):
        ''' <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.4.0.cl or later</span>  Processes a natural language query and returns an AI-generated response based on a specified data model.  Requires at least view access to the metadata object specified in the request.  > ###### Note: > * This endpoint is currently in Beta. Breaking changes may be introduced before the endpoint is made Generally Available. > * This endpoint requires Spotter - please contact ThoughtSpot support to enable Spotter on your cluster.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/ai/answer/create"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='createConnection', description="\n <span class=\"since-beta-tag\">Version: 9.2.0.cl or later</span>\n\nCreates a connection to a data warehouse for live query services.  \n\nRequires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) privilege is required.\n\n#### Create a connection without tables\n\nTo create a connection without tables:\n\n1. Pass these parameters in your API request.\n * Name of the connection.\n * Type of the data warehouse to connect to.\n * A JSON map of configuration attributes in `data_warehouse_config`. The following example shows the configuration attributes for a SnowFlake connection:\n  ```\n  {\n     \"configuration\":{\n        \"accountName\":\"thoughtspot_partner\",\n        \"user\":\"tsadmin\",\n        \"password\":\"TestConn123\",\n        \"role\":\"sysadmin\",\n        \"warehouse\":\"MEDIUM_WH\"\n     },\n     \"externalDatabases\":[\n\n     ]\n  }\n  ```\n2. Set `validate` to `false`.\n\n#### Create a connection with tables\n\nTo create a connection with tables:\n\n1. Pass these parameters in your API request.\n * Name of the connection.\n * Type of the data warehouse to connect to.\n * A JSON map of configuration attributes, database details, and table properties in `data_warehouse_config` as shown in the following example:\n  ```\n  {\n     \"configuration\":{\n        \"accountName\":\"thoughtspot_partner\",\n        \"user\":\"tsadmin\",\n        \"password\":\"TestConn123\",\n        \"role\":\"sysadmin\",\n        \"warehouse\":\"MEDIUM_WH\"\n     },\n     \"externalDatabases\":[\n        {\n           \"name\":\"AllDatatypes\",\n           \"isAutoCreated\":false,\n           \"schemas\":[\n              {\n                 \"name\":\"alldatatypes\",\n                 \"tables\":[\n                    {\n                       \"name\":\"allDatatypes\",\n                       \"type\":\"TABLE\",\n                       \"description\":\"\",\n                       \"selected\":true,\n                       \"linked\":true,\n                       \"columns\":[\n                          {\n                             \"name\":\"CNUMBER\",\n                             \"type\":\"INT64\",\n                             \"canImport\":true,\n                             \"selected\":true,\n                             \"isLinkedActive\":true,\n                             \"isImported\":false,\n                             \"tableName\":\"allDatatypes\",\n                             \"schemaName\":\"alldatatypes\",\n                             \"dbName\":\"AllDatatypes\"\n                          },\n                          {\n                             \"name\":\"CDECIMAL\",\n                             \"type\":\"INT64\",\n                             \"canImport\":true,\n                             \"selected\":true,\n                             \"isLinkedActive\":true,\n                             \"isImported\":false,\n                             \"tableName\":\"allDatatypes\",\n                             \"schemaName\":\"alldatatypes\",\n                             \"dbName\":\"AllDatatypes\"\n                          }\n                       ]\n                    }\n                 ]\n              }\n           ]\n        }\n     ]\n  }\n  ```\n2. Set `validate` to `true`.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (required)\n    Unique name for the connection.\n  - description (string) (optional)\n    Description of the connection.\n  - data_warehouse_type (string) (required)\n    Type of the data warehouse.\n  - data_warehouse_config (object) (required)\n    Connection configuration attributes in JSON format. To create a connection with tables, include tabl\n  - validate (boolean) (optional)\n    Validates the connection metadata if tables are included. If you are creating a connection without t")
    def createConnection(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.2.0.cl or later</span>  Creates a connection to a data warehouse for live query services.    Requires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) privilege is required.  #### Create a connection without tables  To create a connection without tables:  1. Pass these parameters in your API request.  * Name of the connection.  * Type of the data warehouse to connect to.  * A JSON map of configuration attributes in `data_warehouse_config`. The following example shows the configuration attributes for a SnowFlake connection:   ```   {      "configuration":{         "accountName":"thoughtspot_partner",         "user":"tsadmin",         "password":"TestConn123",         "role":"sysadmin",         "warehouse":"MEDIUM_WH"      },      "externalDatabases":[       ]   }   ``` 2. Set `validate` to `false`.  #### Create a connection with tables  To create a connection with tables:  1. Pass these parameters in your API request.  * Name of the connection.  * Type of the data warehouse to connect to.  * A JSON map of configuration attributes, database details, and table properties in `data_warehouse_config` as shown in the following example:   ```   {      "configuration":{         "accountName":"thoughtspot_partner",         "user":"tsadmin",         "password":"TestConn123",         "role":"sysadmin",         "warehouse":"MEDIUM_WH"      },      "externalDatabases":[         {            "name":"AllDatatypes",            "isAutoCreated":false,            "schemas":[               {                  "name":"alldatatypes",                  "tables":[                     {                        "name":"allDatatypes",                        "type":"TABLE",                        "description":"",                        "selected":true,                        "linked":true,                        "columns":[                           {                              "name":"CNUMBER",                              "type":"INT64",                              "canImport":true,                              "selected":true,                              "isLinkedActive":true,                              "isImported":false,                              "tableName":"allDatatypes",                              "schemaName":"alldatatypes",                              "dbName":"AllDatatypes"                           },                           {                              "name":"CDECIMAL",                              "type":"INT64",                              "canImport":true,                              "selected":true,                              "isLinkedActive":true,                              "isImported":false,                              "tableName":"allDatatypes",                              "schemaName":"alldatatypes",                              "dbName":"AllDatatypes"                           }                        ]                     }                  ]               }            ]         }      ]   }   ``` 2. Set `validate` to `true`.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/connection/create"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='deleteConnectionV2', description="\n <span class=\"since-beta-tag\">Version: 10.4.0.cl or later</span>\n\nDeletes a connection object.\n\n**Note**: If a connection has dependent objects, make sure you remove its associations before the delete operation.\n\nRequires `DATAMANAGEMENT` (**Can manage data**) and edit permissions to the connection object, or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) privilege is required.\n\n\n\n\n#### Endpoint URL\n")
    def deleteConnectionV2(connection_identifier: str):
        '''  <span class="since-beta-tag">Version: 10.4.0.cl or later</span>  Deletes a connection object.  **Note**: If a connection has dependent objects, make sure you remove its associations before the delete operation.  Requires `DATAMANAGEMENT` (**Can manage data**) and edit permissions to the connection object, or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) privilege is required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/connections/{connection_identifier}/delete"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if connection_identifier is None:
            raise ValueError('Missing required path parameter: connection_identifier')
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='downloadConnectionMetadataChanges', description="\n <span class=\"since-beta-tag\">Version: 9.9.0.cl or later</span>\n\nExports the difference in connection metadata between CDW and ThoughtSpot\n\nRequires `DATAMANAGEMENT` (**Can manage data**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following Data control privileges may be required:  \n- `CAN_MANAGE_CUSTOM_CALENDAR`(**Can manage custom calendars**)\n- `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**)\n- `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)\n\nTo download the connection metadata difference between ThoughtSpot and CDW, pass the connection GUID as `connection_identifier` in the API request.\n\n\n\n\n#### Endpoint URL\n")
    def downloadConnectionMetadataChanges(connection_identifier: str):
        '''  <span class="since-beta-tag">Version: 9.9.0.cl or later</span>  Exports the difference in connection metadata between CDW and ThoughtSpot  Requires `DATAMANAGEMENT` (**Can manage data**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following Data control privileges may be required:   - `CAN_MANAGE_CUSTOM_CALENDAR`(**Can manage custom calendars**) - `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) - `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)  To download the connection metadata difference between ThoughtSpot and CDW, pass the connection GUID as `connection_identifier` in the API request.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/connections/download-connection-metadata-changes/{connection_identifier}"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if connection_identifier is None:
            raise ValueError('Missing required path parameter: connection_identifier')
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='fetchConnectionDiffStatus', description="\n <span class=\"since-beta-tag\">Version: 9.9.0.cl or later</span>\n\nValidates the difference in connection metadata between CDW and ThoughtSpot.\n\nRequires `DATAMANAGEMENT` (**Can manage data**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following Data control privileges may be required:\n\n- `CAN_MANAGE_CUSTOM_CALENDAR`(**Can manage custom calendars**)\n- `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**)\n- `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)\n\nReturns a boolean indicating whether there is any difference between the connection metadata at ThoughtSpot and CDW.\n\nTo get the connection metadata difference status, pass the connection GUID as `connection_identifier` in the API request.\n\n\n\n\n#### Endpoint URL\n")
    def fetchConnectionDiffStatus(connection_identifier: str):
        '''  <span class="since-beta-tag">Version: 9.9.0.cl or later</span>  Validates the difference in connection metadata between CDW and ThoughtSpot.  Requires `DATAMANAGEMENT` (**Can manage data**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following Data control privileges may be required:  - `CAN_MANAGE_CUSTOM_CALENDAR`(**Can manage custom calendars**) - `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) - `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)  Returns a boolean indicating whether there is any difference between the connection metadata at ThoughtSpot and CDW.  To get the connection metadata difference status, pass the connection GUID as `connection_identifier` in the API request.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/connections/fetch-connection-diff-status/{connection_identifier}"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if connection_identifier is None:
            raise ValueError('Missing required path parameter: connection_identifier')
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='searchConnection', description="\n <span class=\"since-beta-tag\">Version: 9.2.0.cl or later</span>\n\nGets connection objects.\nRequires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) privilege is required.\n\n- To get a list of all connections available in the ThoughtSpot system, send the API request without any attributes in the request body.\n- To get the connection objects for a specific type of data warehouse, specify the type in `data_warehouse_types`.\n- To fetch details of a connection object, specify the connection object GUID or name. The `name_pattern` attribute allows passing partial text with `%` for a wildcard match.\n- To get details of the database, schemas, tables, or columns from a data connection object, specify `data_warehouse_object_type`.\n- To get a specific database, schema, table, or column from a connection object, define the object type in `data_warehouse_object_type` and object properties in the `data_warehouse_objects` array. For example, to search for a column, you must pass the database, schema, and table names in the API request.\n  Note that in the following example, object properties are set in a hierarchical order (`database` > `schema` > `table` > `column`).\n\n```\n{\n  \"connections\": [\n    {\n      \"identifier\": \"b9d1f2ef-fa65-4a4b-994e-30fa2d57b0c2\",\n      \"data_warehouse_objects\": [\n        {\n          \"database\": \"NEBULADEV\",\n          \"schema\": \"INFORMATION_SCHEMA\",\n          \"table\": \"APPLICABLE_ROLES\",\n          \"column\": \"ROLE_NAME\"\n        }\n      ]\n    }\n  ],\n  \"data_warehouse_object_type\": \"COLUMN\"\n}\n```\n\n- To fetch data by `configuration`, specify `data_warehouse_object_type`. For example, to fetch columns from the `DEVELOPMENT` database, specify the `data_warehouse_object_type` as `DATABASE` and define the `configuration` string as `{\"database\":\"DEVELOPMENT\"}`. To get column data for a specific table, specify the table, for example,`{\"database\":\"RETAILAPPAREL\",\"table\":\"PIPES\"}`.\n- To query connections by `authentication_type`, specify `data_warehouse_object_type`. Supported values for `authentication_type` are:\n  - `SERVICE_ACCOUNT`: For connections that require service account credentials to authenticate to the Cloud Data Warehouse and fetch data.\n  - `OAUTH`: For connections that require OAuth credentials to authenticate to the Cloud Data Warehouse and fetch data. Teradata, Oracle, and Presto Cloud Data Warehouses do not support the OAuth authentication type.\n  - `IAM`: For connections that have the IAM OAuth set up. This authentication type is supported on Amazon Redshift connections only.\n  - `EXTOAUTH`: For connections that have External OAuth set up. ThoughtSpot supports external [OAuth with Microsoft Azure Active Directory (AD)](https://docs.thoughtspot.com/cloud/latest/ connections-snowflake-azure-ad-oauth) and [Okta for Snowflake data connections](https://docs.thoughtspot.com/cloud/latest/connections-snowflake-okta-oauth).\n  - `KEY_PAIR`: For connections that require Key Pair account credentials to authenticate to the Cloud Data Warehouse and fetch data. This authentication type is supported on Snowflake connections only.\n  - `OAUTH_WITH_PKCE`: For connections that require OAuth with PKCE account credentials to authenticate to the Cloud Data Warehouse and fetch data. This authentication type is supported on Snowflake, Starburst, Databricks, Denodo  connections only.\n  - `EXTOAUTH_WITH_PKCE`: For connections that require External OAuth With PKCE account credentials to authenticate to the Cloud Data Warehouse and fetch data. This authentication type is supported on Snowflake connections only.\n  - `OAUTH_WITH_PEZ`: For connections that require OAuth With PEZ account credentials to authenticate to the Cloud Data Warehouse and fetch data. This authentication type is supported on Amazon Redshift connections only.\n  - `OAUTH_WITH_SERVICE_PRINCIPAL`: For connections that require OAuth With Service Principal account credentials to authenticate to the Cloud Data Warehouse and fetch data. This authentication type is supported on Databricks connections only.\n  - `PERSONAL_ACCESS_TOKEN`: For connections that require Personal Access Token account credentials to authenticate to the Cloud Data Warehouse and fetch data. This authentication type is supported on Databricks connections only.\n  - `OAUTH_CLIENT_CREDENTIALS`: For connections that require OAuth Client Credentials to authenticate to the Cloud Data Warehouse and fetch data. This authentication type is supported on Snowflake connections only.\n- To include more details about connection objects in the API response, set `include_details` to `true`.\n- You can also sort the output by field names and filter connections by tags.\n\n**NOTE**: When filtering connection records by parameters other than `data_warehouse_types` or `tag_identifiers`, ensure that you set `record_size` to `-1` and `record_offset` to `0` for precise results.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - connections (array of object) (optional)\n    List of connections and name pattern\n  - data_warehouse_types (array of string) (optional)\n    Array of types of data warehouse defined for the connection.\n  - record_offset (integer) (optional)\n    The starting record number from where the records should be included.\n  - record_size (integer) (optional)\n    The number of records that should be included.\n  - tag_identifiers (array of string) (optional)\n    Unique ID or name of tags.\n  - data_warehouse_object_type (string) (optional)\n    Data warehouse object type.\n  - sort_options (any) (optional)\n    Sort options.\n  - include_details (boolean) (optional)\n    Indicates whether to include complete details of the connection objects.\n  - configuration (object) (optional)\n    Configuration values. If empty we are fetching configuration from datasource based on given connecti\n  - authentication_type (string) (optional)\n    List of authentication types to fetch data_ware_house_objects from external Data warehouse. This is \n  - show_resolved_parameters (boolean) (optional)\n    <div><span class=\"since-beta-tag\">Version: 10.9.0.cl or later</span></div>\n\nIndicates whether to sho")
    def searchConnection(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.2.0.cl or later</span>  Gets connection objects. Requires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) privilege is required.  - To get a list of all connections available in the ThoughtSpot system, send the API request without any attributes in the request body. - To get the connection objects for a specific type of data warehouse, specify the type in `data_warehouse_types`. - To fetch details of a connection object, specify the connection object GUID or name. The `name_pattern` attribute allows passing partial text with `%` for a wildcard match. - To get details of the database, schemas, tables, or columns from a data connection object, specify `data_warehouse_object_type`. - To get a specific database, schema, table, or column from a connection object, define the object type in `data_warehouse_object_type` and object properties in the `data_warehouse_objects` array. For example, to search for a column, you must pass the database, schema, and table names in the API request.   Note that in the following example, object properties are set in a hierarchical order (`database` > `schema` > `table` > `column`).  ``` {   "connections": [     {       "identifier": "b9d1f2ef-fa65-4a4b-994e-30fa2d57b0c2",       "data_warehouse_objects": [         {           "database": "NEBULADEV",           "schema": "INFORMATION_SCHEMA",           "table": "APPLICABLE_ROLES",           "column": "ROLE_NAME"         }       ]     }   ],   "data_warehouse_object_type": "COLUMN" } ```  - To fetch data by `configuration`, specify `data_warehouse_object_type`. For example, to fetch columns from the `DEVELOPMENT` database, specify the `data_warehouse_object_type` as `DATABASE` and define the `configuration` string as `{"database":"DEVELOPMENT"}`. To get column data for a specific table, specify the table, for example,`{"database":"RETAILAPPAREL","table":"PIPES"}`. - To query connections by `authentication_type`, specify `data_warehouse_object_type`. Supported values for `authentication_type` are:   - `SERVICE_ACCOUNT`: For connections that require service account credentials to authenticate to the Cloud Data Warehouse and fetch data.   - `OAUTH`: For connections that require OAuth credentials to authenticate to the Cloud Data Warehouse and fetch data. Teradata, Oracle, and Presto Cloud Data Warehouses do not support the OAuth authentication type.   - `IAM`: For connections that have the IAM OAuth set up. This authentication type is supported on Amazon Redshift connections only.   - `EXTOAUTH`: For connections that have External OAuth set up. ThoughtSpot supports external [OAuth with Microsoft Azure Active Directory (AD)](https://docs.thoughtspot.com/cloud/latest/ connections-snowflake-azure-ad-oauth) and [Okta for Snowflake data connections](https://docs.thoughtspot.com/cloud/latest/connections-snowflake-okta-oauth).   - `KEY_PAIR`: For connections that require Key Pair account credentials to authenticate to the Cloud Data Warehouse and fetch data. This authentication type is supported on Snowflake connections only.   - `OAUTH_WITH_PKCE`: For connections that require OAuth with PKCE account credentials to authenticate to the Cloud Data Warehouse and fetch data. This authentication type is supported on Snowflake, Starburst, Databricks, Denodo  connections only.   - `EXTOAUTH_WITH_PKCE`: For connections that require External OAuth With PKCE account credentials to authenticate to the Cloud Data Warehouse and fetch data. This authentication type is supported on Snowflake connections only.   - `OAUTH_WITH_PEZ`: For connections that require OAuth With PEZ account credentials to authenticate to the Cloud Data Warehouse and fetch data. This authentication type is supported on Amazon Redshift connections only.   - `OAUTH_WITH_SERVICE_PRINCIPAL`: For connections that require OAuth With Service Principal account credentials to authenticate to the Cloud Data Warehouse and fetch data. This authentication type is supported on Databricks connections only.   - `PERSONAL_ACCESS_TOKEN`: For connections that require Personal Access Token account credentials to authenticate to the Cloud Data Warehouse and fetch data. This authentication type is supported on Databricks connections only.   - `OAUTH_CLIENT_CREDENTIALS`: For connections that require OAuth Client Credentials to authenticate to the Cloud Data Warehouse and fetch data. This authentication type is supported on Snowflake connections only. - To include more details about connection objects in the API response, set `include_details` to `true`. - You can also sort the output by field names and filter connections by tags.  **NOTE**: When filtering connection records by parameters other than `data_warehouse_types` or `tag_identifiers`, ensure that you set `record_size` to `-1` and `record_offset` to `0` for precise results.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/connection/search"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateConnectionV2', description="\n <span class=\"since-beta-tag\">Version: 10.4.0.cl or later</span>\n\nUpdates a connection object.\n\nRequires `DATAMANAGEMENT` (**Can manage data**) and edit permissions to the connection object, or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) privilege is required.\n\nTo update a connection object, pass these parameters in your API request:\n\n1. GUID of the connection object.\n2. If you are updating tables or database schema of a connection object:\n   a. Add the updated JSON map of metadata with database, schema, and tables in `data_warehouse_config`.\n   b. Set `validate` to `true`.\n   \n   **NOTE:** If the `authentication_type` is anything other than SERVICE_ACCOUNT, you must explicitly provide the authenticationType property in the payload. If you do not specify authenticationType, the API will default to SERVICE_ACCOUNT as the authentication type.\n\n   * A JSON map of configuration attributes, database details, and table properties in `data_warehouse_config` as shown in the following example:\n\n   ```\n   {\n      \"configuration\":{\n         \"accountName\":\"thoughtspot_partner\",\n         \"user\":\"tsadmin\",\n         \"password\":\"TestConn123\",\n         \"role\":\"sysadmin\",\n         \"warehouse\":\"MEDIUM_WH\"\n      },\n      \"externalDatabases\":[\n         {\n            \"name\":\"AllDatatypes\",\n            \"isAutoCreated\":false,\n            \"schemas\":[\n               {\n                  \"name\":\"alldatatypes\",\n                  \"tables\":[\n                     {\n                        \"name\":\"allDatatypes\",\n                        \"type\":\"TABLE\",\n                        \"description\":\"\",\n                        \"selected\":true,\n                        \"linked\":true,\n                        \"columns\":[\n                           {\n                              \"name\":\"CNUMBER\",\n                              \"type\":\"INT64\",\n                              \"canImport\":true,\n                              \"selected\":true,\n                              \"isLinkedActive\":true,\n                              \"isImported\":false,\n                              \"tableName\":\"allDatatypes\",\n                              \"schemaName\":\"alldatatypes\",\n                              \"dbName\":\"AllDatatypes\"\n                           },\n                           {\n                              \"name\":\"CDECIMAL\",\n                              \"type\":\"INT64\",\n                              \"canImport\":true,\n                              \"selected\":true,\n                              \"isLinkedActive\":true,\n                              \"isImported\":false,\n                              \"tableName\":\"allDatatypes\",\n                              \"schemaName\":\"alldatatypes\",\n                              \"dbName\":\"AllDatatypes\"\n                           }\n                        ]\n                     }\n                  ]\n               }\n            ]\n         }\n      ]\n   }\n   ```\n\n3. If you are updating a configuration attribute, connection name, or description, you can set `validate` to `false`.\n\n  **NOTE:** If the `authentication_type` is anything other than SERVICE_ACCOUNT, you must explicitly provide the authenticationType property in the payload. If you do not  specify authenticationType, the API will default to SERVICE_ACCOUNT as the authentication type.\n\n  * A JSON map of configuration attributes in `data_warehouse_config`. The following example shows the configuration attributes for a Snowflake connection:\n   ```\n   {\n      \"configuration\":{\n         \"accountName\":\"thoughtspot_partner\",\n         \"user\":\"tsadmin\",\n         \"password\":\"TestConn123\",\n         \"role\":\"sysadmin\",\n         \"warehouse\":\"MEDIUM_WH\"\n      },\n      \"externalDatabases\":[\n\n      ]\n   }\n   ```\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (optional)\n    Updated name of the connection.\n  - description (string) (optional)\n    Updated description of the connection.\n  - data_warehouse_config (object) (optional)\n    Configuration of the data warehouse in JSON.\n  - validate (boolean) (optional)\n    Indicates whether to validate the connection details.")
    def updateConnectionV2(connection_identifier: str, body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 10.4.0.cl or later</span>  Updates a connection object.  Requires `DATAMANAGEMENT` (**Can manage data**) and edit permissions to the connection object, or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) privilege is required.  To update a connection object, pass these parameters in your API request:  1. GUID of the connection object. 2. If you are updating tables or database schema of a connection object:    a. Add the updated JSON map of metadata with database, schema, and tables in `data_warehouse_config`.    b. Set `validate` to `true`.        **NOTE:** If the `authentication_type` is anything other than SERVICE_ACCOUNT, you must explicitly provide the authenticationType property in the payload. If you do not specify authenticationType, the API will default to SERVICE_ACCOUNT as the authentication type.     * A JSON map of configuration attributes, database details, and table properties in `data_warehouse_config` as shown in the following example:     ```    {       "configuration":{          "accountName":"thoughtspot_partner",          "user":"tsadmin",          "password":"TestConn123",          "role":"sysadmin",          "warehouse":"MEDIUM_WH"       },       "externalDatabases":[          {             "name":"AllDatatypes",             "isAutoCreated":false,             "schemas":[                {                   "name":"alldatatypes",                   "tables":[                      {                         "name":"allDatatypes",                         "type":"TABLE",                         "description":"",                         "selected":true,                         "linked":true,                         "columns":[                            {                               "name":"CNUMBER",                               "type":"INT64",                               "canImport":true,                               "selected":true,                               "isLinkedActive":true,                               "isImported":false,                               "tableName":"allDatatypes",                               "schemaName":"alldatatypes",                               "dbName":"AllDatatypes"                            },                            {                               "name":"CDECIMAL",                               "type":"INT64",                               "canImport":true,                               "selected":true,                               "isLinkedActive":true,                               "isImported":false,                               "tableName":"allDatatypes",                               "schemaName":"alldatatypes",                               "dbName":"AllDatatypes"                            }                         ]                      }                   ]                }             ]          }       ]    }    ```  3. If you are updating a configuration attribute, connection name, or description, you can set `validate` to `false`.    **NOTE:** If the `authentication_type` is anything other than SERVICE_ACCOUNT, you must explicitly provide the authenticationType property in the payload. If you do not  specify authenticationType, the API will default to SERVICE_ACCOUNT as the authentication type.    * A JSON map of configuration attributes in `data_warehouse_config`. The following example shows the configuration attributes for a Snowflake connection:    ```    {       "configuration":{          "accountName":"thoughtspot_partner",          "user":"tsadmin",          "password":"TestConn123",          "role":"sysadmin",          "warehouse":"MEDIUM_WH"       },       "externalDatabases":[        ]    }    ```     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/connections/{connection_identifier}/update"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if connection_identifier is None:
            raise ValueError('Missing required path parameter: connection_identifier')
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='createCustomAction', description="\n <span class=\"since-beta-tag\">Version: 9.6.0.cl or later</span>\n\nCreates a custom action that appears as a menu action on a saved Answer or Liveboard visualization.\n\nRequires `DEVELOPER` (**Has Developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.\n\n#### Usage Guidelines\n\nThe API lets you create the following types of custom actions:\n\n* URL-based action     \n  Allows pushing data to an external URL.\n* Callback action        \n  Triggers a callback to the host application and initiates a response payload on an embedded ThoughtSpot instance.\n\nBy default, custom actions are visible to only administrator or developer users. To make a custom action available to other users, and specify the groups in `group_identifiers`.\n\nBy default, the custom action is set as a _global_ action on all visualizations and saved Answers. To assign a custom action to specific Liveboard visualization, saved Answer, or Worksheet, set `visibility` to `false` in `default_action_config` property and specify the GUID or name of the object in `associate_metadata`.\n\nFor more information, see [Custom actions](https://developers.thoughtspot.com/docs/custom-action-intro).\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (required)\n    Name of the custom action. The custom action name must be unique.\n  - action_details (any) (required)\n    Action details includes `Type` and Configuration data for Custom Actions, either Callback or URL is \n  - associate_metadata (array of object) (optional)\n    Metadata objects to which the custom action needs to be associated.\n  - default_action_config (any) (optional)\n    Default Custom action configuration. This includes if the custom action is available on all visualiz\n  - group_identifiers (array of string) (optional)\n    Unique ID or name of the groups that can view and access the custom action.")
    def createCustomAction(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.6.0.cl or later</span>  Creates a custom action that appears as a menu action on a saved Answer or Liveboard visualization.  Requires `DEVELOPER` (**Has Developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.  #### Usage Guidelines  The API lets you create the following types of custom actions:  * URL-based action        Allows pushing data to an external URL. * Callback action           Triggers a callback to the host application and initiates a response payload on an embedded ThoughtSpot instance.  By default, custom actions are visible to only administrator or developer users. To make a custom action available to other users, and specify the groups in `group_identifiers`.  By default, the custom action is set as a _global_ action on all visualizations and saved Answers. To assign a custom action to specific Liveboard visualization, saved Answer, or Worksheet, set `visibility` to `false` in `default_action_config` property and specify the GUID or name of the object in `associate_metadata`.  For more information, see [Custom actions](https://developers.thoughtspot.com/docs/custom-action-intro).     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/customization/custom-actions"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='deleteCustomAction', description="\n <span class=\"since-beta-tag\">Version: 9.6.0.cl or later</span>\n\nRemoves the custom action specified in the API request.\n\nRequires `DEVELOPER` (**Has Developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.\n\n\n\n\n#### Endpoint URL\n")
    def deleteCustomAction(custom_action_identifier: str):
        '''  <span class="since-beta-tag">Version: 9.6.0.cl or later</span>  Removes the custom action specified in the API request.  Requires `DEVELOPER` (**Has Developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/customization/custom-actions/{custom_action_identifier}/delete"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if custom_action_identifier is None:
            raise ValueError('Missing required path parameter: custom_action_identifier')
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='searchCustomActions', description="\n <span class=\"since-beta-tag\">Version: 9.6.0.cl or later</span>\n\nGets custom actions configured on the cluster.\n\nRequires `DEVELOPER` (**Has Developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - custom_action_identifier (string) (optional)\n    Name or ID of the custom action.\n  - name_pattern (string) (optional)\n    A pattern to match case-insensitive name of the custom-action object.\n  - default_action_config (any) (optional)\n    Default Custom action configuration. This includes if the custom action is available on all visualiz\n  - include_group_associations (boolean) (optional)\n    When set to true, returns the associated groups for a custom action.\n  - include_metadata_associations (boolean) (optional)\n    When set to true, returns the associated metadata for a custom action.\n  - metadata (array of object) (optional)\n    Search with a given metadata identifier.\n  - type (string) (optional)\n    Filter the action objects based on type")
    def searchCustomActions(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.6.0.cl or later</span>  Gets custom actions configured on the cluster.  Requires `DEVELOPER` (**Has Developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/customization/custom-actions/search"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateCustomAction', description="\n <span class=\"since-beta-tag\">Version: 9.6.0.cl or later</span>\n\nUpdates a custom action.\n\nRequires `DEVELOPER` (**Has Developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.\n\n#### Usage Guidelines\n\nThe API allows you to modify the following properties:\n\n* Name of the custom action\n* Action availability to groups\n* Association to metadata objects\n* Authentication settings for a URL-based action\n\nFor more information, see [Custom actions](https://developers.thoughtspot.com/docs/custom-action-intro).\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - action_details (any) (optional)\n    Action details includes `Type` and Configuration for Custom Actions, either Callback or URL is requi\n  - associate_metadata (array of object) (optional)\n    Metadata objects to which the custom action needs to be associated.\n  - default_action_config (any) (optional)\n    Default Custom action configuration. This includes if the custom action available on visualizations \n  - group_identifiers (array of string) (optional)\n    Unique ID or name of the groups that can view and access the custom action.\n  - name (string) (optional)\n    Name of the custom action. The custom action name must be unique.\n  - operation (string) (optional)\n    Type of update operation. Default operation type is ADD")
    def updateCustomAction(custom_action_identifier: str, body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.6.0.cl or later</span>  Updates a custom action.  Requires `DEVELOPER` (**Has Developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.  #### Usage Guidelines  The API allows you to modify the following properties:  * Name of the custom action * Action availability to groups * Association to metadata objects * Authentication settings for a URL-based action  For more information, see [Custom actions](https://developers.thoughtspot.com/docs/custom-action-intro).     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/customization/custom-actions/{custom_action_identifier}/update"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if custom_action_identifier is None:
            raise ValueError('Missing required path parameter: custom_action_identifier')
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='createCalendar', description="\n <span class=\"since-beta-tag\">Version: 10.12.0.cl or later</span>\n\nCreates a new [custom calendar](https://docs.thoughtspot.com/cloud/latest/connections-cust-cal).\n\nRequires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your ThoughtSpot instance, the `CAN_MANAGE_CUSTOM_CALENDAR` (**Can manage custom calendars**) privilege is required.\n\n\n#### Usage guidelines\n\nYou can create a custom calendar from scratch or an existing Table in ThoughtSpot. For both methods of calendar creation, the following parameters are required:\n\n* Name of the custom calendar.\n* Calendar creation method. To create a calendar from an existing table, specify the method:\n\n - `FROM_EXISTING_TABLE` - Creates calendar from the table reference provided in the API request.\n - `FROM_INPUT_PARAMS` - Creates a calendar from the parameters defined in the API request.\n\n* Connection ID and Table name\n* Database and schema name attributes:\n  For most Cloud Data Warehouse (CDW) connectors, both `database_name` and `schema_name` attributes are required.  \n  However, the attribute requirements are conditional and vary based on the connector type and its metadata structure. For example, for connectors such as Teradata, MySQL, SingleSore, Amazon Aurora MySQL, Amazon RDS MySQL, Oracle, and GCP_MYSQL, the `schema_name` is required, whereas the `database_name` attribute is not.\n  Similarly, connectors such as ClickHouse require you to specify the `database_name` and the schema specification in such cases is optional.\n\n**NOTE**: If you are creating a calendar from an existing table, ensure that the referenced table matches the required DDL for custom calendars. If the schema does not match, the API returns an error.\n\n##### Calendar type\nThe API allows you to create the following types of calendars:\n\n* `MONTH_OFFSET`. The default calendar type. A `MONTH_OFFSET` calendar is offset by a few months from the standard calendar months (January to December) and the year begins with the month defined in the request. For example, if the `month_offset` value is set as `April`, the calendar year begins in April.\n\n* `4-4-5`. Each quarter in the calendar will include two 4-week months followed by one 5-week month.\n* `4-5-4`. Each quarter in the calendar will include two 4-week months with a 5-week month between.\n* `5-4-4`. Each quarter begins with a 5-week month, followed by two 4-week months.\n\nTo start and end the calendar on a specific date, specify the dates in the `MM/DD/YYYY` format. For `MONTH_OFFSET` calendars, ensure that the `start_date` matches the month specified in the `month_offset` attribute.\n\nYou can also set the starting day of the week and customize the prefixes for year and quarter labels.\n\n#### Examples\n\nTo create a calendar from an existing table:\n\n```\n{\n  \"name\": \"MyCustomCalendar1\",\n  \"table_reference\": {\n    \"connection_identifier\": \"4db8ea22-2ff4-4224-b05a-26674717e468\",\n    \"table_name\": \"MyCalendarTable\",\n    \"database_name\": \"RETAILAPPAREL\",\n    \"schema_name\": \"PUBLIC\"\n  },\n  \"creation_method\": \"FROM_EXISTING_TABLE\",\n}\n```\n\nTo create a calendar from scratch:\n\n```\n{\n  \"name\": \"MyCustomCalendar1\",\n  \"table_reference\": {\n    \"connection_identifier\": \"4db8ea22-2ff4-4224-b05a-26674717e468\",\n    \"table_name\": \"MyCalendarTable\",\n    \"database_name\": \"RETAILAPPAREL\",\n    \"schema_name\": \"PUBLIC\"\n  },\n  \"creation_method\": \"FROM_INPUT_PARAMS\",\n  \"calendar_type\": \"MONTH_OFFSET\",\n  \"month_offset\": \"April\",\n  \"start_day_of_week\": \"Monday\",\n  \"quarter_name_prefix\": \"Q\",\n  \"year_name_prefix\": \"FY\",\n  \"start_date\": \"04/01/2025\",\n  \"end_date\": \"04/31/2025\"\n}\n```\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (required)\n    Name of the custom calendar.\n  - creation_method (string) (required)\n    Type of create operation.\n  - table_reference (any) (required)\n    Table reference containing connection identifier and table details in this format: `{\"connection_ide\n  - start_date (string) (optional)\n    Start date for the calendar in `MM/dd/yyyy` format. This parameter is mandatory if `creation_method`\n  - end_date (string) (optional)\n    End date for the calendar in `MM/dd/yyyy` format. This parameter is mandatory if `creation_method` i\n  - calendar_type (string) (optional)\n    Type of the calendar.\n  - month_offset (string) (optional)\n    Specify the month in which the fiscal or custom calendar year should start. For example, if you set \n  - start_day_of_week (string) (optional)\n    Specify the starting day of the week.\n  - quarter_name_prefix (string) (optional)\n    Prefix to add before the quarter.\n  - year_name_prefix (string) (optional)\n    Prefix to add before the year.")
    def createCalendar(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 10.12.0.cl or later</span>  Creates a new [custom calendar](https://docs.thoughtspot.com/cloud/latest/connections-cust-cal).  Requires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your ThoughtSpot instance, the `CAN_MANAGE_CUSTOM_CALENDAR` (**Can manage custom calendars**) privilege is required.   #### Usage guidelines  You can create a custom calendar from scratch or an existing Table in ThoughtSpot. For both methods of calendar creation, the following parameters are required:  * Name of the custom calendar. * Calendar creation method. To create a calendar from an existing table, specify the method:   - `FROM_EXISTING_TABLE` - Creates calendar from the table reference provided in the API request.  - `FROM_INPUT_PARAMS` - Creates a calendar from the parameters defined in the API request.  * Connection ID and Table name * Database and schema name attributes:   For most Cloud Data Warehouse (CDW) connectors, both `database_name` and `schema_name` attributes are required.     However, the attribute requirements are conditional and vary based on the connector type and its metadata structure. For example, for connectors such as Teradata, MySQL, SingleSore, Amazon Aurora MySQL, Amazon RDS MySQL, Oracle, and GCP_MYSQL, the `schema_name` is required, whereas the `database_name` attribute is not.   Similarly, connectors such as ClickHouse require you to specify the `database_name` and the schema specification in such cases is optional.  **NOTE**: If you are creating a calendar from an existing table, ensure that the referenced table matches the required DDL for custom calendars. If the schema does not match, the API returns an error.  ##### Calendar type The API allows you to create the following types of calendars:  * `MONTH_OFFSET`. The default calendar type. A `MONTH_OFFSET` calendar is offset by a few months from the standard calendar months (January to December) and the year begins with the month defined in the request. For example, if the `month_offset` value is set as `April`, the calendar year begins in April.  * `4-4-5`. Each quarter in the calendar will include two 4-week months followed by one 5-week month. * `4-5-4`. Each quarter in the calendar will include two 4-week months with a 5-week month between. * `5-4-4`. Each quarter begins with a 5-week month, followed by two 4-week months.  To start and end the calendar on a specific date, specify the dates in the `MM/DD/YYYY` format. For `MONTH_OFFSET` calendars, ensure that the `start_date` matches the month specified in the `month_offset` attribute.  You can also set the starting day of the week and customize the prefixes for year and quarter labels.  #### Examples  To create a calendar from an existing table:  ``` {   "name": "MyCustomCalendar1",   "table_reference": {     "connection_identifier": "4db8ea22-2ff4-4224-b05a-26674717e468",     "table_name": "MyCalendarTable",     "database_name": "RETAILAPPAREL",     "schema_name": "PUBLIC"   },   "creation_method": "FROM_EXISTING_TABLE", } ```  To create a calendar from scratch:  ``` {   "name": "MyCustomCalendar1",   "table_reference": {     "connection_identifier": "4db8ea22-2ff4-4224-b05a-26674717e468",     "table_name": "MyCalendarTable",     "database_name": "RETAILAPPAREL",     "schema_name": "PUBLIC"   },   "creation_method": "FROM_INPUT_PARAMS",   "calendar_type": "MONTH_OFFSET",   "month_offset": "April",   "start_day_of_week": "Monday",   "quarter_name_prefix": "Q",   "year_name_prefix": "FY",   "start_date": "04/01/2025",   "end_date": "04/31/2025" } ```     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/calendars/create"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='deleteCalendar', description="\n <span class=\"since-beta-tag\">Version: 10.12.0.cl or later</span>\n\nDeletes a [custom calendar](https://docs.thoughtspot.com/cloud/latest/connections-cust-cal).\n\nRequires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your ThoughtSpot instance, the `CAN_MANAGE_CUSTOM_CALENDAR` (**Can manage custom calendars**) privilege is required.\n\n#### Usage guidelines\nTo delete a custom calendar, specify the calendar ID as a path parameter in the request URL.  \n\n\n\n\n#### Endpoint URL\n")
    def deleteCalendar(calendar_identifier: str):
        '''  <span class="since-beta-tag">Version: 10.12.0.cl or later</span>  Deletes a [custom calendar](https://docs.thoughtspot.com/cloud/latest/connections-cust-cal).  Requires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your ThoughtSpot instance, the `CAN_MANAGE_CUSTOM_CALENDAR` (**Can manage custom calendars**) privilege is required.  #### Usage guidelines To delete a custom calendar, specify the calendar ID as a path parameter in the request URL.       #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/calendars/{calendar_identifier}/delete"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if calendar_identifier is None:
            raise ValueError('Missing required path parameter: calendar_identifier')
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='generateCSV', description="\n <span class=\"since-beta-tag\">Version: 10.12.0.cl or later</span>\n\nExports a [custom calendar](https://docs.thoughtspot.com/cloud/latest/connections-cust-cal) in the CSV format.\n\nRequires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your ThoughtSpot instance, the `CAN_MANAGE_CUSTOM_CALENDAR` (**Can manage custom calendars**) privilege is required.\n\n#### Usage guidelines\n\nUse this API to download a custom calendar in the CSV file format. In your API request, specify the following parameters.\n\n* Start and end date of the calendar. For \"month offset\" calendars, the start date must match the month defined in the `month_offset` attribute.\n\nYou can also specify optional parameters such as the starting day of the week and prefixes for the quarter and year labels.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - start_date (string) (required)\n    Start date for the calendar in `MM/dd/yyyy` format.\n  - end_date (string) (required)\n    End date for the calendar in `MM/dd/yyyy` format.\n  - calendar_type (string) (optional)\n    Type of the calendar.\n  - month_offset (string) (optional)\n    Month offset to start calendar from `January`.\n  - start_day_of_week (string) (optional)\n    Specify the starting day of the week.\n  - quarter_name_prefix (string) (optional)\n    Prefix to add before the quarter.\n  - year_name_prefix (string) (optional)\n    Prefix to add before the year.")
    def generateCSV(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 10.12.0.cl or later</span>  Exports a [custom calendar](https://docs.thoughtspot.com/cloud/latest/connections-cust-cal) in the CSV format.  Requires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your ThoughtSpot instance, the `CAN_MANAGE_CUSTOM_CALENDAR` (**Can manage custom calendars**) privilege is required.  #### Usage guidelines  Use this API to download a custom calendar in the CSV file format. In your API request, specify the following parameters.  * Start and end date of the calendar. For "month offset" calendars, the start date must match the month defined in the `month_offset` attribute.  You can also specify optional parameters such as the starting day of the week and prefixes for the quarter and year labels.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/calendars/generate-csv"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='searchCalendars', description="\n <span class=\"since-beta-tag\">Version: 10.12.0.cl or later</span>\n\nGets a list of [custom calendars](https://docs.thoughtspot.com/cloud/latest/connections-cust-cal).\n\nRequires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your ThoughtSpot instance, the `CAN_MANAGE_CUSTOM_CALENDAR` (**Can manage custom calendars**) privilege is required.\n\n#### Usage guidelines\n\nBy default, the API returns a list of custom calendars for all connection objects. To retrieve custom calendar details for a particular connection, specify the connection ID. You can also use other search parameters such as `name_pattern` and `sort_options` as search filters.\n\nThe `name_pattern` parameter filters and returns only those objects that match the specified pattern. Use `%` as a wildcard for pattern matching.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - connection_identifier (string) (optional)\n    Unique ID or name of the connection.\n  - name_pattern (string) (optional)\n    Pattern to match for calendar names (use '%' for wildcard match).\n  - record_offset (integer) (optional)\n    The starting record number from where the records should be included.\n  - record_size (integer) (optional)\n    The number of records that should be included.\n  - sort_options (any) (optional)\n    Sort options.")
    def searchCalendars(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 10.12.0.cl or later</span>  Gets a list of [custom calendars](https://docs.thoughtspot.com/cloud/latest/connections-cust-cal).  Requires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your ThoughtSpot instance, the `CAN_MANAGE_CUSTOM_CALENDAR` (**Can manage custom calendars**) privilege is required.  #### Usage guidelines  By default, the API returns a list of custom calendars for all connection objects. To retrieve custom calendar details for a particular connection, specify the connection ID. You can also use other search parameters such as `name_pattern` and `sort_options` as search filters.  The `name_pattern` parameter filters and returns only those objects that match the specified pattern. Use `%` as a wildcard for pattern matching.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/calendars/search"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateCalendar', description="\n <span class=\"since-beta-tag\">Version: 10.12.0.cl or later</span>\n\nUpdates the properties of a [custom calendar](https://docs.thoughtspot.com/cloud/latest/connections-cust-cal).\n\nRequires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your ThoughtSpot instance, the `CAN_MANAGE_CUSTOM_CALENDAR` (**Can manage custom calendars**) privilege is required.\n\n#### Usage guidelines\n\nYou can update the properties of a calendar using one of the following methods:\n* `FROM_INPUT_PARAMS` to update the calendar properties with the values defined in the API request.\n* `FROM_EXISTING_TABLE` Creates a calendar from the parameters defined in the API request.\n\nTo update a custom calendar, specify the calendar ID as a path parameter in the request URL and the following parameters in the request body:  \n\n* Connection ID and Table name\n* Database and schema name attributes:\n  For most Cloud Data Warehouse (CDW) connectors, both `database_name` and `schema_name` attributes are required.  \n  However, the attribute requirements are conditional and vary based on the connector type and its metadata structure. For example, for connectors such as Teradata, MySQL, SingleSore, Amazon Aurora MySQL, Amazon RDS MySQL, Oracle, and GCP_MYSQL, the `schema_name` is required, whereas the `database_name` attribute is not.\n  Similarly, connectors such as ClickHouse require you to specify the `database_name` and the schema specification in such cases is optional.\n\nThe API allows you to modify the calendar type, month offset value, start and end date, starting day of the week, and prefixes assigned to the year and quarter labels.   \n\n#### Examples\n\nUpdate a custom calendar using an existing Table in ThoughtSpot:\n\n```\n{\n  \"update_method\": \"FROM_EXISTING_TABLE\",\n  \"table_reference\": {\n    \"connection_identifier\": \"Connection1\",\n    \"database_name\": \"db1\",\n    \"table_name\": \"custom_calendar_2025\",\n    \"schame_name\": \"schemaVar\"\n  }\n}\n```\n\nUpdate a custom calendar with the attributes defined in the API request:\n\n```\n{\n  \"update_method\": \"FROM_INPUT_PARAMS\",\n  \"table_reference\": {\n    \"connection_identifier\": \"Connection1\",\n    \"database_name\": \"db1\",\n    \"table_name\": \"custom_calendar_2025\",\n    \"schame_name\": \"schemaVar\"\n  },\n  \"month_offset\": \"August\",\n  \"start_day_of_week\": \"Monday\",\n  \"start_date\": \"08/01/2025\",\n  \"end_date\": \"07/31/2026\"\n}\n```\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - update_method (string) (optional)\n    Type of update operation.\n  - table_reference (any) (required)\n    Table reference containing connection identifier and table details in this format: `{\"connection_ide\n  - start_date (string) (optional)\n    Start date for the calendar in `MM/dd/yyyy` format. This parameter is mandatory if `update_method` i\n  - end_date (string) (optional)\n    End date for the calendar in `MM/dd/yyyy` format. This parameter is mandatory if `update_method` is \n  - calendar_type (string) (optional)\n    Type of the calendar.\n  - month_offset (string) (optional)\n    Specify the month in which the fiscal or custom calendar year should start. For example, if you set \n  - start_day_of_week (string) (optional)\n    Specify the starting day of the week\n  - quarter_name_prefix (string) (optional)\n    Prefix to add before the quarter.\n  - year_name_prefix (string) (optional)\n    Prefix to add before the year.")
    def updateCalendar(calendar_identifier: str, body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 10.12.0.cl or later</span>  Updates the properties of a [custom calendar](https://docs.thoughtspot.com/cloud/latest/connections-cust-cal).  Requires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your ThoughtSpot instance, the `CAN_MANAGE_CUSTOM_CALENDAR` (**Can manage custom calendars**) privilege is required.  #### Usage guidelines  You can update the properties of a calendar using one of the following methods: * `FROM_INPUT_PARAMS` to update the calendar properties with the values defined in the API request. * `FROM_EXISTING_TABLE` Creates a calendar from the parameters defined in the API request.  To update a custom calendar, specify the calendar ID as a path parameter in the request URL and the following parameters in the request body:    * Connection ID and Table name * Database and schema name attributes:   For most Cloud Data Warehouse (CDW) connectors, both `database_name` and `schema_name` attributes are required.     However, the attribute requirements are conditional and vary based on the connector type and its metadata structure. For example, for connectors such as Teradata, MySQL, SingleSore, Amazon Aurora MySQL, Amazon RDS MySQL, Oracle, and GCP_MYSQL, the `schema_name` is required, whereas the `database_name` attribute is not.   Similarly, connectors such as ClickHouse require you to specify the `database_name` and the schema specification in such cases is optional.  The API allows you to modify the calendar type, month offset value, start and end date, starting day of the week, and prefixes assigned to the year and quarter labels.     #### Examples  Update a custom calendar using an existing Table in ThoughtSpot:  ``` {   "update_method": "FROM_EXISTING_TABLE",   "table_reference": {     "connection_identifier": "Connection1",     "database_name": "db1",     "table_name": "custom_calendar_2025",     "schame_name": "schemaVar"   } } ```  Update a custom calendar with the attributes defined in the API request:  ``` {   "update_method": "FROM_INPUT_PARAMS",   "table_reference": {     "connection_identifier": "Connection1",     "database_name": "db1",     "table_name": "custom_calendar_2025",     "schame_name": "schemaVar"   },   "month_offset": "August",   "start_day_of_week": "Monday",   "start_date": "08/01/2025",   "end_date": "07/31/2026" } ```     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/calendars/{calendar_identifier}/update"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if calendar_identifier is None:
            raise ValueError('Missing required path parameter: calendar_identifier')
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='fetchAnswerData', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nFetches data from a saved Answer.\n\nRequires at least view access to the saved Answer.\n\nThe `record_size` attribute determines the number of records to retrieve in an API call. For more information about pagination, record size, and maximum row limit, see [Pagination and record size settings](https://developers.thoughtspot.com/docs/fetch-data-and-report-apis#_pagination_settings_for_data_and_report_apis).\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_identifier (string) (required)\n    GUID or name of the Answer.\n  - data_format (string) (optional)\n    JSON output in compact or full format. The FULL option is available in 9.12.5.cl or later.\n  - record_offset (integer) (optional)\n    The starting record number from where the records should be included.\n  - record_size (integer) (optional)\n    The number of records to include in a batch.\n  - runtime_filter (object) (optional)\n    JSON object with representing filter condition to apply filters at runtime. For example, <code> {\"co\n  - runtime_sort (object) (optional)\n    JSON object representing columns to sort data at runtime. For example, <code> {\"sortCol1\": \"sales\", \n  - runtime_param_override (object) (optional)\n    JSON object for setting values of parameters at runtime. For example, <code> {\"param1\": \"Double List")
    def fetchAnswerData(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Fetches data from a saved Answer.  Requires at least view access to the saved Answer.  The `record_size` attribute determines the number of records to retrieve in an API call. For more information about pagination, record size, and maximum row limit, see [Pagination and record size settings](https://developers.thoughtspot.com/docs/fetch-data-and-report-apis#_pagination_settings_for_data_and_report_apis).    #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/answer/data"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='fetchLiveboardData', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nGets data from a Liveboard object and its visualization.  \n\nRequires at least view access to the Liveboard.\n\n#### Usage guidelines\n\nIn the request body, specify the GUID or name of the Liveboard. To get data for specific visualizations, add the GUIDs or names of the visualizations in the API request.\n\nTo include unsaved changes in the report, pass the `transient_pinboard_content` script generated from the `getExportRequestForCurrentPinboard` method in the Visual Embed SDK. Upon successful execution, the API returns the report with unsaved changes. If the new Liveboard experience mode, the transient content includes ad hoc changes to visualizations such as sorting, toggling of legends, and data drill down.\n\nFor more information, and see [Liveboard data API](https://developers.thoughtspot.com/docs/fetch-data-and-report-apis#_fetch_liveboard_data_api).\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_identifier (string) (required)\n    GUID or name of the Liveboard.\n  - visualization_identifiers (array of string) (optional)\n    GUIDs or names of the visualizations on the Liveboard.\n  - transient_content (string) (optional)\n    Transient content of the Liveboard.\n  - data_format (string) (optional)\n    JSON output in compact or full format. The FULL option is available in 9.12.5.cl or later.\n  - record_offset (integer) (optional)\n    The starting record number from where the records should be included.\n  - record_size (integer) (optional)\n    The number of records to include in a batch.\n  - runtime_filter (object) (optional)\n    JSON object with representing filter condition to apply filters at runtime. For example, <code> {\"co\n  - runtime_sort (object) (optional)\n    JSON object representing columns to sort data at runtime. For example, <code> {\"sortCol1\": \"sales\", \n  - runtime_param_override (object) (optional)\n    JSON object for setting values of parameters at runtime. For example, <code> {\"param1\": \"Double List")
    def fetchLiveboardData(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Gets data from a Liveboard object and its visualization.    Requires at least view access to the Liveboard.  #### Usage guidelines  In the request body, specify the GUID or name of the Liveboard. To get data for specific visualizations, add the GUIDs or names of the visualizations in the API request.  To include unsaved changes in the report, pass the `transient_pinboard_content` script generated from the `getExportRequestForCurrentPinboard` method in the Visual Embed SDK. Upon successful execution, the API returns the report with unsaved changes. If the new Liveboard experience mode, the transient content includes ad hoc changes to visualizations such as sorting, toggling of legends, and data drill down.  For more information, and see [Liveboard data API](https://developers.thoughtspot.com/docs/fetch-data-and-report-apis#_fetch_liveboard_data_api).    #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/liveboard/data"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='searchData', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nGenerates an Answer from a given data source.\n\nRequires at least view access to the data source object (Worksheet or View).\n\n#### Usage guidelines\n\nTo search data, specify the data source GUID in `logical_table_identifier`. The data source can be a Worksheet, View, Table, or SQL view.\n\nPass search tokens in the `query_string` attribute in the API request as shown in the following example:\n\n```\n{\n  \"query_string\": \"[sales] by [store]\",\n  \"logical_table_identifier\": \"cd252e5c-b552-49a8-821d-3eadaa049cca\",\n}\n```\n\nFor more information about the `query_string` format and data source attribute, see [Search data API](https://developers.thoughtspot.com/docs/fetch-data-and-report-apis#_search_data_api).  \n\nThe `record_size` attribute determines the number of records to retrieve in an API call. For more information about pagination, record size, and maximum row limit, see [Pagination and record size settings](https://developers.thoughtspot.com/docs/fetch-data-and-report-apis#_pagination_settings_for_data_and_report_api).  \n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - query_string (string) (required)\n    Query string with search tokens. For example, <code>[Sales][Region]</code>. See [API Documentation](\n  - logical_table_identifier (string) (required)\n    GUID of the data source object, such as a Worksheet, View, or Table. You can find the GUID of a data\n  - data_format (string) (optional)\n    JSON output in compact or full format. The FULL option is available in 9.12.5.cl or later.\n  - record_offset (integer) (optional)\n    The starting record number from where the records should be included.\n  - record_size (integer) (optional)\n    The number of records to include in a batch.\n  - runtime_filter (object) (optional)\n    JSON object with representing filter condition to apply filters at runtime. For example, <code> {\"co\n  - runtime_sort (object) (optional)\n    JSON object representing columns to sort data at runtime. For example, <code> {\"sortCol1\": \"sales\", \n  - runtime_param_override (object) (optional)\n    JSON object for setting values of parameters at runtime. For example, <code> {\"param1\": \"Double List")
    def searchData(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Generates an Answer from a given data source.  Requires at least view access to the data source object (Worksheet or View).  #### Usage guidelines  To search data, specify the data source GUID in `logical_table_identifier`. The data source can be a Worksheet, View, Table, or SQL view.  Pass search tokens in the `query_string` attribute in the API request as shown in the following example:  ``` {   "query_string": "[sales] by [store]",   "logical_table_identifier": "cd252e5c-b552-49a8-821d-3eadaa049cca", } ```  For more information about the `query_string` format and data source attribute, see [Search data API](https://developers.thoughtspot.com/docs/fetch-data-and-report-apis#_search_data_api).    The `record_size` attribute determines the number of records to retrieve in an API call. For more information about pagination, record size, and maximum row limit, see [Pagination and record size settings](https://developers.thoughtspot.com/docs/fetch-data-and-report-apis#_pagination_settings_for_data_and_report_api).      #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/searchdata"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='dbtConnection', description="\n <span class=\"since-beta-tag\">Version: 9.9.0.cl or later</span>\n\nCreates a DBT connection object in ThoughtSpot.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege or `DATAMANAGEMENT` (**Can manage data**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following Data control privileges may be required:\n\n- `CAN_MANAGE_CUSTOM_CALENDAR`(**Can manage custom calendars**)\n- `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**)\n- `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)\n\n#### About create DBT connection\nDBT connection in ThoughtSpot is used by the user to define DBT credentials for cloud . The API needs  embrace connection, embrace database name, DBT url, import type, DBT account identifier, DBT project identifier, DBT access token and environment details (or) embrace connection, embrace database name, import type, file_content to create a connection object. To know more about DBT, see ThoughtSpot Product Documentation.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - connection_name (string) (required)\n    Name of the connection.\n  - database_name (string) (required)\n    Name of the Database.\n  - import_type (string) (optional)\n    Mention type of Import\n  - access_token (string) (optional)\n    Access token is mandatory when Import_Type is DBT_CLOUD.\n  - dbt_url (string) (optional)\n    DBT URL is mandatory when Import_Type is DBT_CLOUD.\n  - account_id (string) (optional)\n    Account ID is mandatory when Import_Type is DBT_CLOUD\n  - project_id (string) (optional)\n    Project ID is mandatory when Import_Type is DBT_CLOUD\n  - dbt_env_id (string) (optional)\n    DBT Environment ID\"\n  - project_name (string) (optional)\n    Name of the project\n  - file_content (string) (optional)\n    Upload DBT Manifest and Catalog artifact files as a ZIP file. This field is Mandatory when Import Ty")
    def dbtConnection(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.9.0.cl or later</span>  Creates a DBT connection object in ThoughtSpot.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege or `DATAMANAGEMENT` (**Can manage data**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following Data control privileges may be required:  - `CAN_MANAGE_CUSTOM_CALENDAR`(**Can manage custom calendars**) - `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) - `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)  #### About create DBT connection DBT connection in ThoughtSpot is used by the user to define DBT credentials for cloud . The API needs  embrace connection, embrace database name, DBT url, import type, DBT account identifier, DBT project identifier, DBT access token and environment details (or) embrace connection, embrace database name, import type, file_content to create a connection object. To know more about DBT, see ThoughtSpot Product Documentation.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/dbt/dbt-connection"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'multipart/form-data'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='dbtGenerateSyncTml', description="\n <span class=\"since-beta-tag\">Version: 9.9.0.cl or later</span>\n\nResynchronize the existing list of models, tables, worksheet tml\u2019s and import them to Thoughtspot based on the DBT connection object.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege or `DATAMANAGEMENT` (**Can manage data**) privilege, along with an existing DBT connection.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following data control privileges may be required:\n\n- `CAN_MANAGE_CUSTOM_CALENDAR`(**Can manage custom calendars**)\n- `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**)\n- `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - dbt_connection_identifier (string) (required)\n    Unique ID of the DBT connection.\n  - file_content (string) (optional)\n    Upload DBT Manifest and Catalog artifact files as a ZIP file. This field is mandatory if the connect")
    def dbtGenerateSyncTml(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.9.0.cl or later</span>  Resynchronize the existing list of models, tables, worksheet tml’s and import them to Thoughtspot based on the DBT connection object.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege or `DATAMANAGEMENT` (**Can manage data**) privilege, along with an existing DBT connection. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following data control privileges may be required:  - `CAN_MANAGE_CUSTOM_CALENDAR`(**Can manage custom calendars**) - `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) - `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/dbt/generate-sync-tml"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'multipart/form-data'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='dbtGenerateTml', description="\n <span class=\"since-beta-tag\">Version: 9.9.0.cl or later</span>\n\nGenerate required table and worksheet and import them.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege or `DATAMANAGEMENT` (**Can manage data**) privilege, along with an existing DBT connection.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following data control privileges may be required:\n\n- `CAN_MANAGE_CUSTOM_CALENDAR`(**Can manage custom calendars**)\n- `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**)\n- `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)\n\n#### About generate TML\nModels and Worksheets to be imported can be selected by the user as part of the API.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - dbt_connection_identifier (string) (required)\n    Unique ID of the DBT connection.\n  - model_tables (string) (optional)\n    List of Models and their respective Tables\n  - import_worksheets (string) (required)\n    Mention the worksheet tmls to import\n  - worksheets (string) (optional)\n    List of worksheets is mandatory when import_Worksheets is type SELECTED\n  - file_content (string) (optional)\n    Upload DBT Manifest and Catalog artifact files as a ZIP file. This field is mandatory if the connect")
    def dbtGenerateTml(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.9.0.cl or later</span>  Generate required table and worksheet and import them.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege or `DATAMANAGEMENT` (**Can manage data**) privilege, along with an existing DBT connection. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following data control privileges may be required:  - `CAN_MANAGE_CUSTOM_CALENDAR`(**Can manage custom calendars**) - `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) - `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)  #### About generate TML Models and Worksheets to be imported can be selected by the user as part of the API.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/dbt/generate-tml"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'multipart/form-data'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='dbtSearch', description="\n <span class=\"since-beta-tag\">Version: 9.9.0.cl or later</span>\n\nGets a list of DBT connection objects by user and organization, available on the ThoughtSpot system.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege or `DATAMANAGEMENT` (**Can manage data**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following Data control privileges may be required:\n\n- `CAN_MANAGE_CUSTOM_CALENDAR`(**Can manage custom calendars**)\n- `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**)\n- `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)\n\n#### About search DBT connection\nTo get details of a specific DBT connection identifier, database connection identifier, database connection name, database name, project name, project identifier, environment identifier , import type and author.\n\n\n\n\n#### Endpoint URL\n")
    def dbtSearch():
        '''  <span class="since-beta-tag">Version: 9.9.0.cl or later</span>  Gets a list of DBT connection objects by user and organization, available on the ThoughtSpot system.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege or `DATAMANAGEMENT` (**Can manage data**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following Data control privileges may be required:  - `CAN_MANAGE_CUSTOM_CALENDAR`(**Can manage custom calendars**) - `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) - `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)  #### About search DBT connection To get details of a specific DBT connection identifier, database connection identifier, database connection name, database name, project name, project identifier, environment identifier , import type and author.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/dbt/search"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='deleteDbtConnection', description="\n <span class=\"since-beta-tag\">Version: 9.9.0.cl or later</span>\n\nRemoves the specified DBT connection object from the ThoughtSpot system.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) or `DATAMANAGEMENT` (**Can manage data ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following Data control privileges may be required:\n\n- `CAN_MANAGE_CUSTOM_CALENDAR`(**Can manage custom calendars**)\n- `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**)\n- `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)\n\n\n\n\n#### Endpoint URL\n")
    def deleteDbtConnection(dbt_connection_identifier: str):
        '''  <span class="since-beta-tag">Version: 9.9.0.cl or later</span>  Removes the specified DBT connection object from the ThoughtSpot system.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) or `DATAMANAGEMENT` (**Can manage data ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following Data control privileges may be required:  - `CAN_MANAGE_CUSTOM_CALENDAR`(**Can manage custom calendars**) - `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) - `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/dbt/{dbt_connection_identifier}/delete"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if dbt_connection_identifier is None:
            raise ValueError('Missing required path parameter: dbt_connection_identifier')
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateDbtConnection', description="\n <span class=\"since-beta-tag\">Version: 9.9.0.cl or later</span>\n\nUpdates a DBT connection object.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege or `DATAMANAGEMENT` (**Can manage data ThoughtSpot**) privilege, along with an existing DBT connection.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following data control privileges may be required:\n\n- `CAN_MANAGE_CUSTOM_CALENDAR`(**Can manage custom calendars**)\n- `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**)\n- `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)\n\n#### About update DBT connection\nYou can modify DBT connection object properties such as embrace connection name, embrace database name, import type, account identifier, access token, project identifier and environment (or) embrace connection, embrace database name, import type, file_content settings.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - dbt_connection_identifier (string) (required)\n    Unique ID of the DBT Connection.\n  - connection_name (string) (optional)\n    Name of the connection.\n  - database_name (string) (optional)\n    Name of the Database.\n  - import_type (string) (optional)\n    Mention type of Import\n  - access_token (string) (optional)\n    Access token is mandatory when Import_Type is DBT_CLOUD.\n  - dbt_url (string) (optional)\n    DBT URL is mandatory when Import_Type is DBT_CLOUD.\n  - account_id (string) (optional)\n    Account ID is mandatory when Import_Type is DBT_CLOUD\n  - project_id (string) (optional)\n    Project ID is mandatory when Import_Type is DBT_CLOUD\n  - dbt_env_id (string) (optional)\n    DBT Environment ID\"\n  - project_name (string) (optional)\n    Name of the project\n  - file_content (string) (optional)\n    Upload DBT Manifest and Catalog artifact files as a ZIP file. This field is Mandatory when Import Ty")
    def updateDbtConnection(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.9.0.cl or later</span>  Updates a DBT connection object.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege or `DATAMANAGEMENT` (**Can manage data ThoughtSpot**) privilege, along with an existing DBT connection. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following data control privileges may be required:  - `CAN_MANAGE_CUSTOM_CALENDAR`(**Can manage custom calendars**) - `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) - `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)  #### About update DBT connection You can modify DBT connection object properties such as embrace connection name, embrace database name, import type, account identifier, access token, project identifier and environment (or) embrace connection, embrace database name, import type, file_content settings.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/dbt/update-dbt-connection"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'multipart/form-data'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='createEmailCustomization', description="\n<span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.10.0.cl or later</span>\n\nCreates a customization configuration for the notification email.\n\n#### Pre-requisites\n\nRequires `DEVELOPER` (**has developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.\n\n**NOTE**:This endpoint in currently in beta. Contact ThoughtSpot support to enable this on your instance.\n\n#### Usage guidelines\n\nTo create a custom configuration pass these parameters in your API request:\n\n- A JSON map of configuration attributes `template_properties`. The following example shows a sample set of customization configuration:\n\n```\n{\n  {\n    \"ctaButtonBgColor\": \"#444DEA\",\n    \"ctaTextFontColor\": \"#FFFFFF\",\n    \"primaryBgColor\": \"#D3DEF0\",\n    \"hideMobileAppNudge\": false,\n    \"fontFamily\" : \"\",\n    \"hideProductName\" : false,\n    \"hideFooterPhone\" : false,\n    \"hideFooterAddress\" : false,\n    \"hidePrivacyPolicy\" : false,\n    \"hideManageNotification\" : false,\n    \"hideTsVocabularyDefinitions\": false,\n    \"hideNotificationStatus\" : false,\n    \"hideErrorMessage\": false,\n    \"hideUnsubscribeLink\" : false,\n    \"hideModifyAlert\": false,\n    \"textTransform\": \"\",\n    \"replacementValueForLiveboard\": \"LB dashboard\",\n    \"replacementValueForAnswer\": \"Answer dashboard\",\n    \"replacementValueForSpotIQ\": \"SpotIQ dashboard\",\n    \"logoUrl\":\"\",\n    \"productName\":\"ThoughtSpot\",\n    \"footerPhone\":\"(800) 508-7008\",\n    \"footerAddress\":\"444 Castro St, Suite 1000 Mountain View, CA 94041\",\n    \"companyWebsiteUrl\":\"\",\n    \"companyPrivacyPolicyUrl\":\"\"\n  }\n}\n```\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - template_properties (any) (required)\n    Email customization configuration as key value pair\n  - org_identifier (string) (optional)\n    Unique ID or name of org <br/>  <span class=\"since-beta-tag\">Version: 10.12.0.cl or later</span>")
    def createEmailCustomization(body: Optional[dict] = None):
        ''' <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.10.0.cl or later</span>  Creates a customization configuration for the notification email.  #### Pre-requisites  Requires `DEVELOPER` (**has developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.  **NOTE**:This endpoint in currently in beta. Contact ThoughtSpot support to enable this on your instance.  #### Usage guidelines  To create a custom configuration pass these parameters in your API request:  - A JSON map of configuration attributes `template_properties`. The following example shows a sample set of customization configuration:  ``` {   {     "ctaButtonBgColor": "#444DEA",     "ctaTextFontColor": "#FFFFFF",     "primaryBgColor": "#D3DEF0",     "hideMobileAppNudge": false,     "fontFamily" : "",     "hideProductName" : false,     "hideFooterPhone" : false,     "hideFooterAddress" : false,     "hidePrivacyPolicy" : false,     "hideManageNotification" : false,     "hideTsVocabularyDefinitions": false,     "hideNotificationStatus" : false,     "hideErrorMessage": false,     "hideUnsubscribeLink" : false,     "hideModifyAlert": false,     "textTransform": "",     "replacementValueForLiveboard": "LB dashboard",     "replacementValueForAnswer": "Answer dashboard",     "replacementValueForSpotIQ": "SpotIQ dashboard",     "logoUrl":"",     "productName":"ThoughtSpot",     "footerPhone":"(800) 508-7008",     "footerAddress":"444 Castro St, Suite 1000 Mountain View, CA 94041",     "companyWebsiteUrl":"",     "companyPrivacyPolicyUrl":""   } } ```     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/customization/email"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='deleteOrgEmailCustomization', description="\n<span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.12.0.cl or later</span>\n\nDeletes the configuration for the email customization.\n\n#### Pre-requisites\n\nRequires `DEVELOPER` (**has developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.\n\n**NOTE**:This endpoint in currently in beta. Contact ThoughtSpot support to enable this on your instance.\n\n#### Usage guidelines\n\n- Call the search API endpoint to get the `org_identifier` from the response.\n- Use that `org_identifier` as a parameter in this API request.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - org_identifiers (array of string) (optional)\n    Unique identifier of the organization.")
    def deleteOrgEmailCustomization(body: Optional[dict] = None):
        ''' <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.12.0.cl or later</span>  Deletes the configuration for the email customization.  #### Pre-requisites  Requires `DEVELOPER` (**has developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.  **NOTE**:This endpoint in currently in beta. Contact ThoughtSpot support to enable this on your instance.  #### Usage guidelines  - Call the search API endpoint to get the `org_identifier` from the response. - Use that `org_identifier` as a parameter in this API request.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/customization/email/delete"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='searchEmailCustomization', description="\n<span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.10.0.cl or later</span>\n\nSearch the email customization configuration if any set for the ThoughtSpot system.\n\n#### Pre-requisites\n\nRequires `DEVELOPER` (**has developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.\n\n**NOTE**:This endpoint in currently in beta. Contact ThoughtSpot support to enable this on your instance.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - org_identifiers (array of string) (optional)\n    Unique ID or name of org <br/>  <span class=\"since-beta-tag\">Version: 10.12.0.cl or later</span>")
    def searchEmailCustomization(body: Optional[dict] = None):
        ''' <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.10.0.cl or later</span>  Search the email customization configuration if any set for the ThoughtSpot system.  #### Pre-requisites  Requires `DEVELOPER` (**has developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.  **NOTE**:This endpoint in currently in beta. Contact ThoughtSpot support to enable this on your instance.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/customization/email/search"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateEmailCustomization', description="\n<span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.12.0.cl or later</span>\n\nUpdates a customization configuration for the notification email.\n\n#### Pre-requisites\n\nRequires `DEVELOPER` (**has developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.\n\n**NOTE**:This endpoint in currently in beta. Contact ThoughtSpot support to enable this on your instance.\n\n#### Usage guidelines\n\nTo update a custom configuration pass these parameters in your API request:\n\n- A JSON map of configuration attributes `template_properties`. The following example shows a sample set of customization configuration:\n\n```\n{\n  {\n    \"ctaButtonBgColor\": \"#444DEA\",\n    \"ctaTextFontColor\": \"#FFFFFF\",\n    \"primaryBgColor\": \"#D3DEF0\",\n    \"hideMobileAppNudge\": false,\n    \"fontFamily\" : \"\",\n    \"hideProductName\" : false,\n    \"hideFooterPhone\" : false,\n    \"hideFooterAddress\" : false,\n    \"hidePrivacyPolicy\" : false,\n    \"hideManageNotification\" : false,\n    \"hideTsVocabularyDefinitions\": false,\n    \"hideNotificationStatus\" : false,\n    \"hideErrorMessage\": false,\n    \"hideUnsubscribeLink\" : false,\n    \"hideModifyAlert\": false,\n    \"textTransform\": \"\",\n    \"replacementValueForLiveboard\": \"LB dashboard\",\n    \"replacementValueForAnswer\": \"Answer dashboard\",\n    \"replacementValueForSpotIQ\": \"SpotIQ dashboard\",\n    \"logoUrl\":\"\",\n    \"productName\":\"ThoughtSpot\",\n    \"footerPhone\":\"(800) 508-7008\",\n    \"footerAddress\":\"444 Castro St, Suite 1000 Mountain View, CA 94041\",\n    \"companyWebsiteUrl\":\"\",\n    \"companyPrivacyPolicyUrl\":\"\"\n  }\n}\n```\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - template_properties (any) (required)\n    Email customization configuration as key value pair\n  - org_identifier (string) (optional)\n    Unique ID or name of org")
    def updateEmailCustomization(body: Optional[dict] = None):
        ''' <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.12.0.cl or later</span>  Updates a customization configuration for the notification email.  #### Pre-requisites  Requires `DEVELOPER` (**has developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.  **NOTE**:This endpoint in currently in beta. Contact ThoughtSpot support to enable this on your instance.  #### Usage guidelines  To update a custom configuration pass these parameters in your API request:  - A JSON map of configuration attributes `template_properties`. The following example shows a sample set of customization configuration:  ``` {   {     "ctaButtonBgColor": "#444DEA",     "ctaTextFontColor": "#FFFFFF",     "primaryBgColor": "#D3DEF0",     "hideMobileAppNudge": false,     "fontFamily" : "",     "hideProductName" : false,     "hideFooterPhone" : false,     "hideFooterAddress" : false,     "hidePrivacyPolicy" : false,     "hideManageNotification" : false,     "hideTsVocabularyDefinitions": false,     "hideNotificationStatus" : false,     "hideErrorMessage": false,     "hideUnsubscribeLink" : false,     "hideModifyAlert": false,     "textTransform": "",     "replacementValueForLiveboard": "LB dashboard",     "replacementValueForAnswer": "Answer dashboard",     "replacementValueForSpotIQ": "SpotIQ dashboard",     "logoUrl":"",     "productName":"ThoughtSpot",     "footerPhone":"(800) 508-7008",     "footerAddress":"444 Castro St, Suite 1000 Mountain View, CA 94041",     "companyWebsiteUrl":"",     "companyPrivacyPolicyUrl":""   } } ```     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/customization/email/update"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='validateEmailCustomization', description="\n<span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.10.0.cl or later</span>\n\nValidates the email customization configuration if any set for the ThoughtSpot system.\n\n#### Pre-requisites\n\nRequires `DEVELOPER` (**has developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.\n\n**NOTE**:This endpoint in currently in beta. Contact ThoughtSpot support to enable this on your instance.\n\n\n\n\n#### Endpoint URL\n")
    def validateEmailCustomization():
        ''' <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.10.0.cl or later</span>  Validates the email customization configuration if any set for the ThoughtSpot system.  #### Pre-requisites  Requires `DEVELOPER` (**has developer privilege**) or `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `DEVELOPER` (**Has developer privilege**) privilege is required.  **NOTE**:This endpoint in currently in beta. Contact ThoughtSpot support to enable this on your instance.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/customization/email/validate"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='createUserGroup', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nCreates a group object in ThoughtSpot.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `GROUP_ADMINISTRATION` (**Can manage groups**) privilege is required.\n\n#### About groups\nGroups in ThoughtSpot are used by the administrators to define privileges and organize users based on their roles and access requirements. To know more about groups and privileges, see [ThoughtSpot Product Documentation](https://docs.thoughtspot.com/cloud/latest/groups-privileges).\n\n#### Supported operations\n\nThe API endpoint lets you perform the following operations:\n\n* Assign privileges\n* Add users\n* Define sharing visibility\n* Add sub-groups\n* Assign a default Liveboard\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (required)\n    Name of the group. The group name must be unique.\n  - display_name (string) (required)\n    Display name for the group.\n  - default_liveboard_identifiers (array of string) (optional)\n    GUID of the Liveboards to assign as default Liveboards to the users in the group.\n  - description (string) (optional)\n    Description of the group\n  - privileges (array of string) (optional)\n    Privileges to assign to the group\n  - sub_group_identifiers (array of string) (optional)\n    GUID or name of the sub groups. A subgroup is a group assigned to a parent group.\n  - type (string) (optional)\n    Group type.\n  - user_identifiers (array of string) (optional)\n    GUID or name of the users to assign to the group.\n  - visibility (string) (optional)\n    Visibility of the group. To make a group visible to other users and groups,\nset the visibility to SH\n  - role_identifiers (array of string) (optional)\n    Role identifiers of the roles that should be assigned to the group.")
    def createUserGroup(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Creates a group object in ThoughtSpot.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `GROUP_ADMINISTRATION` (**Can manage groups**) privilege is required.  #### About groups Groups in ThoughtSpot are used by the administrators to define privileges and organize users based on their roles and access requirements. To know more about groups and privileges, see [ThoughtSpot Product Documentation](https://docs.thoughtspot.com/cloud/latest/groups-privileges).  #### Supported operations  The API endpoint lets you perform the following operations:  * Assign privileges * Add users * Define sharing visibility * Add sub-groups * Assign a default Liveboard     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/groups/create"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='deleteUserGroup', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nRemoves the specified group object from the ThoughtSpot system.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `GROUP_ADMINISTRATION` (**Can manage groups**) privilege is required.\n\n\n\n\n#### Endpoint URL\n")
    def deleteUserGroup(group_identifier: str):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Removes the specified group object from the ThoughtSpot system.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `GROUP_ADMINISTRATION` (**Can manage groups**) privilege is required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/groups/{group_identifier}/delete"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if group_identifier is None:
            raise ValueError('Missing required path parameter: group_identifier')
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='importUserGroups', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nImports group objects from external databases into ThoughtSpot.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `GROUP_ADMINISTRATION` (**Can manage groups**) privilege is required.\n\nDuring the import operation:\n\n* If the specified group is not available in ThoughtSpot, it will be added to ThoughtSpot.\n* If `delete_unspecified_groups` is set to `true`, the groups not specified in the API request, excluding administrator and system user groups, are deleted.\n* If the specified groups are already available in ThoughtSpot, the object properties of these groups are modified and synchronized as per the input data in the API request.\n\nA successful API call returns the object that represents the changes made in the ThoughtSpot system.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - groups (array of object) (optional)\n    Details of groups which are to be imported\n  - delete_unspecified_groups (boolean) (optional)\n    If set to true, removes groups that are not specified in the API request.\n  - dry_run (boolean) (optional)\n    If true, the API performs a test operation and returns user IDs whose\ndata will be edited after the ")
    def importUserGroups(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Imports group objects from external databases into ThoughtSpot.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `GROUP_ADMINISTRATION` (**Can manage groups**) privilege is required.  During the import operation:  * If the specified group is not available in ThoughtSpot, it will be added to ThoughtSpot. * If `delete_unspecified_groups` is set to `true`, the groups not specified in the API request, excluding administrator and system user groups, are deleted. * If the specified groups are already available in ThoughtSpot, the object properties of these groups are modified and synchronized as per the input data in the API request.  A successful API call returns the object that represents the changes made in the ThoughtSpot system.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/groups/import"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='searchUserGroups', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nGets a list of user group objects from the ThoughtSpot system.\n\nTo get details of a specific user group, specify the user group GUID or name. You can also filter the API response based on User ID, Org ID, Role ID, type of group, sharing visibility, privileges assigned to the group, and the Liveboard IDs assigned to the users in the group.\n\nAvailable to all users. Users with `ADMINISTRATION` (**Can administer ThoughtSpot**) privileges can view all users properties.\n\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `GROUP_ADMINISTRATION` (**Can manage groups**) privilege is required.\n\n**NOTE**: If you do not get precise results, try setting `record_size` to `-1` and `record_offset` to `0`.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - default_liveboard_identifiers (array of string) (optional)\n    GUID of Liveboards that are assigned as default Liveboards to the users in the group.\n  - description (string) (optional)\n    Description of the group\n  - display_name (string) (optional)\n    Display name of the group\n  - name_pattern (string) (optional)\n    A pattern to match case-insensitive name of the Group object.\n  - group_identifier (string) (optional)\n    GUID or name of the group\n  - org_identifiers (array of string) (optional)\n    ID or name of the Org to which the group belongs\n  - privileges (array of string) (optional)\n    Privileges assigned to the group.\n  - sub_group_identifiers (array of string) (optional)\n    GUID or name of the sub groups. A subgroup is a group assigned to a parent group.\n  - type (string) (optional)\n    Group type.\n  - user_identifiers (array of string) (optional)\n    GUID or name of the users assigned to the group.\n  - visibility (string) (optional)\n    Visibility of the group. To make a group visible to other users and groups,\nset the visibility to SH\n  - role_identifiers (array of string) (optional)\n    Filter groups with a list of Roles assigned to a group\n  - record_offset (integer) (optional)\n    The starting record number from where the records should be included.\n  - record_size (integer) (optional)\n    The number of records that should be included.\n  - sort_options (any) (optional)\n    Sort options to filter group details.\n  - include_users (boolean) (optional)\n    <div><span class=\"since-beta-tag\">Version: 10.10.0.cl or later</span></div>\n\nDefine Parameter to con\n  - include_sub_groups (boolean) (optional)\n    <div><span class=\"since-beta-tag\">Version: 10.10.0.cl or later</span></div>\n\nDefine Parameter to con")
    def searchUserGroups(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Gets a list of user group objects from the ThoughtSpot system.  To get details of a specific user group, specify the user group GUID or name. You can also filter the API response based on User ID, Org ID, Role ID, type of group, sharing visibility, privileges assigned to the group, and the Liveboard IDs assigned to the users in the group.  Available to all users. Users with `ADMINISTRATION` (**Can administer ThoughtSpot**) privileges can view all users properties.  If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `GROUP_ADMINISTRATION` (**Can manage groups**) privilege is required.  **NOTE**: If you do not get precise results, try setting `record_size` to `-1` and `record_offset` to `0`.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/groups/search"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateUserGroup', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nUpdates the properties of a group object in ThoughtSpot.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `GROUP_ADMINISTRATION` (**Can manage groups**) privilege is required.\n\n#### Supported operations\n\nThis API endpoint lets you perform the following operations in a single API request:\n\n* Edit [privileges](https://developers.thoughtspot.com/docs/?pageid=api-user-management#group-privileges)\n* Add or remove users\n* Change sharing visibility settings\n* Add or remove sub-groups\n* Assign a default Liveboard  or update the existing settings\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (optional)\n    Name of the group to modify.\n  - default_liveboard_identifiers (array of string) (optional)\n    ID of the Liveboards to be assigned as default Liveboards to the users in the group.\n  - description (string) (optional)\n    Description for the group.\n  - display_name (string) (optional)\n    Display name of the group.\n  - privileges (array of string) (optional)\n    Privileges to assign to the group.\n  - sub_group_identifiers (array of string) (optional)\n    GUID or name of the sub groups. A subgroup is a group assigned to a parent group.\n  - type (string) (optional)\n    Type of the group\n  - user_identifiers (array of string) (optional)\n    GUID or name of the users to assign to the group.\n  - visibility (string) (optional)\n    Visibility of the group. To make a group visible to other users and\ngroups, set the visibility to SH\n  - role_identifiers (array of string) (optional)\n    Role identifiers of the Roles that should be assigned to the group.\n  - operation (string) (optional)\n    Type of update operation. Default operation type is REPLACE")
    def updateUserGroup(group_identifier: str, body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Updates the properties of a group object in ThoughtSpot.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `GROUP_ADMINISTRATION` (**Can manage groups**) privilege is required.  #### Supported operations  This API endpoint lets you perform the following operations in a single API request:  * Edit [privileges](https://developers.thoughtspot.com/docs/?pageid=api-user-management#group-privileges) * Add or remove users * Change sharing visibility settings * Add or remove sub-groups * Assign a default Liveboard  or update the existing settings     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/groups/{group_identifier}/update"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if group_identifier is None:
            raise ValueError('Missing required path parameter: group_identifier')
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='fetchLogs', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nFetches security audit logs.  \n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the [Admin Control](https://developers.thoughtspot.com/docs/rbac#_admin_control) privileges are required.\n\n\n#### Usage guidelines\n\nBy default, the API retrieves logs for the last 24 hours. You can set a custom duration in EPOCH time. Make sure the log duration specified in your API request doesn\u2019t exceed 24 hours. If you must fetch logs for a longer time range, modify the duration and make multiple sequential API requests.\n\nUpon successful execution, the API returns logs with the following information:\n* timestamp of the event\n* event ID\n* event type\n* name and GUID of the user\n* IP address of ThoughtSpot instance\n\nFor more information see [Audit logs Documentation](https://developers.thoughtspot.com/docs/audit-logs).\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - log_type (string) (required)\n    Name of the log type\n  - start_epoch_time_in_millis (number) (optional)\n    Start time in EPOCH format\n  - end_epoch_time_in_millis (number) (optional)\n    End time in EPOCH format\n  - get_all_logs (boolean) (optional)\n    Fetch all the logs. This is available from 9.10.5.cl")
    def fetchLogs(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Fetches security audit logs.    Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the [Admin Control](https://developers.thoughtspot.com/docs/rbac#_admin_control) privileges are required.   #### Usage guidelines  By default, the API retrieves logs for the last 24 hours. You can set a custom duration in EPOCH time. Make sure the log duration specified in your API request doesn’t exceed 24 hours. If you must fetch logs for a longer time range, modify the duration and make multiple sequential API requests.  Upon successful execution, the API returns logs with the following information: * timestamp of the event * event ID * event type * name and GUID of the user * IP address of ThoughtSpot instance  For more information see [Audit logs Documentation](https://developers.thoughtspot.com/docs/audit-logs).     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/logs/fetch"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='convertWorksheetToModel', description="\nConvert worksheets to models <br/>  <span class=\"since-beta-tag\">Version: 10.6.0.cl or later</span>\n\n## Prerequisites\n- **Privileges Required:**\n  - `DATAMANAGEMENT` (Can manage data) or `ADMINISTRATION` (Can administer ThoughtSpot).\n- **Additional Privileges (if RBAC is enabled):**\n  - `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (Can manage data models).\n\n---\n\n## Usage Guidelines\n\n### Parameters\n\n1. **worksheet_ids**  \n   - **Description:** A comma-separated list of GUIDs (Globally Unique Identifiers) specifying the Worksheets to be converted.  \n   - **Usage:**  \n     - Used only when `convert_all` is set to `false`.  \n     - Leave empty or omit when `convert_all` is set to `true`.\n\n2. **exclude_worksheet_ids**  \n   - **Description:** A comma-separated list of GUIDs specifying Worksheets to be excluded from conversion.  \n   - **Usage:**  \n     - Useful when `convert_all` is set to `true` and specific Worksheets should not be converted.\n\n3. **convert_all**  \n   - **Description:** Sets the scope of conversion.\n   - **Options:**  \n     - `true`: Converts all Worksheets in the system, except those specified in `exclude_worksheet_ids`.  \n     - `false`: Converts only the Worksheets listed in `worksheet_ids`.\n\n4. **apply_changes**  \n   - **Description:** Specifies whether to apply changes directly to ThoughtSpot or to generate a preview before applying any changes.Used for validation of conversion.\n   - **Options:**  \n     - `true`: Applies conversion changes directly to ThoughtSpot.\n     - `false`: Generates only a preview of the changes and does not apply any changes to ThoughtSpot\n\n---\n\n## Best Practices\n\n1. **Backup Before Conversion:**  \n   Always export metadata as a backup before initiating the conversion process\n\n2. **Partial Conversion for Testing:**  \n   Test the conversion process by setting `convert_all` to `false` and specifying a small number of `worksheet_ids`.\n\n3. **Verify Dependencies:**  \n   Check for dependent objects, such as Tables and Connections, to avoid invalid references.\n\n4. **Review Changes:**  \n   Use `apply_changes: false` to preview the impact of the conversion before applying changes.\n\n---\n\n## Examples\n\n### Convert Specific Worksheets\n```json\n{\n  \"worksheet_ids\": [\"guid1\", \"guid2\", \"guid3\"],\n  \"exclude_worksheet_ids\": [],\n  \"convert_all\": false,\n  \"apply_changes\": true\n}\n```\n\n### Convert All Accessible Worksheets\n```json\n{\n  \"worksheet_ids\": [],\n  \"exclude_worksheet_ids\": [],\n  \"convert_all\": true,\n  \"apply_changes\": true\n}\n```\n\n### Exclude Specific Worksheets While Converting All Accessible Worksheets\n```json\n{\n  \"worksheet_ids\": [],\n  \"exclude_worksheet_ids\": [\"abc\"],\n  \"convert_all\": true,\n  \"apply_changes\": true\n}\n```\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - worksheet_ids (array of string) (optional)\n    List of Worksheet IDs.\n  - exclude_worksheet_ids (array of string) (optional)\n    List of Worksheet IDs to be excluded.\n  - convert_all (boolean) (optional)\n    Indicates whether all the worksheet needs to be converted to models.\n  - apply_changes (boolean) (optional)\n    Indicates whether the changes should be applied to database.")
    def convertWorksheetToModel(body: Optional[dict] = None):
        ''' Convert worksheets to models <br/>  <span class="since-beta-tag">Version: 10.6.0.cl or later</span>  ## Prerequisites - **Privileges Required:**   - `DATAMANAGEMENT` (Can manage data) or `ADMINISTRATION` (Can administer ThoughtSpot). - **Additional Privileges (if RBAC is enabled):**   - `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (Can manage data models).  ---  ## Usage Guidelines  ### Parameters  1. **worksheet_ids**      - **Description:** A comma-separated list of GUIDs (Globally Unique Identifiers) specifying the Worksheets to be converted.      - **Usage:**        - Used only when `convert_all` is set to `false`.        - Leave empty or omit when `convert_all` is set to `true`.  2. **exclude_worksheet_ids**      - **Description:** A comma-separated list of GUIDs specifying Worksheets to be excluded from conversion.      - **Usage:**        - Useful when `convert_all` is set to `true` and specific Worksheets should not be converted.  3. **convert_all**      - **Description:** Sets the scope of conversion.    - **Options:**        - `true`: Converts all Worksheets in the system, except those specified in `exclude_worksheet_ids`.        - `false`: Converts only the Worksheets listed in `worksheet_ids`.  4. **apply_changes**      - **Description:** Specifies whether to apply changes directly to ThoughtSpot or to generate a preview before applying any changes.Used for validation of conversion.    - **Options:**        - `true`: Applies conversion changes directly to ThoughtSpot.      - `false`: Generates only a preview of the changes and does not apply any changes to ThoughtSpot  ---  ## Best Practices  1. **Backup Before Conversion:**      Always export metadata as a backup before initiating the conversion process  2. **Partial Conversion for Testing:**      Test the conversion process by setting `convert_all` to `false` and specifying a small number of `worksheet_ids`.  3. **Verify Dependencies:**      Check for dependent objects, such as Tables and Connections, to avoid invalid references.  4. **Review Changes:**      Use `apply_changes: false` to preview the impact of the conversion before applying changes.  ---  ## Examples  ### Convert Specific Worksheets ```json {   "worksheet_ids": ["guid1", "guid2", "guid3"],   "exclude_worksheet_ids": [],   "convert_all": false,   "apply_changes": true } ```  ### Convert All Accessible Worksheets ```json {   "worksheet_ids": [],   "exclude_worksheet_ids": [],   "convert_all": true,   "apply_changes": true } ```  ### Exclude Specific Worksheets While Converting All Accessible Worksheets ```json {   "worksheet_ids": [],   "exclude_worksheet_ids": ["abc"],   "convert_all": true,   "apply_changes": true } ```    #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/worksheets/convert"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='copyObject', description="\nMakes a copy of an Answer or Liveboard saved in Atlas <br/>  <span class=\"since-beta-tag\">Version: 10.3.0.cl or later</span>\n\nCreates a copy of a metadata object.\n\nRequires at least view access to the metadata object being copied.\n\nUpon successful execution, the API creates a copy of the metadata object specified in the API request and returns the ID of the new object.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - description (string) (optional)\n    Description of the new object\n  - identifier (string) (required)\n    GUID of metadata object to be copied (answer id or liveboard id)\n  - type (string) (optional)\n    Type of metadata object\n  - title (string) (optional)\n    Title of the new object")
    def copyObject(body: Optional[dict] = None):
        ''' Makes a copy of an Answer or Liveboard saved in Atlas <br/>  <span class="since-beta-tag">Version: 10.3.0.cl or later</span>  Creates a copy of a metadata object.  Requires at least view access to the metadata object being copied.  Upon successful execution, the API creates a copy of the metadata object specified in the API request and returns the ID of the new object.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/copyobject"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='deleteMetadata', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nRemoves the specified metadata object from the ThoughtSpot system.\n\nRequires edit access to the metadata object. \n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata (array of object) (required)\n    Metadata objects.\n  - delete_disabled_objects (boolean) (optional)\n    Indicates whether to delete disabled metadata objects.")
    def deleteMetadata(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Removes the specified metadata object from the ThoughtSpot system.  Requires edit access to the metadata object.      #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/delete"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='exportMetadataTML', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nExports the [TML](https://docs.thoughtspot.com/cloud/latest/tml) representation of metadata objects in JSON or YAML format.\n\nRequires `DATADOWNLOADING` (**Can download Data**) and at least view access to the metadata object.\n\n#### Usage guidelines\n\n* You can export one or several objects by passing metadata object GUIDs in the `metadata` array.\n* When exporting TML content for a Liveboard or Answer object, you can set `export_associated` to `true` to retrieve TML content for underlying Worksheets, Tables, or Views, including the GUID of each object within the headers. When   `export_associated` is set to `true`, consider retrieving one metadata object at a time.\n* Set `export_fqns` to `true` to add FQNs of the referenced objects in the TML content. For example, if you send an API request to retrieve TML for a Liveboard and its associated objects, the API returns the TML content with FQNs of the referenced Worksheet. Exporting TML with FQNs is useful if ThoughtSpot has multiple objects with the same name and you want to eliminate ambiguity when importing TML files into ThoughtSpot. It eliminates the need for adding FQNs of the referenced objects manually during the import operation.\n* To export only the TML of feedbacks associated with an object, set the GUID of the object as `identifier`, and set the `type` as `FEEDBACK` in the `metadata` array.\n* To export the TML of an object along with the feedbacks associated with it, set the GUID of the object as `identifier`, set the `type` as `LOGIAL_TABLE` in the `metadata` array, and set `export_with_associated_feedbacks` in `export_options` to true.\n\nFor more information, see [TML Documentation](https://developers.thoughtspot.com/docs/tml#_export_a_tml).\n\nFor more information on feedbacks, see [Feedback Documentation](https://docs.thoughtspot.com/cloud/latest/sage-feedback).\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata (array of object) (required)\n    Metadata objects.\n  - export_associated (boolean) (optional)\n    Indicates whether to export associated metadata objects of specified metadata objects.\n  - export_fqn (boolean) (optional)\n    Adds FQNs of the referenced objects. For example, if you are exporting a Liveboard and its associate\n  - edoc_format (string) (optional)\n    TML EDOC content format.\n\n**Note: exporting in YAML format currently requires manual formatting of t\n  - export_schema_version (string) (optional)\n    Indicates whether to export worksheet TML in DEFAULT or V1 or V2 version.\n  - export_dependent (boolean) (optional)\n    Indicates whether to export table while exporting connection.\n  - export_connection_as_dependent (boolean) (optional)\n    Indicates whether to export connection as dependent while exporting table/worksheet/answer/liveboard\n  - all_orgs_override (boolean) (optional)\n    Indicates whether to export is happening from all orgs context.\n  - export_options (any) (optional)\n    Flags to specify additional options for export. <br/>  <span class=\"since-beta-tag\">Version: 10.6.0.")
    def exportMetadataTML(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Exports the [TML](https://docs.thoughtspot.com/cloud/latest/tml) representation of metadata objects in JSON or YAML format.  Requires `DATADOWNLOADING` (**Can download Data**) and at least view access to the metadata object.  #### Usage guidelines  * You can export one or several objects by passing metadata object GUIDs in the `metadata` array. * When exporting TML content for a Liveboard or Answer object, you can set `export_associated` to `true` to retrieve TML content for underlying Worksheets, Tables, or Views, including the GUID of each object within the headers. When   `export_associated` is set to `true`, consider retrieving one metadata object at a time. * Set `export_fqns` to `true` to add FQNs of the referenced objects in the TML content. For example, if you send an API request to retrieve TML for a Liveboard and its associated objects, the API returns the TML content with FQNs of the referenced Worksheet. Exporting TML with FQNs is useful if ThoughtSpot has multiple objects with the same name and you want to eliminate ambiguity when importing TML files into ThoughtSpot. It eliminates the need for adding FQNs of the referenced objects manually during the import operation. * To export only the TML of feedbacks associated with an object, set the GUID of the object as `identifier`, and set the `type` as `FEEDBACK` in the `metadata` array. * To export the TML of an object along with the feedbacks associated with it, set the GUID of the object as `identifier`, set the `type` as `LOGIAL_TABLE` in the `metadata` array, and set `export_with_associated_feedbacks` in `export_options` to true.  For more information, see [TML Documentation](https://developers.thoughtspot.com/docs/tml#_export_a_tml).  For more information on feedbacks, see [Feedback Documentation](https://docs.thoughtspot.com/cloud/latest/sage-feedback).    #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/tml/export"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='exportMetadataTMLBatched', description="\n <span class=\"since-beta-tag\">Version: 10.1.0.cl or later</span>\n\nExports the [TML](https://docs.thoughtspot.com/cloud/latest/tml) representation of metadata objects in JSON or YAML format.\n\n### **Permissions Required**\n\nRequires `DATAMANAGEMENT` (**Can manage data**) and `USERMANAGEMENT` (**Can manage users**) privileges.\n\n#### **Usage Guidelines**\n\nThis API is only applicable for `USER`, `GROUP`, and `ROLES` metadata types.\n\n- `batch_offset` Indicates the starting position within the complete dataset from which the API should begin returning objects. Useful for paginating results efficiently.\n- `batch_size` Specifies the number of objects or items to retrieve in a single request. Helps control response size for better performance.\n- `edoc_format` Defines the format of the TML content. The exported metadata can be in JSON or YAML format.\n- `export_dependent` Specifies whether to include dependent metadata objects in the export. Ensures related objects are also retrieved if needed.\n- `all_orgs_override` Indicates whether the export operation applies across all organizations. Useful for multi-tenant environments where cross-org exports are required.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_type (string) (required)\n    Type of metadata object to export, can be one of USER | ROLE | USER_GROUP\n  - batch_offset (integer) (optional)\n    Indicates the position within the complete set from where the API should begin returning objects.\n  - batch_size (integer) (optional)\n    Determines the number of objects or items to be retrieved in a single request.\n  - edoc_format (string) (optional)\n    TML EDOC content format.\n  - export_dependent (boolean) (optional)\n    Indicates whether to export dependent metadata objects of specified metadata objects.\n  - all_orgs_override (boolean) (optional)\n    Indicates whether to export is happening from all orgs context.")
    def exportMetadataTMLBatched(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 10.1.0.cl or later</span>  Exports the [TML](https://docs.thoughtspot.com/cloud/latest/tml) representation of metadata objects in JSON or YAML format.  ### **Permissions Required**  Requires `DATAMANAGEMENT` (**Can manage data**) and `USERMANAGEMENT` (**Can manage users**) privileges.  #### **Usage Guidelines**  This API is only applicable for `USER`, `GROUP`, and `ROLES` metadata types.  - `batch_offset` Indicates the starting position within the complete dataset from which the API should begin returning objects. Useful for paginating results efficiently. - `batch_size` Specifies the number of objects or items to retrieve in a single request. Helps control response size for better performance. - `edoc_format` Defines the format of the TML content. The exported metadata can be in JSON or YAML format. - `export_dependent` Specifies whether to include dependent metadata objects in the export. Ensures related objects are also retrieved if needed. - `all_orgs_override` Indicates whether the export operation applies across all organizations. Useful for multi-tenant environments where cross-org exports are required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/tml/export/batch"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='fetchAnswerSqlQuery', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nFetches the underlying SQL query data for an Answer object.\n\nRequires at least view access to the Answer object.\n\nUpon successful execution, the API returns the SQL queries for the specified object as shown in this example:\n```\n{\n   \"metadata_id\":\"8fbe44a8-46ad-4b16-8d39-184b2fada490\",\n   \"metadata_name\":\"Total sales\",\n   \"metadata_type\":\"ANSWER\",\n   \"sql_queries\":[\n      {\n         \"metadata_id\":\"8fbe44a8-46ad-4b16-8d39-184b2fada490\",\n         \"metadata_name\":\"Total sales -test\",\n         \"sql_query\":\"SELECT \\n  \\\"ta_1\\\".\\\"REGION\\\" \\\"ca_1\\\", \\n  \\\"ta_2\\\".\\\"PRODUCTNAME\\\" \\\"ca_2\\\", \\n  \\\"ta_1\\\".\\\"STORENAME\\\" \\\"ca_3\\\", \\n  CASE\\n    WHEN sum(\\\"ta_3\\\".\\\"SALES\\\") IS NOT NULL THEN sum(\\\"ta_3\\\".\\\"SALES\\\")\\n    ELSE 0\\n  END \\\"ca_4\\\", \\n  CASE\\n    WHEN sum(\\\"ta_3\\\".\\\"QUANTITYPURCHASED\\\") IS NOT NULL THEN sum(\\\"ta_3\\\".\\\"QUANTITYPURCHASED\\\")\\n    ELSE 0\\n  END \\\"ca_5\\\"\\nFROM \\\"RETAILAPPAREL\\\".\\\"PUBLIC\\\".\\\"FACT_RETAPP_SALES\\\" \\\"ta_3\\\"\\n  JOIN \\\"RETAILAPPAREL\\\".\\\"PUBLIC\\\".\\\"DIM_RETAPP_STORES\\\" \\\"ta_1\\\"\\n    ON \\\"ta_3\\\".\\\"STOREID\\\" = \\\"ta_1\\\".\\\"STOREID\\\"\\n  JOIN \\\"RETAILAPPAREL\\\".\\\"PUBLIC\\\".\\\"DIM_RETAPP_PRODUCTS\\\" \\\"ta_2\\\"\\n    ON \\\"ta_3\\\".\\\"PRODUCTID\\\" = \\\"ta_2\\\".\\\"PRODUCTID\\\"\\nGROUP BY \\n  \\\"ca_1\\\", \\n  \\\"ca_2\\\", \\n  \\\"ca_3\\\"\\n\"\n      }\n   ]\n}\n```\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_identifier (string) (required)\n    ID or name of an Answer.")
    def fetchAnswerSqlQuery(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Fetches the underlying SQL query data for an Answer object.  Requires at least view access to the Answer object.  Upon successful execution, the API returns the SQL queries for the specified object as shown in this example: ``` {    "metadata_id":"8fbe44a8-46ad-4b16-8d39-184b2fada490",    "metadata_name":"Total sales",    "metadata_type":"ANSWER",    "sql_queries":[       {          "metadata_id":"8fbe44a8-46ad-4b16-8d39-184b2fada490",          "metadata_name":"Total sales -test",          "sql_query":"SELECT \n  \"ta_1\".\"REGION\" \"ca_1\", \n  \"ta_2\".\"PRODUCTNAME\" \"ca_2\", \n  \"ta_1\".\"STORENAME\" \"ca_3\", \n  CASE\n    WHEN sum(\"ta_3\".\"SALES\") IS NOT NULL THEN sum(\"ta_3\".\"SALES\")\n    ELSE 0\n  END \"ca_4\", \n  CASE\n    WHEN sum(\"ta_3\".\"QUANTITYPURCHASED\") IS NOT NULL THEN sum(\"ta_3\".\"QUANTITYPURCHASED\")\n    ELSE 0\n  END \"ca_5\"\nFROM \"RETAILAPPAREL\".\"PUBLIC\".\"FACT_RETAPP_SALES\" \"ta_3\"\n  JOIN \"RETAILAPPAREL\".\"PUBLIC\".\"DIM_RETAPP_STORES\" \"ta_1\"\n    ON \"ta_3\".\"STOREID\" = \"ta_1\".\"STOREID\"\n  JOIN \"RETAILAPPAREL\".\"PUBLIC\".\"DIM_RETAPP_PRODUCTS\" \"ta_2\"\n    ON \"ta_3\".\"PRODUCTID\" = \"ta_2\".\"PRODUCTID\"\nGROUP BY \n  \"ca_1\", \n  \"ca_2\", \n  \"ca_3\"\n"       }    ] } ```     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/answer/sql"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='fetchAsyncImportTaskStatus', description="\n <span class=\"since-beta-tag\">Version: 10.4.0.cl or later</span>\n\nGets information about the status of the TML async import task scheduled using the `/api/rest/2.0/metadata/tml/async/import` API call.\n\nTo fetch the task details, specify the ID of the TML async import task.  \n\nRequires access to the task ID. The API allows users who initiated the asynchronous TML import via `/api/rest/2.0/metadata/tml/async/import` to view the status of their tasks.  Users with administration privilege can view the status of all import tasks initiated by the users in their Org.\n\n#### Usage guidelines\n\nSee [TML API Documentation](https://developers.thoughtspot.com/docs/tml#_fetch_status_of_the_tml_import_task) for usage guidelines.\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - task_ids (array of string) (optional)\n    List of task IDs to fetch status for.\n  - task_status (array of string) (optional)\n    List of task statuses to filter on. Valid values: [IN_QUEUE, IN_PROGRESS, COMPLETED, FAILED]\n  - author_identifier (string) (optional)\n    Author GUID or name of async import tasks to filter on.\n  - record_offset (integer) (optional)\n    The offset point, starting from where the task status should be included in the response.\n  - record_size (integer) (optional)\n    The number of task statuses that should be included in the response starting from offset position.\n  - include_import_response (boolean) (optional)\n    Boolean flag to specify whether to include import response in the task status objects.")
    def fetchAsyncImportTaskStatus(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 10.4.0.cl or later</span>  Gets information about the status of the TML async import task scheduled using the `/api/rest/2.0/metadata/tml/async/import` API call.  To fetch the task details, specify the ID of the TML async import task.    Requires access to the task ID. The API allows users who initiated the asynchronous TML import via `/api/rest/2.0/metadata/tml/async/import` to view the status of their tasks.  Users with administration privilege can view the status of all import tasks initiated by the users in their Org.  #### Usage guidelines  See [TML API Documentation](https://developers.thoughtspot.com/docs/tml#_fetch_status_of_the_tml_import_task) for usage guidelines.    #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/tml/async/status"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='fetchLiveboardSqlQuery', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nFetches the underlying SQL query data for a Liveboard object and its visualizations.\n\nRequires at least view access to the Liveboard object.\n\nTo get SQL query data for a Liveboard, specify the GUID of the Liveboard. Optionally, you can add an array of visualization GUIDs to retrieve the SQL query data for visualizations in the Liveboard.\n\nUpon successful execution, the API returns the SQL queries for the specified object as shown in this example:\n```\n{\n  \"metadata_id\": \"fa68ae91-7588-4136-bacd-d71fb12dda69\",\n  \"metadata_name\": \"Total Sales\",\n  \"metadata_type\": \"LIVEBOARD\",\n  \"sql_queries\": [\n    {\n      \"metadata_id\": \"b3b6d2b9-089a-490c-8e16-b144650b7843\",\n      \"metadata_name\": \"Total quantity purchased, Total sales by region\",\n      \"sql_query\": \"SELECT \\n  \\\"ta_1\\\".\\\"REGION\\\" \\\"ca_1\\\", \\n  CASE\\n    WHEN sum(\\\"ta_2\\\".\\\"QUANTITYPURCHASED\\\") IS NOT NULL THEN sum(\\\"ta_2\\\".\\\"QUANTITYPURCHASED\\\")\\n    ELSE 0\\n  END \\\"ca_2\\\", \\n  CASE\\n    WHEN sum(\\\"ta_2\\\".\\\"SALES\\\") IS NOT NULL THEN sum(\\\"ta_2\\\".\\\"SALES\\\")\\n    ELSE 0\\n  END \\\"ca_3\\\"\\nFROM \\\"RETAILAPPAREL\\\".\\\"PUBLIC\\\".\\\"FACT_RETAPP_SALES\\\" \\\"ta_2\\\"\\n  JOIN \\\"RETAILAPPAREL\\\".\\\"PUBLIC\\\".\\\"DIM_RETAPP_STORES\\\" \\\"ta_1\\\"\\n    ON \\\"ta_2\\\".\\\"STOREID\\\" = \\\"ta_1\\\".\\\"STOREID\\\"\\nGROUP BY \\\"ca_1\\\"\"\n    }\n  ]\n}\n```\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_identifier (string) (required)\n    ID or name of the Liveboard.\n  - visualization_identifiers (array of string) (optional)\n    Unique ID or name of visualizations.")
    def fetchLiveboardSqlQuery(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Fetches the underlying SQL query data for a Liveboard object and its visualizations.  Requires at least view access to the Liveboard object.  To get SQL query data for a Liveboard, specify the GUID of the Liveboard. Optionally, you can add an array of visualization GUIDs to retrieve the SQL query data for visualizations in the Liveboard.  Upon successful execution, the API returns the SQL queries for the specified object as shown in this example: ``` {   "metadata_id": "fa68ae91-7588-4136-bacd-d71fb12dda69",   "metadata_name": "Total Sales",   "metadata_type": "LIVEBOARD",   "sql_queries": [     {       "metadata_id": "b3b6d2b9-089a-490c-8e16-b144650b7843",       "metadata_name": "Total quantity purchased, Total sales by region",       "sql_query": "SELECT \n  \"ta_1\".\"REGION\" \"ca_1\", \n  CASE\n    WHEN sum(\"ta_2\".\"QUANTITYPURCHASED\") IS NOT NULL THEN sum(\"ta_2\".\"QUANTITYPURCHASED\")\n    ELSE 0\n  END \"ca_2\", \n  CASE\n    WHEN sum(\"ta_2\".\"SALES\") IS NOT NULL THEN sum(\"ta_2\".\"SALES\")\n    ELSE 0\n  END \"ca_3\"\nFROM \"RETAILAPPAREL\".\"PUBLIC\".\"FACT_RETAPP_SALES\" \"ta_2\"\n  JOIN \"RETAILAPPAREL\".\"PUBLIC\".\"DIM_RETAPP_STORES\" \"ta_1\"\n    ON \"ta_2\".\"STOREID\" = \"ta_1\".\"STOREID\"\nGROUP BY \"ca_1\""     }   ] } ```     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/liveboard/sql"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='importMetadataTML', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nImports [TML](https://docs.thoughtspot.com/cloud/latest/tml) files into ThoughtSpot.\n\nRequires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtsSpot**) privilege.\n\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following Data control privileges may be required:\n- `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**)\n- `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)\n\n#### Usage guidelines\n\n*  Import all related objects in a single TML Import API call. For example, Tables that use the same Connection object and Worksheets connected to these Tables.\n* Include the `fqn` property to distinguish objects that have the same name.\n  For example, if you have multiple Connections or Worksheets with the same name on ThoughtSpot and the Connection or Worksheet referenced in your TML file does not have a unique name to distinguish, it may result in invalid object references.\n  Adding `fqn` helps ThoughtSpot differentiate a Table from another with the same name.\n  We recommend [exporting TML with FQNs](#/http/api-endpoints/metadata/export-metadata-tml) and using these during the import operation.\n* You can upload multiple TML files at a time.\n  If you import a Worksheet along with Liveboards, Answers, and other dependent objects in a single API call, the imported objects will be immediately available for use.\n  When you import only a Worksheet object, it may take some time for the Worksheet to become available in the ThoughtSpot system. Please wait for a few minutes, and then proceed to create an Answer and Liveboard from the newly imported Worksheet.\n\nFor more information, see [TML Documentation](https://developers.thoughtspot.com/docs/tml#_import_a_tml).\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_tmls (array of string) (required)\n    Details of TML objects.\n\n**Note: importing TML in YAML format, when coming directly from our Playgro\n  - import_policy (string) (optional)\n    Specifies the import policy for the TML import.\n  - create_new (boolean) (optional)\n    If selected, creates TML objects with new GUIDs.\n  - all_orgs_override (boolean) (optional)\n    If import is happening from all orgs context.\n  - skip_diff_check (boolean) (optional)\n    <div><span class=\"since-beta-tag\">Version: 10.6.0.cl or later</span></div>\n\nBoolean Flag to skip TML\n  - enable_large_metadata_validation (boolean) (optional)\n    <div><span class=\"since-beta-tag\">Version: 10.5.0.cl or later</span></div>\n\nBoolean to indicate if t")
    def importMetadataTML(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Imports [TML](https://docs.thoughtspot.com/cloud/latest/tml) files into ThoughtSpot.  Requires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtsSpot**) privilege.  If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following Data control privileges may be required: - `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) - `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)  #### Usage guidelines  *  Import all related objects in a single TML Import API call. For example, Tables that use the same Connection object and Worksheets connected to these Tables. * Include the `fqn` property to distinguish objects that have the same name.   For example, if you have multiple Connections or Worksheets with the same name on ThoughtSpot and the Connection or Worksheet referenced in your TML file does not have a unique name to distinguish, it may result in invalid object references.   Adding `fqn` helps ThoughtSpot differentiate a Table from another with the same name.   We recommend [exporting TML with FQNs](#/http/api-endpoints/metadata/export-metadata-tml) and using these during the import operation. * You can upload multiple TML files at a time.   If you import a Worksheet along with Liveboards, Answers, and other dependent objects in a single API call, the imported objects will be immediately available for use.   When you import only a Worksheet object, it may take some time for the Worksheet to become available in the ThoughtSpot system. Please wait for a few minutes, and then proceed to create an Answer and Liveboard from the newly imported Worksheet.  For more information, see [TML Documentation](https://developers.thoughtspot.com/docs/tml#_import_a_tml).     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/tml/import"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='importMetadataTMLAsync', description="\n <span class=\"since-beta-tag\">Version: 10.4.0.cl or later</span>\n\nSchedules a task to import [TML](https://docs.thoughtspot.com/cloud/latest/tml) files into ThoughtSpot. You can use this API endpoint to process TML objects asynchronously when importing TMLs of large and complex metadata objects into ThoughtSpot. Unlike the synchronous import TML operation, the API processes TML data in the background and returns a task ID, which can be used to check the status of the import task via `/api/rest/2.0/metadata/tml/async/status` API endpoint.\n\nRequires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtsSpot**) privilege, and edit access to the TML objects.\n\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following Data control privileges may be required:\n- `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**)\n- `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)\n\n#### Usage guidelines\n\nSee [Async TML API Documentation](https://developers.thoughtspot.com/docs/tml#_import_tml_objects_asynchronously) for usage guidelines.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_tmls (array of string) (required)\n    Details of TML objects.\n  - create_new (boolean) (optional)\n    If selected, creates TML objects with new GUIDs.\n  - all_orgs_override (boolean) (optional)\n    If import is happening from all orgs context.\n  - import_policy (string) (optional)\n    <div><span class=\"since-beta-tag\">Version: 10.5.0.cl or later</span></div>\n\nPolicy to be followed wh\n  - skip_diff_check (boolean) (optional)\n    <div><span class=\"since-beta-tag\">Version: 10.6.0.cl or later</span></div>\n\nBoolean Flag to skip TML\n  - enable_large_metadata_validation (boolean) (optional)\n    <div><span class=\"since-beta-tag\">Version: 10.5.0.cl or later</span></div>\n\nBoolean to indicate if t")
    def importMetadataTMLAsync(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 10.4.0.cl or later</span>  Schedules a task to import [TML](https://docs.thoughtspot.com/cloud/latest/tml) files into ThoughtSpot. You can use this API endpoint to process TML objects asynchronously when importing TMLs of large and complex metadata objects into ThoughtSpot. Unlike the synchronous import TML operation, the API processes TML data in the background and returns a task ID, which can be used to check the status of the import task via `/api/rest/2.0/metadata/tml/async/status` API endpoint.  Requires `DATAMANAGEMENT` (**Can manage data**) or `ADMINISTRATION` (**Can administer ThoughtsSpot**) privilege, and edit access to the TML objects.  If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the following Data control privileges may be required: - `CAN_CREATE_OR_EDIT_CONNECTIONS` (**Can create/edit Connections**) - `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**)  #### Usage guidelines  See [Async TML API Documentation](https://developers.thoughtspot.com/docs/tml#_import_tml_objects_asynchronously) for usage guidelines.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/tml/async/import"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='parameterizeMetadata', description="\nParameterize fields in metadata objects. <br/> <span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.9.0.cl or later</span>\n\nAllows parameterizing fields in metadata objects in ThoughtSpot.\n\nRequires appropriate permissions to modify the metadata object.\n\nThe API endpoint allows parameterizing the following types of metadata objects:\n* Logical Tables\n* Connections\n\nFor a Logical Table the field type must be `ATTRIBUTE` and field name can be one of:\n* databaseName\n* schemaName\n* tableName\n\nFor a Connection the field type is always `CONNECTION_PROPERTY`. We use the field_name in this\ncase to specify the exact property of a connection which needs to be parameterized.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_type (string) (optional)\n    Type of metadata object to parameterize.\n  - metadata_identifier (string) (required)\n    Unique ID or name of the metadata object to parameterize.\n  - field_type (string) (required)\n    Type of field in the metadata to parameterize.\n  - field_name (string) (required)\n    Name of the field which needs to be parameterized.\n  - variable_identifier (string) (required)\n    Unique ID or name of the variable to use for parameterization")
    def parameterizeMetadata(body: Optional[dict] = None):
        ''' Parameterize fields in metadata objects. <br/> <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.9.0.cl or later</span>  Allows parameterizing fields in metadata objects in ThoughtSpot.  Requires appropriate permissions to modify the metadata object.  The API endpoint allows parameterizing the following types of metadata objects: * Logical Tables * Connections  For a Logical Table the field type must be `ATTRIBUTE` and field name can be one of: * databaseName * schemaName * tableName  For a Connection the field type is always `CONNECTION_PROPERTY`. We use the field_name in this case to specify the exact property of a connection which needs to be parameterized.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/parameterize"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='searchMetadata', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nGets a list of metadata objects available on the ThoughtSpot system.\n\nThis API endpoint is available to all users who have view access to the object. Users with `ADMINISTRATION` (**Can administer ThoughtSpot**) privileges can view data for all metadata objects, including users and groups.\n\n#### Usage guidelines\n\n- To get all metadata objects, send the API request without any attributes.\n- To get metadata objects of a specific type, set the `type` attribute. For example, to fetch a Worksheet, set the type as `LOGICAL_TABLE`.\n- To filter metadata objects within type `LOGICAL_TABLE`, set the `subtypes` attribute. For example, to fetch a Worksheet, set the type as `LOGICAL_TABLE` & subtypes as `[WORKSHEET]`.\n- To get a specific metadata object, specify the GUID.\n- To customize your search and filter the API response, you can use several parameters.\n  You can search for objects created or modified by specific users, by tags applied to the objects, or by using the include parameters like `include_auto_created_objects`, `include_dependent_objects`, `include_headers`, `include_incomplete_objects`, and so on.\n  You can also define sorting options to sort the data retrieved in the API response.\n- To get discoverable objects when linientmodel is enabled you can use `include_discoverable_objects` as true else false. Default value is true.\n- For liveboard metadata type, to get the newer format, set the `liveboard_response_format` as V2. Default value is V1.\n- To retrieve only objects that are published, set the `include_only_published_objects` as true. Default value is false.\n\n**NOTE**: The following parameters support pagination of metadata records:\n\n- `tag_identifiers`\n- `type`\n- `subtypes`\n- `created_by_user_identifiers`\n- `modified_by_user_identifiers`\n- `owned_by_user_identifiers`\n- `exclude_objects`\n- `include_auto_created_objects`\n- `favorite_object_options`\n- `include_only_published_objects`\nIf you are using other parameters to search metadata, set `record_size` to `-1` and `record_offset` to `0`.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata (array of object) (optional)\n    Metadata objects such as Liveboards, Answers, and Worksheets.\n  - permissions (array of object) (optional)\n    Object permission details to search by.\n  - created_by_user_identifiers (array of string) (optional)\n    GUID or name of user who created the metadata object.\n  - dependent_object_version (string) (optional)\n    Version of the dependent table of the metadata objects like Worksheets.\n  - exclude_objects (array of object) (optional)\n    List of metadata objects to exclude from search.\n  - favorite_object_options (any) (optional)\n    Options to sort the API response by objects set as favorites\nfor the logged-in user or the users spe\n  - include_auto_created_objects (boolean) (optional)\n    Includes system-generated metadata objects.\n  - include_dependent_objects (boolean) (optional)\n    Includes dependents of the metadata object specified in the API request.\nFor example, a worksheet ca\n  - dependent_objects_record_size (integer) (optional)\n    The maximum number of dependents to include per metadata object.\n  - include_details (boolean) (optional)\n    Includes complete details of the metadata objects.\n  - include_headers (boolean) (optional)\n    Includes headers of the metadata objects.\n  - include_hidden_objects (boolean) (optional)\n    Includes details of the hidden objects, such as a column in a worksheet or a table.\n  - include_incomplete_objects (boolean) (optional)\n    Includes objects with incomplete metadata.\n  - include_visualization_headers (boolean) (optional)\n    Includes visualization headers of the specified Liveboard object.\n  - include_worksheet_search_assist_data (boolean) (optional)\n    If search assistance lessons are configured on a worksheet,\nthe API returns the search assist data f\n  - modified_by_user_identifiers (array of string) (optional)\n    Includes ID or names of the users who modified the metadata object.\n  - record_offset (integer) (optional)\n    The starting record number from where the records should be included.\n  - record_size (integer) (optional)\n    The number of records that should be included. It is recommended to use a smaller `record_size` when\n  - sort_options (any) (optional)\n    Sort options to filter metadata details.\n  - tag_identifiers (array of string) (optional)\n    Tags to filter metadata objects by\n  - include_stats (boolean) (optional)\n    Indicates whether to include stats of the metadata objects.\n  - include_discoverable_objects (boolean) (optional)\n    <div><span class=\"since-beta-tag\">Version: 10.7.0.cl or later</span></div>\n\nBoolean to indicate whet\n  - show_resolved_parameters (boolean) (optional)\n    <div><span class=\"since-beta-tag\">Version: 10.9.0.cl or later</span></div>\n\nIndicates whether to sho\n  - liveboard_response_version (string) (optional)\n    Indicates the model version of Liveboard to be attached in metadata detail.\n  - include_only_published_objects (boolean) (optional)\n    <div><span class=\"since-beta-tag\">Version: 10.11.0.cl or later</span></div>\n\nIf only published objec")
    def searchMetadata(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Gets a list of metadata objects available on the ThoughtSpot system.  This API endpoint is available to all users who have view access to the object. Users with `ADMINISTRATION` (**Can administer ThoughtSpot**) privileges can view data for all metadata objects, including users and groups.  #### Usage guidelines  - To get all metadata objects, send the API request without any attributes. - To get metadata objects of a specific type, set the `type` attribute. For example, to fetch a Worksheet, set the type as `LOGICAL_TABLE`. - To filter metadata objects within type `LOGICAL_TABLE`, set the `subtypes` attribute. For example, to fetch a Worksheet, set the type as `LOGICAL_TABLE` & subtypes as `[WORKSHEET]`. - To get a specific metadata object, specify the GUID. - To customize your search and filter the API response, you can use several parameters.   You can search for objects created or modified by specific users, by tags applied to the objects, or by using the include parameters like `include_auto_created_objects`, `include_dependent_objects`, `include_headers`, `include_incomplete_objects`, and so on.   You can also define sorting options to sort the data retrieved in the API response. - To get discoverable objects when linientmodel is enabled you can use `include_discoverable_objects` as true else false. Default value is true. - For liveboard metadata type, to get the newer format, set the `liveboard_response_format` as V2. Default value is V1. - To retrieve only objects that are published, set the `include_only_published_objects` as true. Default value is false.  **NOTE**: The following parameters support pagination of metadata records:  - `tag_identifiers` - `type` - `subtypes` - `created_by_user_identifiers` - `modified_by_user_identifiers` - `owned_by_user_identifiers` - `exclude_objects` - `include_auto_created_objects` - `favorite_object_options` - `include_only_published_objects` If you are using other parameters to search metadata, set `record_size` to `-1` and `record_offset` to `0`.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/search"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='unparameterizeMetadata', description="\nRemove parameterization from fields in metadata objects. <br/> <span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.9.0.cl or later</span>\n\nAllows removing parameterization from fields in metadata objects in ThoughtSpot.\n\nRequires appropriate permissions to modify the metadata object.\n\nThe API endpoint allows unparameterizing the following types of metadata objects:\n* Logical Tables\n* Connections\n\nFor a Logical Table the field type must be `ATTRIBUTE` and field name can be one of:\n* databaseName\n* schemaName\n* tableName\n\nFor a Connection the field type is always `CONNECTION_PROPERTY`. We use the field_name in this\ncase to specify the exact property of a connection which needs to be unparameterized.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_type (string) (optional)\n    Type of metadata object to unparameterize.\n  - metadata_identifier (string) (required)\n    Unique ID or name of the metadata object to unparameterize.\n  - field_type (string) (required)\n    Type of field in the metadata to unparameterize.\n  - field_name (string) (required)\n    Name of the field which needs to be unparameterized.\n  - value (string) (required)\n    The value to use in place of the variable for the field")
    def unparameterizeMetadata(body: Optional[dict] = None):
        ''' Remove parameterization from fields in metadata objects. <br/> <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.9.0.cl or later</span>  Allows removing parameterization from fields in metadata objects in ThoughtSpot.  Requires appropriate permissions to modify the metadata object.  The API endpoint allows unparameterizing the following types of metadata objects: * Logical Tables * Connections  For a Logical Table the field type must be `ATTRIBUTE` and field name can be one of: * databaseName * schemaName * tableName  For a Connection the field type is always `CONNECTION_PROPERTY`. We use the field_name in this case to specify the exact property of a connection which needs to be unparameterized.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/unparameterize"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateMetadataHeader', description="\nUpdate header attributes for a given list of header objects. <br/> <span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.6.0.cl or later</span>\n\n## Prerequisites\n- **Privileges Required:**\n  - `DATAMANAGEMENT` (Can manage data) or `ADMINISTRATION` (Can administer ThoughtSpot).\n- **Additional Privileges (if RBAC is enabled):**\n  - `ORG_ADMINISTRATION` (Can manage orgs).\n\n---\n\n## Usage Guidelines\n\n### Parameters\n\n1. **headers_update**  \n   - **Description:** List of header objects with their attributes to be updated. Each object contains a list of attributes to be updated in the header.\n   - **Usage:**\n      - You must provide either `identifier` or `obj_identifier`, but not both. Both fields cannot be empty.\n      - When `org_identifier` is set to `-1`, only the `identifier` value is accepted; `obj_identifier` is not allowed.\n\n2. **org_identifier**  \n   - **Description:** GUID (Globally Unique Identifier) or name of the organization.  \n   - **Usage:**\n     - Leaving this field empty assumes that the changes should be applied to the current organization \n     - Provide `org_guid` or `org_name` to uniquely identify the organization where changes need to be applied. .\n     - Provide `-1` if changes have to be applied across all the org.\n\n---\n\n## Note\nCurrently, this API is enabled only for updating the `obj_identifier` attribute. Only `text` will be allowed in attribute's value.\n\n## Best Practices\n\n1. **Backup Before Conversion:**  \n   Always export metadata as a backup before initiating the update process\n\n---\n\n## Examples\n\n### Only `identifier` is given \n```json\n{\n  \"headers_update\":\n  [\n    {\n      \"identifier\": \"guid_1\",\n      \"obj_identifier\": \"\",\n      \"type\": \"LOGICAL_COLUMN\",\n      \"attributes\":\n      [\n        {\n          \"name\": \"obj_id\",\n          \"value\": \"custom_object_id\"\n        }\n      ]\n    }\n  ],\n  \"org_identifier\": \"orgGuid\"\n}\n```\n\n### Only `obj_identifier` is given\n```json\n{\n  \"headers_update\":\n  [\n    {\n      \"obj_identifier\": \"custom_object_id\",\n      \"type\": \"ANSWER\",\n      \"attributes\":\n      [\n        {\n          \"name\": \"obj_id\",\n          \"value\": \"custom_object_id\"\n        }\n      ]\n    }\n  ],\n  \"org_identifier\": \"orgName\"\n}\n```\n\n### Executing update for all org `-1`\n```json\n{\n  \"headers_update\":\n  [\n    {\n      \"identifier\": \"guid_1\",\n      \"type\": \"ANSWER\",\n      \"attributes\":\n      [\n        {\n          \"name\": \"obj_id\",\n          \"value\": \"custom_object_id\"\n        }\n      ]\n    }\n  ],\n  \"org_identifier\": -1\n}\n```\n\n### Optional `type` is not provided\n```json\n{\n  \"headers_update\":\n  [\n    {\n      \"identifier\": \"guid_1\",\n      \"attributes\":\n      [\n        {\n          \"name\": \"obj_id\",\n          \"value\": \"custom_object_id\"\n        }\n      ]\n    }\n  ],\n  \"org_identifier\": -1\n}\n```\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - headers_update (array of object) (required)\n    List of header objects to update.\n  - org_identifier (string) (optional)\n    Unique ID or name of the organization.")
    def updateMetadataHeader(body: Optional[dict] = None):
        ''' Update header attributes for a given list of header objects. <br/> <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.6.0.cl or later</span>  ## Prerequisites - **Privileges Required:**   - `DATAMANAGEMENT` (Can manage data) or `ADMINISTRATION` (Can administer ThoughtSpot). - **Additional Privileges (if RBAC is enabled):**   - `ORG_ADMINISTRATION` (Can manage orgs).  ---  ## Usage Guidelines  ### Parameters  1. **headers_update**      - **Description:** List of header objects with their attributes to be updated. Each object contains a list of attributes to be updated in the header.    - **Usage:**       - You must provide either `identifier` or `obj_identifier`, but not both. Both fields cannot be empty.       - When `org_identifier` is set to `-1`, only the `identifier` value is accepted; `obj_identifier` is not allowed.  2. **org_identifier**      - **Description:** GUID (Globally Unique Identifier) or name of the organization.      - **Usage:**      - Leaving this field empty assumes that the changes should be applied to the current organization       - Provide `org_guid` or `org_name` to uniquely identify the organization where changes need to be applied. .      - Provide `-1` if changes have to be applied across all the org.  ---  ## Note Currently, this API is enabled only for updating the `obj_identifier` attribute. Only `text` will be allowed in attribute's value.  ## Best Practices  1. **Backup Before Conversion:**      Always export metadata as a backup before initiating the update process  ---  ## Examples  ### Only `identifier` is given  ```json {   "headers_update":   [     {       "identifier": "guid_1",       "obj_identifier": "",       "type": "LOGICAL_COLUMN",       "attributes":       [         {           "name": "obj_id",           "value": "custom_object_id"         }       ]     }   ],   "org_identifier": "orgGuid" } ```  ### Only `obj_identifier` is given ```json {   "headers_update":   [     {       "obj_identifier": "custom_object_id",       "type": "ANSWER",       "attributes":       [         {           "name": "obj_id",           "value": "custom_object_id"         }       ]     }   ],   "org_identifier": "orgName" } ```  ### Executing update for all org `-1` ```json {   "headers_update":   [     {       "identifier": "guid_1",       "type": "ANSWER",       "attributes":       [         {           "name": "obj_id",           "value": "custom_object_id"         }       ]     }   ],   "org_identifier": -1 } ```  ### Optional `type` is not provided ```json {   "headers_update":   [     {       "identifier": "guid_1",       "attributes":       [         {           "name": "obj_id",           "value": "custom_object_id"         }       ]     }   ],   "org_identifier": -1 } ```    #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/headers/update"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateMetadataObjId', description="\nUpdate object IDs for given metadata objects. <br/> <span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.8.0.cl or later</span>\n\n## Prerequisites\n- **Privileges Required:**\n  - `DATAMANAGEMENT` (Can manage data) or `ADMINISTRATION` (Can administer ThoughtSpot).\n- **Additional Privileges (if RBAC is enabled):**\n  - `ORG_ADMINISTRATION` (Can manage orgs).\n\n---\n\n## Usage Guidelines\n\n### Parameters\n\n1. **metadata**  \n   - **Description:** List of metadata objects to update their object IDs.\n   - **Usage:**\n      - Use either `current_obj_id` alone OR use `metadata_identifier` with `type` (when needed).\n      - When using `metadata_identifier`, the `type` field is required if using a name instead of a GUID.\n      - The `new_obj_id` field is always required.\n\n---\n\n## Note\nThis API is specifically designed for updating object IDs of metadata objects. It internally uses the header update mechanism to perform the changes.\n\n## Best Practices\n\n1. **Backup Before Update:**  \n   Always export metadata as a backup before initiating the update process.\n\n2. **Validation:**\n   - When using `current_obj_id`, ensure it matches the existing object ID exactly.\n   - When using `metadata_identifier` with a name, ensure the `type` is specified correctly.\n   - Verify that the `new_obj_id` follows your naming conventions and is unique within your system.\n\n---\n\n## Examples\n\n### Using current_obj_id\n```json\n{\n  \"metadata\": [\n    {\n      \"current_obj_id\": \"existing_object_id\",\n      \"new_obj_id\": \"new_object_id\"\n    }\n  ]\n}\n```\n\n### Using metadata_identifier with GUID\n```json\n{\n  \"metadata\": [\n    {\n      \"metadata_identifier\": \"01234567-89ab-cdef-0123-456789abcdef\",\n      \"new_obj_id\": \"new_object_id\"\n    }\n  ]\n}\n```\n\n### Using metadata_identifier with name and type\n```json\n{\n  \"metadata\": [\n    {\n      \"metadata_identifier\": \"My Answer\",\n      \"type\": \"ANSWER\",\n      \"new_obj_id\": \"new_object_id\"\n    }\n  ]\n}\n```\n\n### Multiple objects update\n```json\n{\n  \"metadata\": [\n    {\n      \"current_obj_id\": \"existing_object_id_1\",\n      \"new_obj_id\": \"new_object_id_1\"\n    },\n    {\n      \"metadata_identifier\": \"My Worksheet\",\n      \"type\": \"LOGICAL_TABLE\",\n      \"new_obj_id\": \"new_object_id_2\"\n    }\n  ]\n}\n```\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata (array of object) (required)\n    List of metadata objects to update their object IDs.")
    def updateMetadataObjId(body: Optional[dict] = None):
        ''' Update object IDs for given metadata objects. <br/> <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.8.0.cl or later</span>  ## Prerequisites - **Privileges Required:**   - `DATAMANAGEMENT` (Can manage data) or `ADMINISTRATION` (Can administer ThoughtSpot). - **Additional Privileges (if RBAC is enabled):**   - `ORG_ADMINISTRATION` (Can manage orgs).  ---  ## Usage Guidelines  ### Parameters  1. **metadata**      - **Description:** List of metadata objects to update their object IDs.    - **Usage:**       - Use either `current_obj_id` alone OR use `metadata_identifier` with `type` (when needed).       - When using `metadata_identifier`, the `type` field is required if using a name instead of a GUID.       - The `new_obj_id` field is always required.  ---  ## Note This API is specifically designed for updating object IDs of metadata objects. It internally uses the header update mechanism to perform the changes.  ## Best Practices  1. **Backup Before Update:**      Always export metadata as a backup before initiating the update process.  2. **Validation:**    - When using `current_obj_id`, ensure it matches the existing object ID exactly.    - When using `metadata_identifier` with a name, ensure the `type` is specified correctly.    - Verify that the `new_obj_id` follows your naming conventions and is unique within your system.  ---  ## Examples  ### Using current_obj_id ```json {   "metadata": [     {       "current_obj_id": "existing_object_id",       "new_obj_id": "new_object_id"     }   ] } ```  ### Using metadata_identifier with GUID ```json {   "metadata": [     {       "metadata_identifier": "01234567-89ab-cdef-0123-456789abcdef",       "new_obj_id": "new_object_id"     }   ] } ```  ### Using metadata_identifier with name and type ```json {   "metadata": [     {       "metadata_identifier": "My Answer",       "type": "ANSWER",       "new_obj_id": "new_object_id"     }   ] } ```  ### Multiple objects update ```json {   "metadata": [     {       "current_obj_id": "existing_object_id_1",       "new_obj_id": "new_object_id_1"     },     {       "metadata_identifier": "My Worksheet",       "type": "LOGICAL_TABLE",       "new_obj_id": "new_object_id_2"     }   ] } ```     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/metadata/update-obj-id"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='createOrg', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nCreates an Org object.\n\nTo use this API, the [Orgs](https://docs.thoughtspot.com/cloud/latest/orgs-overview) feature must be enabled in your cluster.\n\nRequires cluster administration (**Can administer Org**) privileges.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `ORG_ADMINISTRATION` (**Can manage Orgs**) privilege is required.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (required)\n    Name of the Org.\n  - description (string) (optional)\n    Description of the Org.")
    def createOrg(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Creates an Org object.  To use this API, the [Orgs](https://docs.thoughtspot.com/cloud/latest/orgs-overview) feature must be enabled in your cluster.  Requires cluster administration (**Can administer Org**) privileges. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `ORG_ADMINISTRATION` (**Can manage Orgs**) privilege is required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/orgs/create"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='deleteOrg', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nDeletes an Org object from the ThoughtSpot system.\n\nRequires cluster administration (**Can administer Org**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `ORG_ADMINISTRATION` (**Can manage Orgs**) privilege is required.\n\nWhen you delete an Org, all its users and objects created in that Org context are removed. However, if the users in the deleted Org also exists in other Orgs, they are removed only from the deleted Org.\n\n\n\n\n#### Endpoint URL\n")
    def deleteOrg(org_identifier: str):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Deletes an Org object from the ThoughtSpot system.  Requires cluster administration (**Can administer Org**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `ORG_ADMINISTRATION` (**Can manage Orgs**) privilege is required.  When you delete an Org, all its users and objects created in that Org context are removed. However, if the users in the deleted Org also exists in other Orgs, they are removed only from the deleted Org.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/orgs/{org_identifier}/delete"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if org_identifier is None:
            raise ValueError('Missing required path parameter: org_identifier')
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='searchOrgs', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nGets a list of Orgs configured on the ThoughtSpot system. To get details of a specific Org, specify the Org ID or name. You can also pass parameters such as status, visibility, and user identifiers to get a specific list of Orgs.\n\nRequires cluster administration (**Can administer Org**) privileges.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `ORG_ADMINISTRATION` (**Can manage Orgs**) privilege is required.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - org_identifier (string) (optional)\n    ID or name of the Org\n  - description (string) (optional)\n    Description of the Org\n  - visibility (string) (optional)\n    Visibility of the Org\n  - status (string) (optional)\n    Status of the Org\n  - user_identifiers (array of string) (optional)\n    GUIDs or names of the users for which you want to retrieve the Orgs data")
    def searchOrgs(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Gets a list of Orgs configured on the ThoughtSpot system. To get details of a specific Org, specify the Org ID or name. You can also pass parameters such as status, visibility, and user identifiers to get a specific list of Orgs.  Requires cluster administration (**Can administer Org**) privileges. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `ORG_ADMINISTRATION` (**Can manage Orgs**) privilege is required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/orgs/search"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateOrg', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nUpdates an Org object. You can modify Org properties such as name, description, and user associations.\n\nRequires cluster administration (**Can administer Org**) privileges.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `ORG_ADMINISTRATION` (**Can manage Orgs**) privilege is required.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (optional)\n    Name of the Org\n  - description (string) (optional)\n    Description of the Org\n  - user_identifiers (array of string) (optional)\n    Add Users to an Org\n  - group_identifiers (array of string) (optional)\n    Add Default Groups to an Org\n  - operation (string) (optional)\n    Type of update operation. Default operation type is ADD")
    def updateOrg(org_identifier: str, body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Updates an Org object. You can modify Org properties such as name, description, and user associations.  Requires cluster administration (**Can administer Org**) privileges. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `ORG_ADMINISTRATION` (**Can manage Orgs**) privilege is required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/orgs/{org_identifier}/update"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if org_identifier is None:
            raise ValueError('Missing required path parameter: org_identifier')
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='exportAnswerReport', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nExports an Answer in the given file format. You can download the Answer data as a PDF, PNG, CSV, or XLSX file.\n\nRequires at least view access to the Answer.\n\n#### Usage guidelines\n\nIn the request body, the GUID or name of the Answer and set `file_format`. The default file format is CSV.\n\n**NOTE**:\n* The downloadable file returned in API response file is extensionless. Please rename the downloaded file by typing in the relevant extension.\n* HTML rendering is not supported for PDF exports of Answers with tables.\n\nOptionally, you can define [runtime overrides](https://developers.thoughtspot.com/docs/fetch-data-and-report-apis#_runtime_overrides) to apply to the Answer data.  \n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_identifier (string) (optional)\n    Unique ID or name of the metadata object.\n  - session_identifier (string) (optional)\n    Unique ID of the answer session.\n  - generation_number (integer) (optional)\n    Generation number of the answer session.\n  - file_format (string) (optional)\n    Export file format.\n  - runtime_filter (object) (optional)\n    JSON string representing runtime filter.\n{ col1:region, op1: EQ, val1: northeast }\n  - runtime_sort (object) (optional)\n    JSON string representing runtime sort.\n{ sortCol1: region, asc1 :true, sortCol2 : date }\n  - runtime_param_override (object) (optional)\n    JSON object for setting values of parameters in runtime.\n  - regional_settings (any) (optional)\n    Options for specific region specific overrides to support date/number/string/currency formatting.")
    def exportAnswerReport(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Exports an Answer in the given file format. You can download the Answer data as a PDF, PNG, CSV, or XLSX file.  Requires at least view access to the Answer.  #### Usage guidelines  In the request body, the GUID or name of the Answer and set `file_format`. The default file format is CSV.  **NOTE**: * The downloadable file returned in API response file is extensionless. Please rename the downloaded file by typing in the relevant extension. * HTML rendering is not supported for PDF exports of Answers with tables.  Optionally, you can define [runtime overrides](https://developers.thoughtspot.com/docs/fetch-data-and-report-apis#_runtime_overrides) to apply to the Answer data.      #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/report/answer"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='exportLiveboardReport', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nExports a Liveboard and its visualizations in PDF or PNG file format.\n\nRequires at least view access to the Liveboard.\n\n#### Usage guidelines\n\nIn the request body, specify the GUID or name of the Liveboard. To generate a Liveboard report with specific visualizations, add GUIDs or names of the visualizations.\n\nThe default `file_format` is PDF. For PDF downloads, you can specify additional parameters to customize the page orientation and include or exclude the cover page, logo, footer text, and page numbers. Similar customization options are also available for PNG output.\n\n**NOTE**: The downloadable file returned in API response file is extensionless. Please rename the downloaded file by typing in the relevant extension.\n\nOptionally, you can define [runtime overrides](https://developers.thoughtspot.com/docs/fetch-data-and-report-apis#_runtime_overrides) to apply to the Answer data.\n\nTo include unsaved changes in the report, pass the `transient_pinboard_content` script generated from the `getExportRequestForCurrentPinboard` method in the Visual Embed SDK. Upon successful execution, the API returns the report with unsaved changes, including ad hoc changes to visualizations. For more information, see [Liveboard Report API](https://developers.thoughtspot.com/docs/fetch-data-and-report-apis#_liveboard_report_api).  \n\n**NOTE**: Starting with ThoughtSpot Cloud 10.9.0.cl release, the Liveboard can be exported in the PNG format in the resolution of your choice. To enable this on your instance, contact ThoughtSpot support. When this feature is enabled, the options `include_cover_page`,`include_filter_page` within the `png_options` will not be available for PNG exports.\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_identifier (string) (required)\n    GUID or name of the Liveboard object.\n  - tab_identifiers (array of string) (optional)\n    GUID or name of the tab of the Liveboard object. <br/>  <span class=\"since-beta-tag\">Version: 10.9.0\n  - personalised_view_identifier (string) (optional)\n    GUID or name of the personalised view of the Liveboard object. <br/>  <span class=\"since-beta-tag\">V\n  - visualization_identifiers (array of string) (optional)\n    GUID or name of visualizations on the Liveboard. If this parameter is\nnot defined, the API returns a\n  - transient_content (string) (optional)\n    Transient content of the Liveboard.\n  - file_format (string) (optional)\n    Export file format.\n  - runtime_filter (object) (optional)\n    JSON object with representing filter condition to apply filters at runtime. For example, {\"col1\": \"r\n  - override_filters (object) (optional)\n    Applied to the liveboard and overrides any filters already applied on the same columns in liveboard.\n  - runtime_sort (object) (optional)\n    JSON string representing runtime sort. For example, {\"sortCol1\": \"region\", \"asc1\" : true}. For more \n  - pdf_options (any) (optional)\n    Options for PDF export.\n  - png_options (any) (optional)\n    Options for PNG export.\n  - runtime_param_override (object) (optional)\n    JSON object for setting values of parameters at runtime. For example, <code> {\"param1\": \"Double List\n  - regional_settings (any) (optional)\n    Options for specific region specific overrides to support date/number/string/currency formatting.")
    def exportLiveboardReport(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Exports a Liveboard and its visualizations in PDF or PNG file format.  Requires at least view access to the Liveboard.  #### Usage guidelines  In the request body, specify the GUID or name of the Liveboard. To generate a Liveboard report with specific visualizations, add GUIDs or names of the visualizations.  The default `file_format` is PDF. For PDF downloads, you can specify additional parameters to customize the page orientation and include or exclude the cover page, logo, footer text, and page numbers. Similar customization options are also available for PNG output.  **NOTE**: The downloadable file returned in API response file is extensionless. Please rename the downloaded file by typing in the relevant extension.  Optionally, you can define [runtime overrides](https://developers.thoughtspot.com/docs/fetch-data-and-report-apis#_runtime_overrides) to apply to the Answer data.  To include unsaved changes in the report, pass the `transient_pinboard_content` script generated from the `getExportRequestForCurrentPinboard` method in the Visual Embed SDK. Upon successful execution, the API returns the report with unsaved changes, including ad hoc changes to visualizations. For more information, see [Liveboard Report API](https://developers.thoughtspot.com/docs/fetch-data-and-report-apis#_liveboard_report_api).    **NOTE**: Starting with ThoughtSpot Cloud 10.9.0.cl release, the Liveboard can be exported in the PNG format in the resolution of your choice. To enable this on your instance, contact ThoughtSpot support. When this feature is enabled, the options `include_cover_page`,`include_filter_page` within the `png_options` will not be available for PNG exports.    #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/report/liveboard"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='createSchedule', description="\nCreate schedule. <br/>  <span class=\"since-beta-tag\">Version: 9.4.0.cl or later</span>\n\nCreates a Liveboard schedule job.\n\nRequires at least edit access to Liveboards. To create a schedule on behalf of another user, you need  `ADMINISTRATION` (**Can administer Org**) or `JOBSCHEDULING` (**Can schedule for others**) privilege and edit access to the Liveboard.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `JOBSCHEDULING` (**Can schedule for others**) privilege is required.\n\n#### Usage guidelines\n\n* The description text is mandatory.  The description text appears as **Description: <your content>** in the Liveboard schedule email notifications.\n* For Liveboards with both charts and tables, schedule creation is only supported in PDF and XLS formats. Schedules created in CSV formats for such Liveboards will fail to run. If `PDF` is set as the `file_format`, enable `pdf_options` to get the correct attachment. Not doing so may cause the attachment to be rendered empty.\n* To include only specific visualizations, specify the visualization GUIDs in the `visualization_identifiers` array.\n* You can schedule a Liveboard job to run periodically by setting frequency parameters. You can set the schedule to run daily, weekly, monthly or every n minutes or hours. The scheduled job can also be configured to run at a specific time of the day or on specific days of the week or month. Please ensure that when setting the schedule frequency for _minute of the object_, only values that are multiples of 5 are included.\n* If the `frequency` parameters are defined, you can set the time zone to a value that matches your server's time zone. For example, `US/Central`, `Etc/UTC`, `CET`. The default time zone is `America/Los_Angeles`.\n\nFor more information about Liveboard jobs, see [ThoughtSpot Product Documentation](https://docs.thoughtspot.com/cloud/latest/liveboard-schedule).\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (required)\n    Name of the scheduled job.\n  - description (string) (required)\n    Description of the job.\n  - metadata_type (string) (required)\n    Type of the metadata object.\n  - metadata_identifier (string) (required)\n    Unique ID or name of the metadata object.\n  - file_format (string) (optional)\n    Export file format.\n  - liveboard_options (any) (optional)\n    Options to specify details of Liveboard.\n  - pdf_options (any) (optional)\n    PDF layout and orientation settings. Applicable only if the `file_format` is specified as `PDF`.\n  - time_zone (string) (required)\n    Time zone\n  - frequency (any) (optional)\n    Frequency settings for the scheduled job.\n  - recipient_details (any) (required)\n    Recipients of the scheduled job notifications. Add the GUID or name of the ThoughtSpot users or grou\n  - personalised_view_id (string) (optional)\n    Personalised view id of the liveboard to be scheduled.")
    def createSchedule(body: Optional[dict] = None):
        ''' Create schedule. <br/>  <span class="since-beta-tag">Version: 9.4.0.cl or later</span>  Creates a Liveboard schedule job.  Requires at least edit access to Liveboards. To create a schedule on behalf of another user, you need  `ADMINISTRATION` (**Can administer Org**) or `JOBSCHEDULING` (**Can schedule for others**) privilege and edit access to the Liveboard. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `JOBSCHEDULING` (**Can schedule for others**) privilege is required.  #### Usage guidelines  * The description text is mandatory.  The description text appears as **Description: <your content>** in the Liveboard schedule email notifications. * For Liveboards with both charts and tables, schedule creation is only supported in PDF and XLS formats. Schedules created in CSV formats for such Liveboards will fail to run. If `PDF` is set as the `file_format`, enable `pdf_options` to get the correct attachment. Not doing so may cause the attachment to be rendered empty. * To include only specific visualizations, specify the visualization GUIDs in the `visualization_identifiers` array. * You can schedule a Liveboard job to run periodically by setting frequency parameters. You can set the schedule to run daily, weekly, monthly or every n minutes or hours. The scheduled job can also be configured to run at a specific time of the day or on specific days of the week or month. Please ensure that when setting the schedule frequency for _minute of the object_, only values that are multiples of 5 are included. * If the `frequency` parameters are defined, you can set the time zone to a value that matches your server's time zone. For example, `US/Central`, `Etc/UTC`, `CET`. The default time zone is `America/Los_Angeles`.  For more information about Liveboard jobs, see [ThoughtSpot Product Documentation](https://docs.thoughtspot.com/cloud/latest/liveboard-schedule).    #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/schedules/create"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='deleteSchedule', description="\nDeletes a scheduled job. <br/>  <span class=\"since-beta-tag\">Version: 9.4.0.cl or later</span>\n\nDeletes a scheduled Liveboard job.\n\nRequires at least edit access to Liveboard or `ADMINISTRATION` (**Can administer Org**) privilege.   \nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `JOBSCHEDULING` (**Can schedule for others**) privilege is required.\n\n\n\n\n#### Endpoint URL\n")
    def deleteSchedule(schedule_identifier: str):
        ''' Deletes a scheduled job. <br/>  <span class="since-beta-tag">Version: 9.4.0.cl or later</span>  Deletes a scheduled Liveboard job.  Requires at least edit access to Liveboard or `ADMINISTRATION` (**Can administer Org**) privilege.    If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `JOBSCHEDULING` (**Can schedule for others**) privilege is required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/schedules/{schedule_identifier}/delete"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if schedule_identifier is None:
            raise ValueError('Missing required path parameter: schedule_identifier')
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='searchSchedules', description="\nSearch Schedules <br/>  <span class=\"since-beta-tag\">Version: 9.4.0.cl or later</span>\n\nGets a list of scheduled jobs configured for a Liveboard.\n\nTo get details of a specific scheduled job, specify the name or GUID of the scheduled job.\n\nRequires at least view access to Liveboards.\n\n**NOTE**: When filtering schedules by parameters other than `metadata`, set `record_size` to `-1` and `record_offset` to `0` for accurate results.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata (array of object) (optional)\n    Metadata objects associated with the scheduled jobs.\n  - record_offset (integer) (optional)\n    The starting record number from where the records should be included.\n  - record_size (integer) (optional)\n    The number of records that should be included.\n  - sort_options (any) (optional)\n    Sort options.\n  - history_runs_options (any) (optional)\n    Options while fetching history runs for the schedule.\n  - schedule_identifiers (array of string) (optional)\n    unique ID or name of the Schedule")
    def searchSchedules(body: Optional[dict] = None):
        ''' Search Schedules <br/>  <span class="since-beta-tag">Version: 9.4.0.cl or later</span>  Gets a list of scheduled jobs configured for a Liveboard.  To get details of a specific scheduled job, specify the name or GUID of the scheduled job.  Requires at least view access to Liveboards.  **NOTE**: When filtering schedules by parameters other than `metadata`, set `record_size` to `-1` and `record_offset` to `0` for accurate results.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/schedules/search"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateSchedule', description="\nUpdate schedule. <br/>  <span class=\"since-beta-tag\">Version: 9.4.0.cl or later</span>\n\nUpdates a scheduled Liveboard job.\n\nRequires at least edit access to Liveboards. To update a schedule on behalf of another user, you need `ADMINISTRATION` (**Can administer Org**) or `JOBSCHEDULING` (**Can schedule for others**) privilege and edit access to the Liveboard.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `JOBSCHEDULING` (**Can schedule for others**) privilege is required.\n\nThe API endpoint allows you to pause a scheduled job, change the status of a paused job. You can also edit the recipients list, frequency of the job, format of the file to send to the recipients in email notifications, PDF options, and time zone setting.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (optional)\n    Name of the scheduled job.\n  - description (string) (optional)\n    Description of the scheduled job.\n  - metadata_type (string) (optional)\n    Type of metadata object.\n  - metadata_identifier (string) (optional)\n    Unique ID or name of the metadata object.\n  - file_format (string) (optional)\n    Export file format.\n  - liveboard_options (any) (optional)\n    Options to specify the details of a Liveboard.\n  - pdf_options (any) (optional)\n    Options for PDF export.\n  - time_zone (string) (optional)\n    Time zone\n  - frequency (any) (optional)\n    Frequency of the scheduled job run.\n  - recipient_details (any) (optional)\n    Recipients of the scheduled job notifications. You can add the ID or name of the ThoughtSpot users o\n  - status (string) (optional)\n    Status of the schedule\n  - personalised_view_id (string) (optional)\n    Personalised view id of the liveboard to be scheduled.")
    def updateSchedule(schedule_identifier: str, body: Optional[dict] = None):
        ''' Update schedule. <br/>  <span class="since-beta-tag">Version: 9.4.0.cl or later</span>  Updates a scheduled Liveboard job.  Requires at least edit access to Liveboards. To update a schedule on behalf of another user, you need `ADMINISTRATION` (**Can administer Org**) or `JOBSCHEDULING` (**Can schedule for others**) privilege and edit access to the Liveboard. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `JOBSCHEDULING` (**Can schedule for others**) privilege is required.  The API endpoint allows you to pause a scheduled job, change the status of a paused job. You can also edit the recipients list, frequency of the job, format of the file to send to the recipients in email notifications, PDF options, and time zone setting.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/schedules/{schedule_identifier}/update"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if schedule_identifier is None:
            raise ValueError('Missing required path parameter: schedule_identifier')
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='assignChangeAuthor', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nTransfers the ownership of one or several objects from one user to another.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\n\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege and edit access to the objects are required.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata (array of object) (required)\n    GUID or name of the metadata object.\n  - user_identifier (string) (required)\n    GUID or name of the user who you want to assign as the author.\n  - current_owner_identifier (string) (optional)\n    GUID or name of the current author.  When defined, the metadata objects\nauthored by the specified ow")
    def assignChangeAuthor(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Transfers the ownership of one or several objects from one user to another.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.  If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege and edit access to the objects are required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/security/metadata/assign"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='fetchColumnSecurityRules', description="\n<span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.12.0.cl or later</span>\n\nFetches column security rules for specified tables.\n\nThis API endpoint retrieves column-level security rules configured for tables. It returns information about which columns are secured and which groups have access to those columns.\n\n#### Usage guidelines\n\n- Provide an array of table identifiers using either `identifier` (GUID or name) or `obj_identifier` (object ID)\n- At least one of `identifier` or `obj_identifier` must be provided for each table\n- The API returns column security rules for all specified tables\n- Users must have appropriate permissions to access security rules for the specified tables\n\n#### Required permissions\n\n- `ADMINISTRATION` - Can administer ThoughtSpot\n- `DATAMANAGEMENT` - Can manage data\n- `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` - Can manage worksheet views and tables\n\n#### Example request\n\n```json\n{\n  \"tables\": [\n    {\n      \"identifier\": \"table-guid\",\n      \"obj_identifier\": \"table-object-id\"\n    }\n  ]\n}\n```\n\n#### Response format\n\nThe API returns an array of `ColumnSecurityRuleResponse` objects wrapped in a `data` field. Each `ColumnSecurityRuleResponse` object contains:\n- Table information (GUID and object ID)  \n- Array of column security rules with column details, group access, and source table information\n\n#### Example response\n\n```json\n{\n  \"data\": [\n    {\n      \"guid\": \"table-guid\",\n      \"objId\": \"table-object-id\",\n      \"columnSecurityRules\": [\n        {\n          \"column\": {\n            \"id\": \"col_123\",\n            \"name\": \"Salary\"\n          },\n          \"groups\": [\n            {\n              \"id\": \"group_1\",\n              \"name\": \"HR Department\"\n            }\n          ],\n          \"sourceTableDetails\": {\n            \"id\": \"source-table-guid\",\n            \"name\": \"Employee_Data\"\n          }\n        }\n      ]\n    }\n  ]\n}\n```\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - tables (array of object) (required)\n    Array of table identifier objects for which to fetch column security rules")
    def fetchColumnSecurityRules(body: Optional[dict] = None):
        ''' <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.12.0.cl or later</span>  Fetches column security rules for specified tables.  This API endpoint retrieves column-level security rules configured for tables. It returns information about which columns are secured and which groups have access to those columns.  #### Usage guidelines  - Provide an array of table identifiers using either `identifier` (GUID or name) or `obj_identifier` (object ID) - At least one of `identifier` or `obj_identifier` must be provided for each table - The API returns column security rules for all specified tables - Users must have appropriate permissions to access security rules for the specified tables  #### Required permissions  - `ADMINISTRATION` - Can administer ThoughtSpot - `DATAMANAGEMENT` - Can manage data - `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` - Can manage worksheet views and tables  #### Example request  ```json {   "tables": [     {       "identifier": "table-guid",       "obj_identifier": "table-object-id"     }   ] } ```  #### Response format  The API returns an array of `ColumnSecurityRuleResponse` objects wrapped in a `data` field. Each `ColumnSecurityRuleResponse` object contains: - Table information (GUID and object ID)   - Array of column security rules with column details, group access, and source table information  #### Example response  ```json {   "data": [     {       "guid": "table-guid",       "objId": "table-object-id",       "columnSecurityRules": [         {           "column": {             "id": "col_123",             "name": "Salary"           },           "groups": [             {               "id": "group_1",               "name": "HR Department"             }           ],           "sourceTableDetails": {             "id": "source-table-guid",             "name": "Employee_Data"           }         }       ]     }   ] } ```    #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/security/column/rules/fetch"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='fetchPermissionsOfPrincipals', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nFetches object permission details for a given principal object such as a user and group.\n\nRequires view access to the metadata object. \n\n#### Usage guidelines\n\n* To get a list of all metadata objects that a user or group can access, specify the `type` and GUID or name of the principal.\n* To get permission details for a specific object, add the `type` and GUID or name of the metadata object to your API request.\n\nUpon successful execution, the API returns a list of metadata objects and permission details for each object.  \n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - principals (array of object) (required)\n    GUID or name of the user or group.\n  - metadata (array of object) (optional)\n    Metadata objects for which you want to fetch permission details.\nIf not specified, the API returns p\n  - record_offset (integer) (optional)\n    The starting record number from where the records should be included for each metadata type.\n  - record_size (integer) (optional)\n    The number of records that should be included for each metadata type.\n  - default_metadata_type (string) (optional)\n    When no metadata objects input is passed, metadata objects of this type are fetched.")
    def fetchPermissionsOfPrincipals(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Fetches object permission details for a given principal object such as a user and group.  Requires view access to the metadata object.   #### Usage guidelines  * To get a list of all metadata objects that a user or group can access, specify the `type` and GUID or name of the principal. * To get permission details for a specific object, add the `type` and GUID or name of the metadata object to your API request.  Upon successful execution, the API returns a list of metadata objects and permission details for each object.       #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/security/principals/fetch-permissions"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='fetchPermissionsOnMetadata', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nFetches permission details for a given metadata object.\n\nRequires view access to the metadata object.\n\n#### Usage guidelines\n\n* To fetch a list of users and groups for a metadata object, specify `type` and GUID or name of the metadata object.\n* To get permission details for a specific user or group, add `type` and GUID or name of the principal object to your API request.\n\nUpon successful execution, the API returns permission details and principal information for the object specified in the API request.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata (array of object) (required)\n    GUID or name of the metadata object.\n  - principals (array of object) (optional)\n    User or group objects for which you want to fetch permissions.\nIf not specified, the API returns all\n  - include_dependent_objects (boolean) (optional)\n    Indicates whether to fetch permissions of dependent metadata objects.\n  - record_offset (integer) (optional)\n    The starting record number from where the records should be included for each metadata type.\n  - record_size (integer) (optional)\n    The number of records that should be included for each metadata type.\n  - permission_type (string) (optional)\n    <div><span class=\"since-beta-tag\">Version: 10.3.0.cl or later</span></div>\n\nSpecifies the type of pe")
    def fetchPermissionsOnMetadata(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Fetches permission details for a given metadata object.  Requires view access to the metadata object.  #### Usage guidelines  * To fetch a list of users and groups for a metadata object, specify `type` and GUID or name of the metadata object. * To get permission details for a specific user or group, add `type` and GUID or name of the principal object to your API request.  Upon successful execution, the API returns permission details and principal information for the object specified in the API request.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/security/metadata/fetch-permissions"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='publishMetadata', description="\n<span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.9.0.cl or later</span>\n\nAllows publishing metadata objects across organizations in ThoughtSpot.\n\nRequires ADMINISTRATION role and TENANT scope.\n\nThe API endpoint allows publishing the following types of metadata objects:\n* Liveboards\n* Answers\n* Logical Tables\n\nThis API will essentially share the objects along with it's dependencies to\nthe org admins of the orgs to which it is being published.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata (array of object) (required)\n    Metadata objects to be published.\n  - org_identifiers (array of string) (required)\n    Unique ID or name of orgs to which metadata objects should be published.\n  - skip_validation (boolean) (optional)\n    Skip validations of objects to be published.")
    def publishMetadata(body: Optional[dict] = None):
        ''' <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.9.0.cl or later</span>  Allows publishing metadata objects across organizations in ThoughtSpot.  Requires ADMINISTRATION role and TENANT scope.  The API endpoint allows publishing the following types of metadata objects: * Liveboards * Answers * Logical Tables  This API will essentially share the objects along with it's dependencies to the org admins of the orgs to which it is being published.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/security/metadata/publish"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='shareMetadata', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nAllows sharing one or several metadata objects with users and groups in ThoughtSpot.\n\nRequires edit access to the metadata object.\n\nThe API endpoint allows sharing only the following types of metadata objects:\n* Liveboards\n* Visualizations\n* Answers\n* Worksheets\n* Views\n* Connections\n\nYou can provide `READ_ONLY` or `MODIFY` access when sharing an object with another user or group. With `READ_ONLY` access grants view access to the shared object, whereas `MODIFY` provides edit access.\n\nTo prevent a user or group from accessing the shared object, specify the GUID or name of the principal and set `shareMode` to `NO_ACCESS`.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_type (string) (optional)\n    Type of metadata. Required if identifier in metadata_identifies is a name.\n1. Liveboard\n2. Answers\n3\n  - metadata_identifiers (array of string) (optional)\n    Unique ID or name of metadata objects. Note: All the names should belong to same metadata_type\n  - metadata (array of object) (optional)\n    Metadata details for sharing objects.\n  - permissions (array of object) (required)\n    Permission details for sharing the objects.\n  - visualization_identifiers (array of string) (optional)\n    Options to specify details of Liveboard. First Liveboard encountered in payload is considered to be \n  - emails (array of string) (optional)\n    Email IDs to which notifications will be sent.\n  - message (string) (required)\n    Message to be included in notification.\n  - enable_custom_url (boolean) (optional)\n    Sends object URLs in the customized format in email notifications.\n  - notify_on_share (boolean) (optional)\n    Flag to notify user when any object is shared.\n  - has_lenient_discoverability (boolean) (optional)\n    Flag to make the object discoverable.")
    def shareMetadata(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Allows sharing one or several metadata objects with users and groups in ThoughtSpot.  Requires edit access to the metadata object.  The API endpoint allows sharing only the following types of metadata objects: * Liveboards * Visualizations * Answers * Worksheets * Views * Connections  You can provide `READ_ONLY` or `MODIFY` access when sharing an object with another user or group. With `READ_ONLY` access grants view access to the shared object, whereas `MODIFY` provides edit access.  To prevent a user or group from accessing the shared object, specify the GUID or name of the principal and set `shareMode` to `NO_ACCESS`.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/security/metadata/share"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='unpublishMetadata', description="\n<span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.9.0.cl or later</span>\n\nAllows unpublishing metadata objects from organizations in ThoughtSpot.\n\nRequires ADMINISTRATION role and TENANT scope.\n\nThe API endpoint allows unpublishing the following types of metadata objects:\n* Liveboards\n* Answers\n* Logical Tables\n\nWhen unpublishing objects, you can:\n* Include dependencies by setting `include_dependencies` to true - this will unpublish all dependent objects if no other published object is using them\n* Force unpublish by setting `force` to true - this will break all dependent objects in the unpublished organizations\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - force (boolean) (optional)\n    Force unpublishes the object. This will break all the dependent objects in the unpublished orgs.\n  - include_dependencies (boolean) (required)\n    Should we unpublish all the dependencies for the objects specified.\nThe dependencies will be unpubli\n  - metadata (array of object) (required)\n    Metadata objects.\n  - org_identifiers (array of string) (required)\n    Unique ID or name of orgs.")
    def unpublishMetadata(body: Optional[dict] = None):
        ''' <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.9.0.cl or later</span>  Allows unpublishing metadata objects from organizations in ThoughtSpot.  Requires ADMINISTRATION role and TENANT scope.  The API endpoint allows unpublishing the following types of metadata objects: * Liveboards * Answers * Logical Tables  When unpublishing objects, you can: * Include dependencies by setting `include_dependencies` to true - this will unpublish all dependent objects if no other published object is using them * Force unpublish by setting `force` to true - this will break all dependent objects in the unpublished organizations     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/security/metadata/unpublish"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateColumnSecurityRules', description="\n<span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.12.0.cl or later</span>\n\nCreates, updates, or deletes column security rules for specified tables.\n\nThis API endpoint allows you to create, update, or delete column-level security rules on columns of a table. The operation follows an \"all or none\" policy: if defining security rules for any of the provided columns fails, the entire operation will be rolled back, and no rules will be created.\n\n#### Usage guidelines\n\n- Provide table identifier using either `identifier` (GUID or name) or `obj_identifier` (object ID)\n- Use `clear_csr: true` to remove all column security rules from the table\n- For each column, specify the security rule using `column_security_rules` array\n- Use `is_unsecured: true` to mark a specific column as unprotected\n- Use `group_access` operations to manage group associations:\n  - `ADD`: Add groups to the column's access list\n  - `REMOVE`: Remove groups from the column's access list\n  - `REPLACE`: Replace all existing groups with the specified groups\n\n#### Required permissions\n\n- `ADMINISTRATION` - Can administer ThoughtSpot\n- `DATAMANAGEMENT` - Can manage data (if RBAC is disabled)\n- `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` - Can manage worksheet views and tables (if RBAC is enabled)\n\n#### Example request\n\n```json\n{\n  \"identifier\": \"table-guid\",\n  \"obj_identifier\": \"table-object-id\",\n  \"clear_csr\": false,\n  \"column_security_rules\": [\n    {\n      \"column_identifier\": \"col id or col name\",\n      \"is_unsecured\": false,\n      \"group_access\": [\n        {\n          \"operation\": \"ADD\",\n          \"group_identifiers\": [\"hr_group_id\", \"hr_group_name\", \"finance_group_id\"]\n        }\n      ]\n    },\n    {\n      \"column_identifier\": \"col id or col name\",\n      \"is_unsecured\": true\n    },\n    {\n      \"column_identifier\": \"col id or col name\",\n      \"is_unsecured\": false,\n      \"group_access\": [\n        {\n          \"operation\": \"REPLACE\",\n          \"group_identifiers\": [\"management_group_id\", \"management_group_name\"]\n        }\n      ]\n    }\n  ]\n}\n```\n\n#### Request Body Schema\n\n- `identifier` (string, optional): GUID or name of the table for which we want to create column security rules\n- `obj_identifier` (string, optional): The object ID of the table\n- `clear_csr` (boolean, optional): If true, then all the secured columns will be marked as unprotected, and all the group associations will be removed\n- `column_security_rules` (array of objects, required): An array where each object defines the security rule for a specific column\n\nEach column security rule object contains:\n- `column_identifier` (string, required): Column identifier (col_id or name)\n- `is_unsecured` (boolean, optional): If true, the column will be marked as unprotected and all groups associated with it will be removed\n- `group_access` (array of objects, optional): Array of group operation objects\n\nEach group operation object contains:\n- `operation` (string, required): Operation type - ADD, REMOVE, or REPLACE\n- `group_identifiers` (array of strings, required): Array of group identifiers (name or GUID) on which the operation will be performed\n\n#### Response\n\nThis API does not return any response body. A successful operation returns HTTP 200 status code.\n\n#### Operation Types\n\n- **ADD**: Adds the specified groups to the column's access list\n- **REMOVE**: Removes the specified groups from the column's access list  \n- **REPLACE**: Replaces all existing groups with the specified groups \n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - identifier (string) (optional)\n    GUID or name of the table for which we want to create column security rules\n  - obj_identifier (string) (optional)\n    The object ID of the table\n  - clear_csr (boolean) (optional)\n    If true, then all the secured columns will be marked as unprotected, and all the group associations \n  - column_security_rules (array of object) (required)\n    Array where each object defines the security rule for a specific column")
    def updateColumnSecurityRules(body: Optional[dict] = None):
        ''' <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.12.0.cl or later</span>  Creates, updates, or deletes column security rules for specified tables.  This API endpoint allows you to create, update, or delete column-level security rules on columns of a table. The operation follows an "all or none" policy: if defining security rules for any of the provided columns fails, the entire operation will be rolled back, and no rules will be created.  #### Usage guidelines  - Provide table identifier using either `identifier` (GUID or name) or `obj_identifier` (object ID) - Use `clear_csr: true` to remove all column security rules from the table - For each column, specify the security rule using `column_security_rules` array - Use `is_unsecured: true` to mark a specific column as unprotected - Use `group_access` operations to manage group associations:   - `ADD`: Add groups to the column's access list   - `REMOVE`: Remove groups from the column's access list   - `REPLACE`: Replace all existing groups with the specified groups  #### Required permissions  - `ADMINISTRATION` - Can administer ThoughtSpot - `DATAMANAGEMENT` - Can manage data (if RBAC is disabled) - `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` - Can manage worksheet views and tables (if RBAC is enabled)  #### Example request  ```json {   "identifier": "table-guid",   "obj_identifier": "table-object-id",   "clear_csr": false,   "column_security_rules": [     {       "column_identifier": "col id or col name",       "is_unsecured": false,       "group_access": [         {           "operation": "ADD",           "group_identifiers": ["hr_group_id", "hr_group_name", "finance_group_id"]         }       ]     },     {       "column_identifier": "col id or col name",       "is_unsecured": true     },     {       "column_identifier": "col id or col name",       "is_unsecured": false,       "group_access": [         {           "operation": "REPLACE",           "group_identifiers": ["management_group_id", "management_group_name"]         }       ]     }   ] } ```  #### Request Body Schema  - `identifier` (string, optional): GUID or name of the table for which we want to create column security rules - `obj_identifier` (string, optional): The object ID of the table - `clear_csr` (boolean, optional): If true, then all the secured columns will be marked as unprotected, and all the group associations will be removed - `column_security_rules` (array of objects, required): An array where each object defines the security rule for a specific column  Each column security rule object contains: - `column_identifier` (string, required): Column identifier (col_id or name) - `is_unsecured` (boolean, optional): If true, the column will be marked as unprotected and all groups associated with it will be removed - `group_access` (array of objects, optional): Array of group operation objects  Each group operation object contains: - `operation` (string, required): Operation type - ADD, REMOVE, or REPLACE - `group_identifiers` (array of strings, required): Array of group identifiers (name or GUID) on which the operation will be performed  #### Response  This API does not return any response body. A successful operation returns HTTP 200 status code.  #### Operation Types  - **ADD**: Adds the specified groups to the column's access list - **REMOVE**: Removes the specified groups from the column's access list   - **REPLACE**: Replaces all existing groups with the specified groups     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/security/column/rules/update"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateSystemConfig', description="\n <span class=\"since-beta-tag\">Version: 9.2.0.cl or later</span>\n\nUpdates the current configuration of the cluster. You must send the configuration data in JSON format.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privileges.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `APPLICATION_ADMINISTRATION` (**Can manage application settings**) privilege is required.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - configuration (object) (required)\n    Configuration JSON with the key-value pair of configuration attributes to be updated.")
    def updateSystemConfig(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.2.0.cl or later</span>  Updates the current configuration of the cluster. You must send the configuration data in JSON format.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privileges. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `APPLICATION_ADMINISTRATION` (**Can manage application settings**) privilege is required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/system/config-update"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='assignTag', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nAssigns tags to Liveboards, Answers, Tables, and Worksheets.\n\nRequires edit access to the metadata object.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata (array of object) (required)\n    Metadata objects.\n  - tag_identifiers (array of string) (required)\n    GUID or name of the tag.")
    def assignTag(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Assigns tags to Liveboards, Answers, Tables, and Worksheets.  Requires edit access to the metadata object.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/tags/assign"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='createTag', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nCreates a tag object.\n\nTags are labels that identify a metadata object. For example, you can create a tag to designate subject areas, such as sales, HR, marketing, and finance.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `TAGMANAGEMENT` (**Can manage tags**) privilege is required to create, edit, and delete tags.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (required)\n    Name of the tag.\n  - color (string) (optional)\n    Hex color code to be assigned to the tag. For example, #ff78a9.")
    def createTag(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Creates a tag object.  Tags are labels that identify a metadata object. For example, you can create a tag to designate subject areas, such as sales, HR, marketing, and finance.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `TAGMANAGEMENT` (**Can manage tags**) privilege is required to create, edit, and delete tags.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/tags/create"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='deleteTag', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nDeletes a tag object from the ThoughtSpot system\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `TAGMANAGEMENT` (**Can manage tags**) privilege is required to create, edit, and delete tags.\n\n\n\n\n#### Endpoint URL\n")
    def deleteTag(tag_identifier: str):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Deletes a tag object from the ThoughtSpot system  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `TAGMANAGEMENT` (**Can manage tags**) privilege is required to create, edit, and delete tags.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/tags/{tag_identifier}/delete"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if tag_identifier is None:
            raise ValueError('Missing required path parameter: tag_identifier')
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='searchTags', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nGets a list of tag objects available on the ThoughtSpot system. To get details of a specific tag object, specify the GUID or name.\n\nAny authenticated user can search for tag objects.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - tag_identifier (string) (optional)\n    Name or Id of the tag.\n  - name_pattern (string) (optional)\n    A pattern to match case-insensitive name of the Tag object.\n  - color (string) (optional)\n    Color of the tag.")
    def searchTags(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Gets a list of tag objects available on the ThoughtSpot system. To get details of a specific tag object, specify the GUID or name.  Any authenticated user can search for tag objects.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/tags/search"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='unassignTag', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nRemoves the tags applied to a Liveboard, Answer, Table, or Worksheet.\n\nRequires edit access to the metadata object.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata (array of object) (required)\n    Metadata objects.\n  - tag_identifiers (array of string) (required)\n    GUID or name of the tag.")
    def unassignTag(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Removes the tags applied to a Liveboard, Answer, Table, or Worksheet.  Requires edit access to the metadata object.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/tags/unassign"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateTag', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nUpdates a tag object.\n\nYou can modify the `name` and `color` properties of a tag object.  \n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `TAGMANAGEMENT` (**Can manage tags**) privilege is required to create, edit, and delete tags.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (optional)\n    Name of the tag.\n  - color (string) (optional)\n    Hex color code to be assigned to the tag. For example, #ff78a9.")
    def updateTag(tag_identifier: str, body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Updates a tag object.  You can modify the `name` and `color` properties of a tag object.    Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `TAGMANAGEMENT` (**Can manage tags**) privilege is required to create, edit, and delete tags.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/tags/{tag_identifier}/update"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if tag_identifier is None:
            raise ValueError('Missing required path parameter: tag_identifier')
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='activateUser', description="\n <span class=\"since-beta-tag\">Version: 9.7.0.cl or later</span>\n\nActivates a deactivated user account.\n\nRequires `ADMINISTRATION` (**Can administer Thoughtspot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.\n\nTo activate an inactive user account, the API request body must include the following information:\n\n- Username or the GUID of the user account.\n- Auth token generated for the deactivated user. The auth token is sent in the API response when a user is deactivated.\n- Password for the user account.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - user_identifier (string) (required)\n    Unique ID or name of the user.\n  - auth_token (string) (required)\n    Auth token for the user.\n  - password (string) (required)\n    New password for the user to access the account.\n  - properties (string) (optional)\n    Properties of the user.")
    def activateUser(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.7.0.cl or later</span>  Activates a deactivated user account.  Requires `ADMINISTRATION` (**Can administer Thoughtspot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.  To activate an inactive user account, the API request body must include the following information:  - Username or the GUID of the user account. - Auth token generated for the deactivated user. The auth token is sent in the API response when a user is deactivated. - Password for the user account.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/users/activate"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='changeUserPassword', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nUpdates the current password of the user.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - current_password (string) (required)\n    Current password of the user.\n  - new_password (string) (required)\n    New password for the user.\n  - user_identifier (string) (required)\n    GUID or name of the user.")
    def changeUserPassword(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Updates the current password of the user.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/users/change-password"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='createUser', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nCreates a user in ThoughtSpot.\n\nThe API endpoint allows you to configure several user properties such as email address, account status, share notification preferences, and sharing visibility. You can provision the user to [groups](https://docs.thoughtspot.com/cloud/latest/groups-privileges) and [Orgs](https://docs.thoughtspot.com/cloud/latest/orgs-overview). You can also add Liveboard, Answer, and Worksheet objects to the user\u2019s favorites list, assign a default Liveboard for the user, and set user preferences.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (required)\n    Name of the user. The username string must be unique.\n  - display_name (string) (required)\n    A unique display name string for the user account, usually their first and last name\n  - password (string) (optional)\n    Password for the user account. For IAMv2 users, you must set this password if you do not want to tri\n  - email (string) (required)\n    Email of the user account\n  - account_type (string) (optional)\n    Type of the account.\n  - account_status (string) (optional)\n    Current status of the user account. The `SUSPENDED` user state indicates a transitional state applic\n  - org_identifiers (array of string) (optional)\n    List of Org IDs to which the user belongs.\n  - group_identifiers (array of string) (optional)\n    GUIDs or names of the groups to which the newly created user belongs.\n  - visibility (string) (optional)\n    Visibility of the users. When set to SHARABLE, the user is visible to other users and groups when th\n  - notify_on_share (boolean) (optional)\n    User preference for receiving email notifications when another ThoughtSpot\nuser shares a metadata ob\n  - show_onboarding_experience (boolean) (optional)\n    The user preference for revisiting the onboarding experience.\n  - onboarding_experience_completed (boolean) (optional)\n    flag to get the on-boarding experience is completed or not.\n  - home_liveboard_identifier (string) (optional)\n    GUID of the Liveboard to set a default Liveboard for the user.\nThoughtSpot displays this Liveboard o\n  - favorite_metadata (array of object) (optional)\n    Metadata objects to add to the user's favorites list.\n  - preferred_locale (string) (optional)\n    Locale for the user.\n  - extended_properties (object) (optional)\n    Properties for the user\n  - extended_preferences (object) (optional)\n    Preferences for the user\n  - trigger_welcome_email (boolean) (optional)\n    Flag to indicate whether welcome email should be sent to user. This parameter is applied only on clu\n  - trigger_activation_email (boolean) (optional)\n    Flag to indicate whether activation email should be sent to the user. Default value for IAMv2 users ")
    def createUser(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Creates a user in ThoughtSpot.  The API endpoint allows you to configure several user properties such as email address, account status, share notification preferences, and sharing visibility. You can provision the user to [groups](https://docs.thoughtspot.com/cloud/latest/groups-privileges) and [Orgs](https://docs.thoughtspot.com/cloud/latest/orgs-overview). You can also add Liveboard, Answer, and Worksheet objects to the user’s favorites list, assign a default Liveboard for the user, and set user preferences.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/users/create"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='deactivateUser', description="\n <span class=\"since-beta-tag\">Version: 9.7.0.cl or later</span>\n\nDeactivates a user account.\n\nRequires `ADMINISTRATION` (**Can administer Thoughtspot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.\n\nTo deactivate a user account, the API request body must include the following information:\n\n- Username or the GUID of the user account\n- Base URL of the ThoughtSpot instance\n\nIf the API request is successful, ThoughtSpot returns the activation URL in the response. The activation URL is valid for 14 days and can be used to re-activate the account and reset the password of the deactivated account.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - user_identifier (string) (required)\n    Unique ID or name of the user.\n  - base_url (string) (required)\n    Base url of the cluster.")
    def deactivateUser(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.7.0.cl or later</span>  Deactivates a user account.  Requires `ADMINISTRATION` (**Can administer Thoughtspot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.  To deactivate a user account, the API request body must include the following information:  - Username or the GUID of the user account - Base URL of the ThoughtSpot instance  If the API request is successful, ThoughtSpot returns the activation URL in the response. The activation URL is valid for 14 days and can be used to re-activate the account and reset the password of the deactivated account.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/users/deactivate"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='deleteUser', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nDeletes a user from the ThoughtSpot system.\n\nIf you want to remove a user from a specific Org but not from ThoughtSpot, update the group and Org mapping properties of the user object via a POST API call to the [/api/rest/2.0/users/{user_identifier}/update](#/http/api-endpoints/users/update-user) endpoint.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.\n\n\n\n\n#### Endpoint URL\n")
    def deleteUser(user_identifier: str):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Deletes a user from the ThoughtSpot system.  If you want to remove a user from a specific Org but not from ThoughtSpot, update the group and Org mapping properties of the user object via a POST API call to the [/api/rest/2.0/users/{user_identifier}/update](#/http/api-endpoints/users/update-user) endpoint.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/users/{user_identifier}/delete"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if user_identifier is None:
            raise ValueError('Missing required path parameter: user_identifier')
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='forceLogoutUsers', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nEnforces logout on current user sessions.  \n\nUse this API with caution as it may invalidate active user sessions and force users to re-login. Make sure you specify the usernames or GUIDs. If you pass null values in the API call, all user sessions on your cluster become invalid, and the users are forced to re-login.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - user_identifiers (array of string) (optional)\n    GUID or name of the users for force logging out their sessions.")
    def forceLogoutUsers(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Enforces logout on current user sessions.    Use this API with caution as it may invalidate active user sessions and force users to re-login. Make sure you specify the usernames or GUIDs. If you pass null values in the API call, all user sessions on your cluster become invalid, and the users are forced to re-login.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/users/force-logout"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='importUsers', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nImports user data from external databases into ThoughtSpot. During the user import operation:\n\n* If the specified users are not available in ThoughtSpot, the users are created and assigned a default password. Defining a `default_password` in the API request is optional.\n* If `delete_unspecified_users` is set to `true`, the users not specified in the API request, excluding the `tsadmin`, `guest`, `system` and `su` users, are deleted.\n* If the specified user objects are already available in ThoughtSpot, the object properties are updated and synchronized as per the input data in the API request.\n\nA successful API call returns the object that represents the changes made in the ThoughtSpot system.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - users (array of object) (required)\n    List of users needs to be imported.\n  - default_password (string) (optional)\n    The default password to assign to users if they do not have a password assigned in ThoughtSpot.\n  - dry_run (boolean) (optional)\n    If true, the API performs a test operation and returns user IDs whose data will be edited after the \n  - delete_unspecified_users (boolean) (optional)\n    If set to true, removes the users that are not specified in the API request.")
    def importUsers(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Imports user data from external databases into ThoughtSpot. During the user import operation:  * If the specified users are not available in ThoughtSpot, the users are created and assigned a default password. Defining a `default_password` in the API request is optional. * If `delete_unspecified_users` is set to `true`, the users not specified in the API request, excluding the `tsadmin`, `guest`, `system` and `su` users, are deleted. * If the specified user objects are already available in ThoughtSpot, the object properties are updated and synchronized as per the input data in the API request.  A successful API call returns the object that represents the changes made in the ThoughtSpot system.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/users/import"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='resetUserPassword', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nResets the password of a user account. Administrators can reset password on behalf of a user.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - new_password (string) (required)\n    New password for the user.\n  - user_identifier (string) (required)\n    GUID or name of the user.")
    def resetUserPassword(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Resets the password of a user account. Administrators can reset password on behalf of a user.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/users/reset-password"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='searchUsers', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nGets a list of users available on the ThoughtSpot system.\n\nTo get details of a specific user, specify the user GUID or name. You can also filter the API response based on groups, Org ID, user visibility, account status, user type, and user preference settings and favorites.\n\nAvailable to all users. Users with `ADMINISTRATION` (**Can administer ThoughtSpot**) privileges can view all users properties. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.\n\n**NOTE**: If the API returns an empty list, consider increasing the value of the `record_size` parameter. To search across all available users, set `record_size` to `-1`.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - user_identifier (string) (optional)\n    GUID / name of the user to search\n  - display_name (string) (optional)\n    A unique display name string for the user account, usually their first and last name\n  - name_pattern (string) (optional)\n    A pattern to match case-insensitive name of the User object.\n  - visibility (string) (optional)\n    Visibility of the user\n  - email (string) (optional)\n    Email of the user account\n  - group_identifiers (array of string) (optional)\n    GUID or name of the group to which the user belongs\n  - privileges (array of string) (optional)\n    Privileges assigned to the user\n  - account_type (string) (optional)\n    Type of the account\n  - account_status (string) (optional)\n    Current status of the user account.\n  - notify_on_share (boolean) (optional)\n    User preference for receiving email notifications when another ThoughtSpot user\nshares a metadata ob\n  - show_onboarding_experience (boolean) (optional)\n    The user preference for revisiting the onboarding experience\n  - onboarding_experience_completed (boolean) (optional)\n    Indicates if the user has completed the onboarding walkthrough\n  - org_identifiers (array of string) (optional)\n    IDs or names of the Orgs to which the user belongs\n  - home_liveboard_identifier (string) (optional)\n    Unique ID or name of the user's home Liveboard.\n  - favorite_metadata (array of object) (optional)\n    Metadata objects which are assigned as favorites of the user.\n  - record_offset (integer) (optional)\n    The starting record number from where the records should be included.\n  - record_size (integer) (optional)\n    The number of records that should be included.\n  - sort_options (any) (optional)\n    Sort options.\n  - role_identifiers (array of string) (optional)\n    Filters by the role assigned to the user.\n  - include_favorite_metadata (boolean) (optional)\n    Indicates if the user's favorite objects should be displayed.")
    def searchUsers(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Gets a list of users available on the ThoughtSpot system.  To get details of a specific user, specify the user GUID or name. You can also filter the API response based on groups, Org ID, user visibility, account status, user type, and user preference settings and favorites.  Available to all users. Users with `ADMINISTRATION` (**Can administer ThoughtSpot**) privileges can view all users properties. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.  **NOTE**: If the API returns an empty list, consider increasing the value of the `record_size` parameter. To search across all available users, set `record_size` to `-1`.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/users/search"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateUser', description="\n <span class=\"since-beta-tag\">Version: 9.0.0.cl or later</span>\n\nUpdates the properties of a user object.\n\nYou can modify user properties such as username, email, and share notification settings. You can also assign new groups and Orgs, remove the user from a group or Org, reset password, and modify user preferences.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (optional)\n    Name of the user. The username string must be unique.\n  - display_name (string) (optional)\n    A unique display name string for the user account, usually their first and last name\n  - visibility (string) (optional)\n    Visibility of the users. When set to SHARABLE, the user is visible to other users and groups when th\n  - email (string) (optional)\n    Email of the user account\n  - account_status (string) (optional)\n    Current status of the user account. The `SUSPENDED` user state indicates a transitional state applic\n  - notify_on_share (boolean) (optional)\n    User preference for receiving email notifications when another ThoughtSpot user\nshares a metadata ob\n  - show_onboarding_experience (boolean) (optional)\n    The user preference for revisiting the onboarding experience.\n  - onboarding_experience_completed (boolean) (optional)\n    Indicates if the user has completed the onboarding and allows turning off the onboarding walkthrough\n  - account_type (string) (optional)\n    Type of the account.\n  - group_identifiers (array of string) (optional)\n    GUIDs or names of the groups.\n  - home_liveboard_identifier (string) (optional)\n    GUID of the Liveboard to set a default Liveboard for the user. ThoughtSpot displays\nthis Liveboard o\n  - favorite_metadata (array of object) (optional)\n    Metadata objects to add to the user's favorites list.\n  - org_identifiers (array of string) (optional)\n    IDs of the Orgs.\n  - operation (string) (optional)\n    Type of update operation. Default operation type is REPLACE\n  - preferred_locale (string) (optional)\n    Locale for the user.\n  - extended_properties (object) (optional)\n    Properties for the user\n  - extended_preferences (object) (optional)\n    Preferences for the user")
    def updateUser(user_identifier: str, body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.0.0.cl or later</span>  Updates the properties of a user object.  You can modify user properties such as username, email, and share notification settings. You can also assign new groups and Orgs, remove the user from a group or Org, reset password, and modify user preferences.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege. If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance, the `USER_ADMINISTRATION` (**Can manage users**) privilege is required.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/users/{user_identifier}/update"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if user_identifier is None:
            raise ValueError('Missing required path parameter: user_identifier')
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='createVariable', description="\nCreate a variable which can be used for parameterizing metadata objects <br/> <span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.9.0.cl or later</span>\n\nAllows creating a variable which can be used for parameterizing metadata objects in ThoughtSpot.\n\nRequires ADMINISTRATION role and TENANT scope.\n\nThe API endpoint supports the following types of variables:\n* CONNECTION_PROPERTY - For connection properties\n* TABLE_MAPPING - For table mappings\n* CONNECTION_PROPERTY_PER_PRINCIPAL - For connection properties per principal. In order to use this please contact support to enable this.\n* FORMULA_VARIABLE - For Formula variables\n\nWhen creating a variable, you need to specify:\n* The variable type\n* A unique name for the variable\n* Whether the variable contains sensitive values (defaults to false)\n* The data type of the variable, only specify for fomula variables (defaults to null)\n\nThe operation will fail if:\n* The user lacks required permissions\n* The variable name already exists\n* The variable type is invalid\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - type (string) (required)\n    Type of variable\n  - name (string) (required)\n    Name of the variable. This is unique across the cluster.\n  - sensitive (boolean) (optional)\n    If the variable contains sensitive values like passwords\n  - values (array of object) (optional)\n    Values of variable")
    def createVariable(body: Optional[dict] = None):
        ''' Create a variable which can be used for parameterizing metadata objects <br/> <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.9.0.cl or later</span>  Allows creating a variable which can be used for parameterizing metadata objects in ThoughtSpot.  Requires ADMINISTRATION role and TENANT scope.  The API endpoint supports the following types of variables: * CONNECTION_PROPERTY - For connection properties * TABLE_MAPPING - For table mappings * CONNECTION_PROPERTY_PER_PRINCIPAL - For connection properties per principal. In order to use this please contact support to enable this. * FORMULA_VARIABLE - For Formula variables  When creating a variable, you need to specify: * The variable type * A unique name for the variable * Whether the variable contains sensitive values (defaults to false) * The data type of the variable, only specify for fomula variables (defaults to null)  The operation will fail if: * The user lacks required permissions * The variable name already exists * The variable type is invalid    #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/template/variables/create"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='deleteVariable', description="\nDelete a variable <br/> <span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.9.0.cl or later</span>\n\nAllows deleting a variable from ThoughtSpot.\n\nRequires ADMINISTRATION role and TENANT scope.\n\nThe API endpoint requires:\n* The variable identifier (ID or name)\n\nThe operation will fail if:\n* The user lacks required permissions\n* The variable doesn't exist\n* The variable is being used by other objects \n\n\n\n#### Endpoint URL\n")
    def deleteVariable(identifier: str):
        ''' Delete a variable <br/> <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.9.0.cl or later</span>  Allows deleting a variable from ThoughtSpot.  Requires ADMINISTRATION role and TENANT scope.  The API endpoint requires: * The variable identifier (ID or name)  The operation will fail if: * The user lacks required permissions * The variable doesn't exist * The variable is being used by other objects     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/template/variables/{identifier}/delete"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if identifier is None:
            raise ValueError('Missing required path parameter: identifier')
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='searchVariables', description="\nSearch variables <br/> <span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.9.0.cl or later</span>\n\nAllows searching for variables in ThoughtSpot.\n\nRequires ADMINISTRATION role.\n\nThe API endpoint supports searching variables by:\n* Variable identifier (ID or name)\n* Variable type\n* Name pattern (case-insensitive, supports % for wildcard)\n\nThe search results can be formatted in three ways:\n* METADATA_ONLY - Returns only variable metadata (default)\n* METADATA_AND_VALUES - Returns variable metadata and values\n* EDITABLE_METADATA_AND_VALUES - Returns only editable variable metadata and values\n\nThe values can be filtered by scope:\n* org_identifier\n* principal_identifier\n* model_identifier\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - variable_details (array of object) (optional)\n    Variable details\n  - record_offset (integer) (optional)\n    The starting record number from where the records should be included\n  - record_size (integer) (optional)\n    The number of records that should be included\n  - output_format (string) (optional)\n    Format in which we want the output")
    def searchVariables(body: Optional[dict] = None):
        ''' Search variables <br/> <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.9.0.cl or later</span>  Allows searching for variables in ThoughtSpot.  Requires ADMINISTRATION role.  The API endpoint supports searching variables by: * Variable identifier (ID or name) * Variable type * Name pattern (case-insensitive, supports % for wildcard)  The search results can be formatted in three ways: * METADATA_ONLY - Returns only variable metadata (default) * METADATA_AND_VALUES - Returns variable metadata and values * EDITABLE_METADATA_AND_VALUES - Returns only editable variable metadata and values  The values can be filtered by scope: * org_identifier * principal_identifier * model_identifier     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/template/variables/search"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateVariable', description="\nUpdate a variable's properties <br/> <span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.9.0.cl or later</span>\n\nAllows updating a variable's properties in ThoughtSpot.\n\nRequires ADMINISTRATION role and TENANT scope.\n\nThe API endpoint allows updating:\n* The variable name\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - name (string) (optional)\n    New name of the variable if we want to rename.\n  - operation (string) (optional)\n    Operation to perform on the values.\n  - values (array of object) (optional)\n    Values of variable to be updated.")
    def updateVariable(identifier: str, body: Optional[dict] = None):
        ''' Update a variable's properties <br/> <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.9.0.cl or later</span>  Allows updating a variable's properties in ThoughtSpot.  Requires ADMINISTRATION role and TENANT scope.  The API endpoint allows updating: * The variable name    #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/template/variables/{identifier}/update"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if identifier is None:
            raise ValueError('Missing required path parameter: identifier')
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateVariableValues', description="\nUpdate values for multiple variables <br/> <span class=\"since-beta-tag\">Beta</span> <span class=\"since-beta-tag\">Version: 10.9.0.cl or later</span>\n\nAllows updating values for multiple variables in ThoughtSpot.\n\nRequires ADMINISTRATION role.\n\nThe API endpoint allows:\n* Adding new values to variables\n* Replacing existing values\n* Deleting values from variables\n\nWhen updating variable values, you need to specify:\n* The variable identifiers\n* The values to add/replace/remove for each variable\n* The operation to perform (ADD, REPLACE, REMOVE, CLEAR)\n\nBehaviour based on operation type:\n* ADD - Adds values to the variable if this is a list type variable, else same as replace.\n* REPLACE - Replaces all values of a given set of constraints with the current set of values.\n* REMOVE - Removes any values which match the set of conditions of the variables if this is a list type variable, else clears value.\n* CLEAR - Removes all constrains for a given variable, scope is ignored\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - variable_updates (array of object) (required)\n    Variables and values\n  - operation (string) (required)\n    Type of update operation")
    def updateVariableValues(body: Optional[dict] = None):
        ''' Update values for multiple variables <br/> <span class="since-beta-tag">Beta</span> <span class="since-beta-tag">Version: 10.9.0.cl or later</span>  Allows updating values for multiple variables in ThoughtSpot.  Requires ADMINISTRATION role.  The API endpoint allows: * Adding new values to variables * Replacing existing values * Deleting values from variables  When updating variable values, you need to specify: * The variable identifiers * The values to add/replace/remove for each variable * The operation to perform (ADD, REPLACE, REMOVE, CLEAR)  Behaviour based on operation type: * ADD - Adds values to the variable if this is a list type variable, else same as replace. * REPLACE - Replaces all values of a given set of constraints with the current set of values. * REMOVE - Removes any values which match the set of conditions of the variables if this is a list type variable, else clears value. * CLEAR - Removes all constrains for a given variable, scope is ignored     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/template/variables/update"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='commitBranch', description="\n <span class=\"since-beta-tag\">Version: 9.2.0.cl or later</span>\n\nCommits TML files of metadata objects to the Git branch configured on your instance.\n\nRequires at least edit access to objects used in the commit operation.\n\nBefore using this endpoint to push your commits:\n\n* Enable Git integration on your instance.\n* Make sure the Git repository and branch details are configured on your instance.\n\nFor more information, see [Git integration documentation](https://developers.thoughtspot.com/docs/git-integration).\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata (array of object) (required)\n    Metadata objects.\n  - delete_aware (boolean) (optional)\n    Delete the tml files from version control repo if it does not exist in the ThoughSpot instance\n  - branch_name (string) (optional)\n       Name of the remote branch where object should be pushed\n   \n\nNote: If no branch_name is specified\n  - comment (string) (required)\n    Comment to be added to the commit")
    def commitBranch(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.2.0.cl or later</span>  Commits TML files of metadata objects to the Git branch configured on your instance.  Requires at least edit access to objects used in the commit operation.  Before using this endpoint to push your commits:  * Enable Git integration on your instance. * Make sure the Git repository and branch details are configured on your instance.  For more information, see [Git integration documentation](https://developers.thoughtspot.com/docs/git-integration).     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/vcs/git/branches/commit"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='createConfig', description="\n <span class=\"since-beta-tag\">Version: 9.2.0.cl or later</span>\n\nAllows you to connect a ThoughtSpot instance to a Git repository.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\n\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance on your instance, the `CAN_SETUP_VERSION_CONTROL` (**Can set up version control**) privilege.\n\nYou can use this API endpoint to connect your ThoughtSpot development and production environments to the development and production branches of a Git repository.\n\nBefore using this endpoint to connect your ThoughtSpot instance to a Git repository, check the following prerequisites:\n\n* You have a Git repository. If you are using GitHub, make sure you have a valid account and an access token to connect ThoughtSpot to GitHub. For information about generating a token, see [GitHub Documentation](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/managing-your-personal-access-tokens).\n\n* Your access token has `repo` scope that grants full access to public and private repositories.\n* Your Git repository has a branch that can be configured as a default branch in ThoughtSpot.\n\nFor more information, see [Git integration documentation](https://developers.thoughtspot.com/docs/?pageid=git-integration).\n\n**Note**: ThoughtSpot supports only GitHub / itHub Enterprise for CI/CD.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - repository_url (string) (required)\n    URL for connecting to remote repository\n  - username (string) (required)\n    Username to authenticate connection to remote repository\n  - access_token (string) (required)\n    Access token corresponding to the user to authenticate connection to remote repository\n  - org_identifier (string) (optional)\n       Applicable when Orgs is enabled in the cluster\n   \n\nList of Org ids or name. Provide value -1 for\n  - branch_names (array of string) (optional)\n    List the remote branches to configure. Example:[development, production]\n  - commit_branch_name (string) (optional)\n    Name of the remote branch where objects from this Thoughtspot instance will be versioned. <br/>  <sp\n  - enable_guid_mapping (boolean) (optional)\n    Maintain mapping of guid for the deployment to an instance <br/>  <span class=\"since-beta-tag\">Versi\n  - configuration_branch_name (string) (optional)\n       Name of the branch where the configuration files related to operations between Thoughtspot and ve")
    def createConfig(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.2.0.cl or later</span>  Allows you to connect a ThoughtSpot instance to a Git repository.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.  If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance on your instance, the `CAN_SETUP_VERSION_CONTROL` (**Can set up version control**) privilege.  You can use this API endpoint to connect your ThoughtSpot development and production environments to the development and production branches of a Git repository.  Before using this endpoint to connect your ThoughtSpot instance to a Git repository, check the following prerequisites:  * You have a Git repository. If you are using GitHub, make sure you have a valid account and an access token to connect ThoughtSpot to GitHub. For information about generating a token, see [GitHub Documentation](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/managing-your-personal-access-tokens).  * Your access token has `repo` scope that grants full access to public and private repositories. * Your Git repository has a branch that can be configured as a default branch in ThoughtSpot.  For more information, see [Git integration documentation](https://developers.thoughtspot.com/docs/?pageid=git-integration).  **Note**: ThoughtSpot supports only GitHub / itHub Enterprise for CI/CD.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/vcs/git/config/create"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='deleteConfig', description="\n <span class=\"since-beta-tag\">Version: 9.2.0.cl or later</span>\n\nDeletes Git repository configuration from your ThoughtSpot instance.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\n\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance on your instance, the `CAN_SETUP_VERSION_CONTROL` (**Can set up version control**) privilege.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - cluster_level (boolean) (optional)\n       Applicable when Orgs is enabled in the cluster\n   \n\nIndicator to consider cluster level or org le")
    def deleteConfig(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.2.0.cl or later</span>  Deletes Git repository configuration from your ThoughtSpot instance.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.  If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance on your instance, the `CAN_SETUP_VERSION_CONTROL` (**Can set up version control**) privilege.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/vcs/git/config/delete"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='deployCommit', description="\n <span class=\"since-beta-tag\">Version: 9.2.0.cl or later</span>\n\nAllows you to deploy a commit and publish TML content to your ThoughtSpot instance.\n\nRequires at least edit access to the objects used in the deploy operation.\n\nThe API deploys the head of the branch unless a `commit_id` is specified in the API request. If the branch name is not defined in the request, the default branch is considered for deploying commits.\n\nFor more information, see [Git integration documentation](https://developers.thoughtspot.com/docs/git-integration).\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - commit_id (string) (optional)\n       Commit_id against which the files should be picked to deploy.\n   \n\nNote: If no commit_id is speci\n  - branch_name (string) (required)\n    Name of the remote branch where changes should be picked\n  - deploy_type (string) (optional)\n    Indicates if all files or only modified file at specified commit point should be considered\n  - deploy_policy (string) (optional)\n    Define the policy to follow while importing TML in the ThoughtSpot environment. Use \u201cALL_OR_NONE\u201d to")
    def deployCommit(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.2.0.cl or later</span>  Allows you to deploy a commit and publish TML content to your ThoughtSpot instance.  Requires at least edit access to the objects used in the deploy operation.  The API deploys the head of the branch unless a `commit_id` is specified in the API request. If the branch name is not defined in the request, the default branch is considered for deploying commits.  For more information, see [Git integration documentation](https://developers.thoughtspot.com/docs/git-integration).     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/vcs/git/commits/deploy"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='revertCommit', description="\n <span class=\"since-beta-tag\">Version: 9.2.0.cl or later</span>\n\nReverts TML objects to a previous commit specified in the API request.\n\nRequires at least edit access to objects.\n\nIn the API request, specify the `commit_id`. If the branch name is not specified in the request, the API will consider the default branch configured on your instance.\n\nBy default, the API reverts all objects. If the revert operation fails for one of the objects provided in the commit, the API returns an error and does not revert any object.\n\nFor more information, see [Git integration documentation](https://developers.thoughtspot.com/docs/git-integration).\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata (array of object) (optional)\n    Metadata objects.\n  - branch_name (string) (optional)\n       Name of the branch where the reverted version should be committed\n   \n\nNote: If no branch_name is\n  - revert_policy (string) (optional)\n    Policy to apply when reverting a commit. Valid values: [ALL_OR_NONE, PARTIAL]")
    def revertCommit(commit_id: str, body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.2.0.cl or later</span>  Reverts TML objects to a previous commit specified in the API request.  Requires at least edit access to objects.  In the API request, specify the `commit_id`. If the branch name is not specified in the request, the API will consider the default branch configured on your instance.  By default, the API reverts all objects. If the revert operation fails for one of the objects provided in the commit, the API returns an error and does not revert any object.  For more information, see [Git integration documentation](https://developers.thoughtspot.com/docs/git-integration).     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/vcs/git/commits/{commit_id}/revert"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        if commit_id is None:
            raise ValueError('Missing required path parameter: commit_id')
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='searchCommits', description="\n <span class=\"since-beta-tag\">Version: 9.2.0.cl or later</span>\n\nGets a list of commits for a given metadata object.\n\nRequires `DATAMANAGEMENT` (**Can manage data**) privilege.\n\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance on your instance, the `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**) privilege and edit access to the metadata objects.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - metadata_identifier (string) (required)\n    Unique ID or name of the metadata.\n  - metadata_type (string) (optional)\n    Type of metadata.\n  - branch_name (string) (optional)\n       Name of the branch from which commit history needs to be displayed.\n   \n\nNote: If no branch_name \n  - record_offset (integer) (optional)\n        Record offset point in the commit history to display the response.\n    \n\nNote: If no record offs\n  - record_size (integer) (optional)\n        Number of history records from record offset point to be displayed in the response.\n    \n\nNote: ")
    def searchCommits(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.2.0.cl or later</span>  Gets a list of commits for a given metadata object.  Requires `DATAMANAGEMENT` (**Can manage data**) privilege.  If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance on your instance, the `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**) privilege and edit access to the metadata objects.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/vcs/git/commits/search"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='searchConfig', description="\n <span class=\"since-beta-tag\">Version: 9.2.0.cl or later</span>\n\nGets Git repository connections configured on the ThoughtSpot instance.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\n\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance on your instance, the `CAN_SETUP_VERSION_CONTROL` (**Can set up version control**) privilege.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - org_identifiers (array of string) (optional)\n       Applicable when Orgs is enabled in the cluster\n   \n\nList of Org ids or name. Provide value -1 for")
    def searchConfig(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.2.0.cl or later</span>  Gets Git repository connections configured on the ThoughtSpot instance.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.  If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance on your instance, the `CAN_SETUP_VERSION_CONTROL` (**Can set up version control**) privilege.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/vcs/git/config/search"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='updateConfig', description="\n <span class=\"since-beta-tag\">Version: 9.2.0.cl or later</span>\n\nUpdates Git repository configuration settings.\n\nRequires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.\n\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance on your instance, the `CAN_SETUP_VERSION_CONTROL` (**Can set up version control**) privilege.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - username (string) (optional)\n    Username to authenticate connection to version control system\n  - access_token (string) (optional)\n    Access token corresponding to the user to authenticate connection to version control system\n  - org_identifier (string) (optional)\n       Applicable when Orgs is enabled in the cluster\n   \n\nList of Org ids or name. Provide value -1 for\n  - branch_names (array of string) (optional)\n    List the remote branches to configure. Example:[development, production]\n  - commit_branch_name (string) (optional)\n    Name of the remote branch where objects from this Thoughtspot instance will be versioned. <br/>  <sp\n  - enable_guid_mapping (boolean) (optional)\n    Maintain mapping of guid for the deployment to an instance <br/>  <span class=\"since-beta-tag\">Versi\n  - configuration_branch_name (string) (optional)\n    Name of the branch where the configuration files related to operations between Thoughtspot and versi")
    def updateConfig(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.2.0.cl or later</span>  Updates Git repository configuration settings.  Requires `ADMINISTRATION` (**Can administer ThoughtSpot**) privilege.  If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance on your instance, the `CAN_SETUP_VERSION_CONTROL` (**Can set up version control**) privilege.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/vcs/git/config/update"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    @mcp.tool(name='validateMerge', description="\n <span class=\"since-beta-tag\">Version: 9.2.0.cl or later</span>\n\nValidates the content of your source branch against the objects in your destination environment.\n\nBefore merging content from your source branch to the destination branch, run this API operation from your destination environment and ensure that the changes from the source branch function in the destination environment.\n\nRequires `DATAMANAGEMENT` (**Can manage data**) privilege.\n\nIf [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance on your instance, the `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**) privilege and edit access to the metadata objects.\n\n\n\n\n#### Endpoint URL\n\n\nRequest Body Schema:\nThe 'body' parameter should be a JSON object with the following structure:\n  - source_branch_name (string) (required)\n    Name of the branch from which changes need to be picked for validation\n  - target_branch_name (string) (required)\n    Name of the branch where files will be merged")
    def validateMerge(body: Optional[dict] = None):
        '''  <span class="since-beta-tag">Version: 9.2.0.cl or later</span>  Validates the content of your source branch against the objects in your destination environment.  Before merging content from your source branch to the destination branch, run this API operation from your destination environment and ensure that the changes from the source branch function in the destination environment.  Requires `DATAMANAGEMENT` (**Can manage data**) privilege.  If [Role-Based Access Control (RBAC)](https://developers.thoughtspot.com/docs/rbac) is enabled on your instance on your instance, the `CAN_MANAGE_WORKSHEET_VIEWS_TABLES` (**Can manage data models**) privilege and edit access to the metadata objects.     #### Endpoint URL '''
        base_url = os.getenv("BASE_URL", "https://localhost:443")
        api_url = f"{base_url}/api/rest/2.0/vcs/git/branches/validate"
        # Fix double slashes in URL (except after protocol)
        api_url = re.sub(r"(?<!:)/+", "/", api_url)
        params = {}
        headers = {}
        cookies = {}
        extra_headers = os.getenv('EXTRA_HEADERS')
        if extra_headers:
            try:
                for k, v in json.loads(extra_headers).items():
                    headers[k] = v
            except Exception as e:
                print('Failed to parse EXTRA_HEADERS:', e, file=sys.stderr)
        auth_satisfied = False
        auth_error_msgs = []
        if not auth_satisfied:
            try:
                token = os.getenv('BEARERAUTH_TOKEN')
                if not token:
                    raise ValueError('Missing Bearer token in .env: BEARERAUTH_TOKEN')
                headers['Authorization'] = f'Bearer {token}'
                auth_satisfied = True
            except Exception as e:
                auth_error_msgs.append(str(e))
        if not auth_satisfied:
            raise ValueError('No valid authentication found. Details: ' + '; '.join(auth_error_msgs))
        json_data = body if body is not None else None
        headers['Content-Type'] = 'application/json'
        try:
            resp = requests.post(api_url, params=params, headers=headers, cookies=cookies, json=json_data)
            resp.raise_for_status()  # Raise an exception for bad status codes
            if 'application/json' in resp.headers.get('Content-Type', ''):
                return resp.json()
            else:
                return {'raw': resp.text, 'status_code': resp.status_code}
        except requests.exceptions.RequestException as e:
            raise ValueError(f'HTTP request failed: {str(e)}')
        except Exception as e:
            raise ValueError(f'Unexpected error: {str(e)}')

    mcp.run(transport="stdio")
